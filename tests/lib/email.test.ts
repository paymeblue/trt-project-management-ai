import { describe, it } from 'vitest'

describe('email utility (Resend)', () => {
  it.todo('EMAIL-01: sendEmail sends verification + reset email via Resend')
  it.todo('EMAIL-02: sendEmail is a reusable server-only utility')
})
