import { describe, it } from 'vitest'

describe('DAL (NextAuth session-based)', () => {
  it.todo('AUTH-06: requireRole calls forbidden() on role mismatch')
  it.todo('AUTH-07: verifySession() rejects calls without a NextAuth session (auth())')
})
