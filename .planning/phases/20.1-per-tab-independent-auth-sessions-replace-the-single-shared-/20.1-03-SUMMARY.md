---
phase: 20.1-per-tab-independent-auth-sessions-replace-the-single-shared-
plan: "03"
subsystem: auth
tags: [nextauth, jwt, route-handler]
status: partial — blocked at Task 1.5 checkpoint

requires:
  - phase: 20.1-01
    provides: lib/tab-session.ts (mintTabAccessToken/verifyTabToken)
  - phase: 20.1-02
    provides: actions/tab-auth.ts (tabSigninAction), app/(auth)/sign-in/new-session-form.tsx
provides:
  - app/api/auth/tab-refresh/route.ts — POST Route Handler minting a fresh
    per-tab access token from a valid refresh token
affects: [20.1-03 remaining tasks, 20.1-04]

tech-stack:
  added: []
  patterns:
    - "Route Handlers that mint credentials (rather than consume them) never call verifySession() — tab-refresh is a credential source, not a protected endpoint"

key-files:
  created:
    - app/api/auth/tab-refresh/route.ts
    - tests/app/api/tab-refresh.test.ts

key-decisions: []

requirements-completed: []

duration: (in progress — paused at Task 1.5)
completed: null
---

# Phase 20.1 Plan 03: Fetch-Wrapper Transport + Silent Refresh + Entry/Sign-Out UI Summary (PARTIAL)

**Only Task 1 (the tab-refresh Route Handler) is complete. Execution is PAUSED at Task 1.5, a blocking human-verify checkpoint that must confirm the fetch-wrapper header-injection mechanism survives Next 16.2.9 soft navigation before Tasks 2-4 are built on top of it.**

## Status

Task 1 complete and committed. Tasks 2, 3, and the Task 4 checkpoint have NOT been started — the plan's own Decision D-20.1-03-A requires Task 1.5 to be confirmed first, since Tasks 2-3 build the real fetch wrapper on top of the mechanism Task 1.5 verifies.

## Task Commits

1. **Task 1: tab-refresh Route Handler** — `3749c09` (feat)
   - `app/api/auth/tab-refresh/route.ts`: `POST` reads `{ refreshToken }`, calls `verifyTabToken`, requires `typ === 'refresh'`, mints a new access token via `mintTabAccessToken`, returns `{ accessToken, expiresAt }`. Never calls `verifySession()`.
   - `tests/app/api/tab-refresh.test.ts`: 5 passing cases (valid refresh → 200; invalid/expired → 401; `typ='access'` presented as refresh → 401 cross-typ rejection; malformed non-JSON body → 400; missing `refreshToken` field → 400).
   - Verified: `npx vitest run tests/app/api/tab-refresh.test.ts` (5/5 passing), `npx tsc --noEmit` clean, `npm run lint` clean (0 errors, pre-existing unrelated warnings only).

## Checkpoint Pending

**Task 1.5** (`checkpoint:human-verify`, `gate="blocking-human"`) is next and has NOT been attempted or faked. It requires a real browser + dev server to confirm RESEARCH.md's one MEDIUM-confidence assumption (A1): that a `window.fetch` override survives a Next.js client-side soft navigation (`router.push`) in this exact Next 16.2.9 + React 19.2.4 install, before Tasks 2-3 build the real, conditional fetch-wrapper provider on top of it.

See the orchestrator-facing checkpoint report for exact steps and resume signal.

## Deviations from Plan

None — Task 1 executed exactly as written.

## Next Steps

Once Task 1.5 is confirmed ("confirmed" resume signal), a continuation executor should resume at Task 2 (build `TabSessionProvider`), then Task 3 (entry link + per-tab sign-out control), then stop again at Task 4's blocking checkpoint.

---
*Phase: 20.1-per-tab-independent-auth-sessions-replace-the-single-shared-*
*Status: PARTIAL — paused at Task 1.5*
