---
phase: 01-foundation-auth-roles-schema-dal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - drizzle.config.ts
  - db/schema.ts
  - db/index.ts
  - vitest.config.ts
  - tests/db/schema.test.ts
  - tests/actions/auth.test.ts
  - tests/lib/dal.test.ts
  - tests/lib/email.test.ts
  - .env.example
  - .env.local
autonomous: false   # drizzle-kit push may prompt interactively
requirements: [CHK-01]
user_setup:
  - service: neon-auth
    why: "Neon Auth must be enabled in the Neon Console to obtain NEON_AUTH_BASE_URL (used by Plan 02). Schema push only needs the existing DATABASE_URL."
    env_vars:
      - name: NEON_AUTH_BASE_URL
        source: "Neon Console -> Project -> Branch -> Auth tab -> Enable"
      - name: NEON_AUTH_COOKIE_SECRET
        source: "Generate locally: openssl rand -base64 32"
    dashboard_config:
      - task: "Enable Neon Auth on the project branch"
        location: "Neon Console -> Project -> Branch -> Auth tab"

must_haves:
  truths:
    - "The full Drizzle schema is pushed to Neon and all tables exist"
    - "Checklists are defined as data (definition -> template items -> responses); no checklist line items are hardcoded in TypeScript"
    - "Vitest runs and the Wave 0 test stub files exist (red/skipped until later waves implement)"
  artifacts:
    - path: "db/schema.ts"
      provides: "All Phase 1 Drizzle tables incl. process_diagrams, conversations, messages, chat_messages, ai_usage, token tables"
      contains: "process_diagrams"
    - path: "db/index.ts"
      provides: "server-only Drizzle client (neon-http)"
      contains: "drizzle-orm/neon-http"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at db/schema.ts"
      contains: "./db/schema.ts"
    - path: "vitest.config.ts"
      provides: "Vitest test runner config"
      contains: "vitest/config"
  key_links:
    - from: "drizzle.config.ts"
      to: "db/schema.ts"
      via: "schema path"
      pattern: "schema:.*db/schema\\.ts"
    - from: "db/index.ts"
      to: "db/schema.ts"
      via: "import * as schema"
      pattern: "import \\* as schema from './schema'"
---

<objective>
Lay the data + tooling foundation for Phase 1: install Drizzle/Neon/Vitest, author the COMPLETE Drizzle schema (all Phase 1 tables plus the chat/diagram/token tables needed by later phases so migrations don't churn), push it to Neon, and create the Vitest config + test stubs the rest of the phase verifies against.

Purpose: Every later plan (auth, DAL, email) imports the DB client and schema. The schema must exist and be pushed before any auth/DAL code is written, and the test harness must exist so each later task carries a real automated verify.
Output: package.json deps, drizzle.config.ts, db/schema.ts, db/index.ts, vitest.config.ts, four test stub files, .env.example, updated .env.local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-roles-schema-dal/01-RESEARCH.md
@.planning/phases/01-foundation-auth-roles-schema-dal/01-VALIDATION.md
@./AGENTS.md
@./CLAUDE.md

<project_layout>
CRITICAL: This project uses a ROOT layout, not src/. The research's `src/` paths are WRONG for this repo.
- App Router lives in `app/` at the repo root (NOT `src/app`).
- `tsconfig.json` maps `@/*` -> `./*` (repo root). So `@/db` = `./db`, `@/lib` = `./lib`, `@/actions` = `./actions`.
- Create `db/`, `lib/`, `actions/`, `tests/` directories at the repo ROOT.
- Existing files: app/layout.tsx, app/page.tsx, app/globals.css. DATABASE_URL already set in .env.local.
</project_layout>

<verified_package_facts>
From STACK.md VERIFIED block + 01-RESEARCH.md (override any stale guesses):
- drizzle-orm @ 0.45.2, drizzle-kit @ 0.31.10 (devDep), @neondatabase/serverless @ 1.1.0
- server-only @ 0.0.1, zod @ 4.4.3
- DB client uses `drizzle-orm/neon-http` adapter with `neon(process.env.DATABASE_URL!)`
- Test: vitest + @vitejs/plugin-react (devDeps)
- AGENTS.md: read node_modules/next/dist/docs/ before writing Next.js code. No src/ — root layout.
</verified_package_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, author full Drizzle schema, write DB client + config</name>
  <files>package.json, drizzle.config.ts, db/schema.ts, db/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-auth-roles-schema-dal/01-RESEARCH.md section "## Drizzle Schema (Phase 1 Full)" (lines 601-805) — the schema, drizzle.config, db client, and package.json scripts
    - <scope_change_note> in the planning brief — the EXTRA tables required beyond the research schema
    - ./package.json (current state) and ./tsconfig.json (path alias)
  </read_first>
  <action>
    Install exact versions:
    `npm install "@neondatabase/serverless@1.1.0" "drizzle-orm@0.45.2" "server-only@0.0.1" "zod@4.4.3"`
    `npm install -D "drizzle-kit@0.31.10" "tsx"`

    Create `db/schema.ts` containing EVERY table from 01-RESEARCH.md "## Drizzle Schema (Phase 1 Full)" VERBATIM (users, projects, checklistDefinitions, checklistTemplateItems, checklists, checklistResponses, attachments, processes, chatMessages, aiUsage, staticContent + all pgEnums), PLUS these ADDITIONAL tables (scope change — they only need to EXIST and be pushed; features built later):

    ```typescript
    // ── Process Diagrams (Phase 7 — React Flow / Mermaid state) ─────────────
    export const processDiagrams = pgTable('process_diagrams', {
      id:        uuid('id').primaryKey().defaultRandom(),
      title:     text('title').notNull(),
      state:     jsonb('state').notNull(),            // React Flow nodes/edges + mermaid source
      ownerId:   uuid('owner_id').notNull().references(() => users.id),
      createdAt: timestamp('created_at').defaultNow().notNull(),
      updatedAt: timestamp('updated_at').defaultNow().notNull(),
    })

    // ── Conversations (Phase 8 — human chat) ────────────────────────────────
    export const conversations = pgTable('conversations', {
      id:        uuid('id').primaryKey().defaultRandom(),
      createdBy: uuid('created_by').notNull().references(() => users.id),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    })

    export const conversationParticipants = pgTable('conversation_participants', {
      id:             uuid('id').primaryKey().defaultRandom(),
      conversationId: uuid('conversation_id').notNull().references(() => conversations.id, { onDelete: 'cascade' }),
      userId:         uuid('user_id').notNull().references(() => users.id),
      createdAt:      timestamp('created_at').defaultNow().notNull(),
    })

    // ── Messages (Phase 8 — human chat messages) ────────────────────────────
    export const messages = pgTable('messages', {
      id:             uuid('id').primaryKey().defaultRandom(),
      conversationId: uuid('conversation_id').notNull().references(() => conversations.id, { onDelete: 'cascade' }),
      senderId:       uuid('sender_id').notNull().references(() => users.id),
      body:           text('body').notNull(),
      createdAt:      timestamp('created_at').defaultNow().notNull(),
    })

    // ── Auth Tokens (Phase 1 — email verification + password reset) ─────────
    // Used by Plan 04 ONLY if @neondatabase/auth does not expose its own
    // verification/reset flow. Created now to avoid later migration churn.
    export const verificationTokens = pgTable('verification_tokens', {
      id:        uuid('id').primaryKey().defaultRandom(),
      userId:    uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      tokenHash: text('token_hash').notNull().unique(),  // store a hash, never the raw token
      expiresAt: timestamp('expires_at').notNull(),
      usedAt:    timestamp('used_at'),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    })

    export const passwordResetTokens = pgTable('password_reset_tokens', {
      id:        uuid('id').primaryKey().defaultRandom(),
      userId:    uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      tokenHash: text('token_hash').notNull().unique(),  // store a hash, never the raw token
      expiresAt: timestamp('expires_at').notNull(),
      usedAt:    timestamp('used_at'),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    })
    ```

    Note: `chatMessages` (AI history) and `aiUsage` (rate limiting) are ALREADY in the research schema — include them as-is, do NOT duplicate.

    Create `drizzle.config.ts` exactly as in research (schema: './db/schema.ts', out: './drizzle', dialect: 'postgresql', dbCredentials.url from DATABASE_URL).

    Create `db/index.ts` exactly as research "### DB Client": `import 'server-only'`, `neon(process.env.DATABASE_URL!)`, `drizzle(sql, { schema })` from `drizzle-orm/neon-http`, `import * as schema from './schema'`.

    Add to package.json scripts: `"db:push": "drizzle-kit push"`, `"db:generate": "drizzle-kit generate"`, `"db:migrate": "drizzle-kit migrate"`, `"db:studio": "drizzle-kit studio"`, `"db:seed-admin": "tsx db/seed-admin.ts"`, `"test": "vitest run"`, `"test:watch": "vitest"`.

    Run `npm run lint` and `npx tsc --noEmit` — both must pass (schema must compile).
  </action>
  <acceptance_criteria>
    - `grep -c "pgTable(" db/schema.ts` returns 17 or more (11 research tables + processDiagrams, conversations, conversationParticipants, messages, verificationTokens, passwordResetTokens)
    - `grep -q "process_diagrams" db/schema.ts && grep -q "conversations" db/schema.ts && grep -q "messages" db/schema.ts && grep -q "ai_usage" db/schema.ts && grep -q "chat_messages" db/schema.ts && grep -q "password_reset_tokens" db/schema.ts` exits 0
    - `grep -q "drizzle-orm/neon-http" db/index.ts && grep -q "import 'server-only'" db/index.ts` exits 0
    - `grep -q "./db/schema.ts" drizzle.config.ts` exits 0
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['drizzle-orm']==='0.45.2' && p.dependencies['@neondatabase/serverless']==='1.1.0' ? 0 : 1)"` exits 0
    - `npx tsc --noEmit` exits 0
    - No hardcoded checklist line-item literals: `grep -rIn "delivery_project_label\|sorting_label" db/ app/ lib/ 2>/dev/null` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "process_diagrams" db/schema.ts && grep -q "password_reset_tokens" db/schema.ts && grep -q "drizzle-orm/neon-http" db/index.ts</automated>
  </verify>
  <done>db/schema.ts has all 17+ tables incl. process_diagrams/conversations/messages/token tables; db/index.ts and drizzle.config.ts exist and compile; deps pinned to exact research versions.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config, test stubs, env scaffolding</name>
  <files>vitest.config.ts, tests/db/schema.test.ts, tests/actions/auth.test.ts, tests/lib/dal.test.ts, tests/lib/email.test.ts, .env.example, .env.local</files>
  <read_first>
    - .planning/phases/01-foundation-auth-roles-schema-dal/01-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
    - .planning/phases/01-foundation-auth-roles-schema-dal/01-RESEARCH.md "### Required Environment Variables" (lines 117-126) and "## Environment Availability" (lines 942-960)
    - ./.env.local (current — only DATABASE_URL present)
  </read_first>
  <action>
    Install test deps: `npm install -D vitest @vitejs/plugin-react`.

    Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'node:path'

    export default defineConfig({
      plugins: [react()],
      test: { environment: 'node', globals: true, include: ['tests/**/*.test.ts'] },
      resolve: { alias: { '@': path.resolve(__dirname, '.') } },
    })
    ```

    Create four test stub files. Each MUST import vitest and contain at least one `it.todo(...)` per requirement it covers so the file is valid and runnable but does not falsely pass. Use this shape (example for auth):
    ```typescript
    import { describe, it } from 'vitest'

    describe('auth actions', () => {
      it.todo('AUTH-01: signupAction accepts a valid email/password')
      it.todo('AUTH-02: signupAction rejects role=super_admin')
      it.todo('AUTH-03: requestPasswordReset issues a Resend email with a token')
      it.todo('AUTH-04: signinAction/signoutAction call auth.signIn/signOut')
    })
    ```
    - tests/actions/auth.test.ts -> AUTH-01, AUTH-02, AUTH-03, AUTH-04 todos
    - tests/lib/dal.test.ts -> AUTH-06, AUTH-07 todos
    - tests/db/schema.test.ts -> CHK-01 todo (and an active test asserting the schema module exports `checklistDefinitions`, `checklistTemplateItems`, `checklistResponses` — `import * as schema from '@/db/schema'` then `expect(schema.checklistDefinitions).toBeDefined()`)
    - tests/lib/email.test.ts -> EMAIL-01, EMAIL-02 todos

    Append the auth/email env keys to `.env.local` (placeholders — real values are user_setup): add `NEON_AUTH_BASE_URL=`, `NEON_AUTH_COOKIE_SECRET=`, `RESEND_API_KEY=`, `EMAIL_FROM=onboarding@resend.dev`, `APP_URL=http://localhost:3000`, `ADMIN_EMAIL=`, `ADMIN_PASSWORD=`, `ADMIN_NAME=Super Admin` (do NOT overwrite the existing DATABASE_URL line).

    Create `.env.example` documenting ALL keys with empty/placeholder values and a one-line comment each: DATABASE_URL, NEON_AUTH_BASE_URL, NEON_AUTH_COOKIE_SECRET, RESEND_API_KEY, EMAIL_FROM, APP_URL, ADMIN_EMAIL, ADMIN_PASSWORD, ADMIN_NAME. NEVER prefix secrets with NEXT_PUBLIC_.

    Run `npx vitest run` — must execute with 0 failures (todos are pending, the schema export assertion passes).
  </action>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with at least 1 passing test and no failures
    - All four files exist: `ls tests/actions/auth.test.ts tests/lib/dal.test.ts tests/db/schema.test.ts tests/lib/email.test.ts` exits 0
    - `grep -q "AUTH-01" tests/actions/auth.test.ts && grep -q "EMAIL-01" tests/lib/email.test.ts && grep -q "AUTH-07" tests/lib/dal.test.ts` exits 0
    - `grep -q "RESEND_API_KEY" .env.example && grep -q "NEON_AUTH_COOKIE_SECRET" .env.example && grep -q "APP_URL" .env.example` exits 0
    - `grep -c "NEXT_PUBLIC_" .env.example` returns 0 (no secret is client-exposed)
    - `grep -q "RESEND_API_KEY" .env.local && grep -q "NEON_AUTH_BASE_URL" .env.local` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run && grep -q "RESEND_API_KEY" .env.example && test "$(grep -c NEXT_PUBLIC_ .env.example)" = "0"</automated>
  </verify>
  <done>Vitest runs green; four test stub files exist with per-req todos + a passing schema-export assertion; .env.example documents every key; .env.local has auth/email placeholders alongside DATABASE_URL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Push full schema to Neon</name>
  <what-built>db/schema.ts (full Phase 1 schema) and drizzle.config.ts from Tasks 1-2.</what-built>
  <how-to-verify>
    Claude runs `npm run db:push` (drizzle-kit push). This connects to Neon using the existing DATABASE_URL and creates all tables/enums. drizzle-kit push may print an interactive confirmation prompt for the statements it will execute — confirm/accept it. Build and type checks pass WITHOUT the push (types come from the schema file, not the live DB), so this push is mandatory to make schema verification real rather than a false positive.

    After push, verify tables exist. Either:
    1. Watch the drizzle-kit output report all tables as created, OR
    2. Run `npm run db:studio` and confirm users, checklist_definitions, checklist_template_items, checklist_responses, projects, attachments, processes, process_diagrams, conversations, conversation_participants, messages, chat_messages, ai_usage, static_content, verification_tokens, password_reset_tokens are present.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run db:push` completes without error (all CREATE statements applied)
    - At least the 17 expected tables exist in Neon (confirmed via drizzle-kit output or studio)
  </acceptance_criteria>
  <resume-signal>Type "pushed" once drizzle-kit reports success, or paste the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev -> Neon Postgres | DATABASE_URL grants full DDL; schema push runs locally only |
| repo -> git remote | .env.local/.env must never be committed (secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | DATABASE_URL / RESEND_API_KEY in env | mitigate | `.env*` is gitignored (confirmed); `.env.example` carries empty placeholders only; no NEXT_PUBLIC_ prefix on any secret (grep-asserted in Task 2) |
| T-01-02 | Tampering | drizzle-kit push against shared/prod DB | mitigate | Push targets the dev Neon branch via local DATABASE_URL only; research forbids push in CI/CD; production uses generate+migrate (documented in package.json scripts) |
| T-01-03 | Information Disclosure | db/index.ts leaking to client bundle | mitigate | `import 'server-only'` at top of db/index.ts (grep-asserted in Task 1) |
| T-01-04 | Repudiation | no audit trail on records | accept | createdAt/updatedAt timestamps on all tables; full audit logging out of scope for v1 |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` passes (green, todos pending)
- `npm run db:push` applied all tables to Neon (Task 3)
- `grep -c "pgTable(" db/schema.ts` >= 17
- No checklist line items hardcoded (CHK-01): grep for label literals returns nothing
</verification>

<success_criteria>
- Full Drizzle schema (incl. process_diagrams, conversations, messages, chat_messages, ai_usage, token tables) exists and is pushed to Neon
- db/index.ts (server-only) + drizzle.config.ts compile and point at db/schema.ts
- Vitest configured; four test stubs exist with per-requirement todos; suite runs green
- .env.example documents every key; no secret is client-exposed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-roles-schema-dal/01-01-SUMMARY.md`
</output>
