---
phase: 16-workflow-engine-core
plan: 05
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - app/(app)/workflow/step/page.tsx
  - app/_components/workflow-kinds/yes-no-upload-step.tsx
  - app/_components/workflow-kinds/approval-step.tsx
  - app/_components/workflow-kinds/assignment-step.tsx
autonomous: true
requirements: [WF-03]
must_haves:
  truths:
    - "A yes_no_upload step renders a yes/no control plus an optional upload button"
    - "An approval step renders a send control and (for the counterparty) a receive control"
    - "An assignment step renders a picker of users filtered to the step's target role"
    - "Each renderer submits through the plan 03 server actions"
  artifacts:
    - path: "app/_components/workflow-kinds/yes-no-upload-step.tsx"
      provides: "minimal yes/no + optional upload renderer"
      contains: "submitYesNoUploadAction"
    - path: "app/(app)/workflow/step/page.tsx"
      provides: "server route that resolves a graph step by projectId+step key and renders the matching kind component"
      contains: "getStepByKey"
  key_links:
    - from: "app/_components/workflow-kinds/*"
      to: "actions/workflow-graph.ts"
      via: "server action calls on submit"
      pattern: "Action"
---

<objective>
Prove WF-03's "renders its correct interface" criterion with minimal (not polished — this phase is under --skip-ui) renderers for the three new fulfillment kinds, plus a server route that resolves a graph step and dispatches to the right renderer. Correctness over polish: each control must present the right inputs and submit through the plan 03 server actions.

Purpose: Demonstrates the new kinds have working UI surfaces on the test graph.
Output: A /workflow/step route + three kind renderers.

SCOPE: Minimal UX only. No styling system work, no responsive polish — that is Phases 18/19/21. These exist to prove the kinds render and gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md

<interfaces>
From actions/workflow-graph.ts (plan 03): submitYesNoUploadAction, sendApprovalAction, receiveApprovalAction, assignUserAction, completeStepAction — all 'use server' actions taking { projectId, stepDefId, ... } and returning { ok, message? }.
From lib/workflow-graph.ts (plan 02): getStepByKey(graph, key) -> GraphStep (has id, kind, targetRole).
From lib/workflow.ts: GraphStep type, graphStepHref.
Next.js 16 (NOT training-data Next): consult node_modules/next/dist/docs before writing the route. Page params/searchParams are async — await them. Use the app-router server-component + client-component ('use client') split: the page.tsx is a server component that reads searchParams and the DB; the three kind components are 'use client' and call the server actions. Follow the existing pattern in app/(app)/checklists/[slug]/page.tsx for how a server page resolves projectId/step from searchParams and gates by role.
An existing users query is needed for the assignment picker — filter users by role = step.targetRole.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server route /workflow/step resolving a graph step and dispatching by kind</name>
  <read_first>
    - app/(app)/checklists/[slug]/page.tsx (the canonical pattern for a server page reading projectId+step from searchParams, awaiting them, and gating by role via verifySession/canRoleActOnStep)
    - node_modules/next/dist/docs (app-router page params/searchParams — confirm the async searchParams API in this Next version)
    - lib/workflow-graph.ts (getStepByKey)
  </read_first>
  <action>
    Create app/(app)/workflow/step/page.tsx as a server component. Read searchParams (await it per Next 16), extract projectId and step (the step KEY, e.g. 'test_yesno'). Resolve the step via getStepByKey('test', step) — for this phase the route serves the test graph; keep graph as a searchParam 'graph' defaulting to 'test' so it is not hardcoded to a single graph. verifySession and, if the step's role is not actionable by the viewer, render a short "not your step" message (mirror the checklists page gate). Based on step.kind, render the matching client component (YesNoUploadStep | ApprovalStep | AssignmentStep) passing projectId, the step id, and (for assignment) the targetRole. For kinds without a new renderer (checklist/readiness/ack/creation) render a short note pointing to the existing route (not in scope here). Keep it minimal.
  </action>
  <acceptance_criteria>
    - app/(app)/workflow/step/page.tsx exists, awaits searchParams, and calls getStepByKey.
    - It dispatches to the three kind components based on step.kind.
    - It gates with verifySession + canRoleActOnStep (grep confirms).
    - npx tsc --noEmit passes; npm run build (or next build) compiles the route without error.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && grep -c "getStepByKey\|verifySession\|searchParams" "app/(app)/workflow/step/page.tsx" && npx tsc --noEmit 2>&1 | grep -i "workflow/step" | head</automated>
  </verify>
  <done>The route resolves a graph step from searchParams, gates by role, and renders the correct kind component.</done>
</task>

<task type="auto">
  <name>Task 2: Three minimal kind renderers (client components) wired to server actions</name>
  <read_first>
    - actions/workflow-graph.ts (the action signatures + return shapes to call)
    - app/_components/readiness-form.tsx (an existing 'use client' form pattern: local state, disabled-until-valid submit, calling a server action — mirror its structure minimally)
    - lib/workflow.ts (GraphStep, WorkflowRole for the assignment picker)
  </read_first>
  <action>
    Create three 'use client' components under app/_components/workflow-kinds/:
    1. yes-no-upload-step.tsx (YesNoUploadStep): props { projectId, stepDefId }. Renders a yes/no toggle (two buttons or a radio) and an OPTIONAL file input (upload not required). On submit, read the file as a base64 data URL if provided (reuse the FileReader pattern from readiness-form.tsx) and call submitYesNoUploadAction({ projectId, stepDefId, answer, uploadData, uploadName }); show the returned message. Then a separate "Complete step" button calls completeStepAction({ projectId, stepDefId }).
    2. approval-step.tsx (ApprovalStep): props { projectId, stepDefId }. Renders a "Send for approval" button (calls sendApprovalAction) and a "Receive / approve" button (calls receiveApprovalAction), then a "Complete step" button (completeStepAction). Show returned messages including the two-party rejection message.
    3. assignment-step.tsx (AssignmentStep): props { projectId, stepDefId, targetRole, candidates: { id: string; name: string }[] }. Renders a select of candidates (users of targetRole, passed from the server page). On submit call assignUserAction({ projectId, stepDefId, assignedUserId }); then a "Complete step" button.
    Minimal styling only. Each component surfaces the server action's { ok, message } so a human/test can see gate outcomes. The AssignmentStep candidates list is resolved in the server page (task 1) by querying users where role = step.targetRole and passed as a prop (client components cannot query the DB).
  </action>
  <acceptance_criteria>
    - The three files exist under app/_components/workflow-kinds/ and each begins with 'use client'.
    - yes-no-upload-step.tsx calls submitYesNoUploadAction and completeStepAction; approval-step.tsx calls sendApprovalAction + receiveApprovalAction; assignment-step.tsx calls assignUserAction.
    - The yes/no step's file input is optional (submit works with no file).
    - npx tsc --noEmit passes; the /workflow/step route builds.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && for f in yes-no-upload-step approval-step assignment-step; do head -1 "app/_components/workflow-kinds/$f.tsx"; done && grep -rc "Action" app/_components/workflow-kinds/ && npx tsc --noEmit 2>&1 | grep -i "workflow-kinds" | head</automated>
  </verify>
  <done>Three minimal client renderers present the correct controls for each new kind and submit through the plan 03 server actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → server action | untrusted answer/file/assignee submitted here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Elevation of Privilege | client rendering a step the viewer may not act on | mitigate | The server page gates with verifySession + canRoleActOnStep before rendering any kind component; the actions re-check server-side (plan 03) so the UI gate is defense-in-depth, not the only gate |
| T-16-12 | Tampering | oversized/hostile upload via the optional file input | accept | Test-graph proof only under --skip-ui; upload size/type hardening deferred to the polished UI phases (18/19/21) |
</threat_model>

<verification>
- The /workflow/step route resolves a graph step and dispatches to the correct kind renderer under role gating.
- Each of the three renderers presents the correct controls and submits through the server actions.
- Typecheck + build pass.
</verification>

<success_criteria>
Each new fulfillment kind renders its correct interface (yes/no + optional upload; approval send/receive; assignment user picker) and drives the engine through the server actions — satisfying the "renders its correct UI" half of WF-03 at test-graph fidelity.
</success_criteria>

<output>
Create `.planning/phases/16-workflow-engine-core/16-05-SUMMARY.md` when done.
</output>
