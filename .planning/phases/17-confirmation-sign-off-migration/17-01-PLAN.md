---
phase: 17-confirmation-sign-off-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/workflow.ts
  - lib/workflow-graph.ts
  - db/seed-workflow-graph.ts
  - scripts/verify-live-workflow.ts
  - package.json
autonomous: true
requirements: [WF-06]

must_haves:
  truths:
    - "getLiveWorkflowSteps() returns the exact same 11 steps (key/label/role/kind/slug/order) that the legacy WORKFLOW_STEPS array defines"
    - "The live graph natively models Delivery Readiness (4) + Delivery Project Checklist (5) both feeding Project Check Report (6) as edges, not sequential numbering"
    - "project_check_report is actionable only once BOTH branch steps are complete, in either completion order"
  artifacts:
    - path: "lib/workflow-graph.ts"
      provides: "getLiveWorkflowSteps() adapter mapping GraphStep(orderIndex) -> WorkflowStep(n) + stepDefId"
      contains: "getLiveWorkflowSteps"
    - path: "lib/workflow.ts"
      provides: "pure additive helpers findStep/lastStepN/projectComplete (legacy array exports still present)"
      contains: "export function lastStepN"
    - path: "db/seed-workflow-graph.ts"
      provides: "corrected fan-out/join edges for the delivery cluster"
      contains: "project_check_report"
    - path: "scripts/verify-live-workflow.ts"
      provides: "parity assertion (adapter == legacy array) + live-graph join-resolution assertion"
      contains: "getLiveWorkflowSteps"
  key_links:
    - from: "lib/workflow-graph.ts:getLiveWorkflowSteps"
      to: "workflow_step_definitions graph='live'"
      via: "getGraphSteps('live')"
      pattern: "getGraphSteps\\('live'\\)"
    - from: "scripts/verify-live-workflow.ts"
      to: "lib/workflow-graph.ts:getActionableSteps"
      via: "live-graph join verification"
      pattern: "getActionableSteps"
---

<objective>
Lay the migration foundation WITHOUT touching any live caller yet: add a server-only adapter that projects the DB `graph='live'` step definitions into the legacy `WorkflowStep` shape, add pure (array-argument) helper variants alongside the existing array-based ones, correct the live graph's edges to natively model the Delivery Readiness + Delivery Project Checklist → Project Check Report parallel/join, and add a verification script that proves (a) the adapter is byte-identical to the legacy `WORKFLOW_STEPS` array and (b) the join resolves through `getActionableSteps` in both completion orders.

This plan is purely additive — the legacy `WORKFLOW_STEPS` literal and every existing caller are left untouched, so the repo compiles and behaves identically after it.

Purpose: De-risk the highest-risk cutover in the milestone by proving the DB is a faithful copy of the hardcoded array and the join works before anything depends on it.
Output: `getLiveWorkflowSteps()`, pure helpers, corrected live edges, and a repeatable `npm run verify:live-workflow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md

# Current live workflow API + engine (source of truth to preserve)
@lib/workflow.ts
@lib/workflow-graph.ts
@db/seed-workflow-graph.ts
@scripts/verify-workflow-engine.ts

<interfaces>
<!-- Contracts the executor must preserve exactly. Extracted from the codebase. -->

lib/workflow.ts (client-safe — MUST NOT import db or 'server-only'):
  type WorkflowRole = 'operations' | 'site_pm' | 'factory_pm' | 'super_admin'
  type WorkflowStep = { n: number; key: string; label: string; role: WorkflowRole; kind: StepKind; slug?: string }
  type GraphStep = { id, graph, key, label, role, kind, slug?, targetRole?, isOptional, orderIndex }
  const WORKFLOW_STEPS: WorkflowStep[]   // 11 steps n=1..11 (LEGACY LITERAL — leave in place this plan)
  const FIRST_ACTION_STEP = 2            // KEEP as constant (position invariant, not step data)
  const LAST_STEP = 11                   // legacy — leave in place this plan
  function stepByN(n): WorkflowStep|undefined            // legacy array-based — leave in place
  function isProjectComplete(currentStep): boolean       // legacy — leave in place
  function canRoleActOnStep(stepRole, userRole): boolean // pure — reused unchanged
  function stepHref(step, projectId): string|null        // pure — reused unchanged

lib/workflow-graph.ts (server-only — `import 'server-only'` first line):
  async getGraphSteps(graph='live'): Promise<GraphStep[]>   // ordered by orderIndex
  async getActionableSteps(projectId, graph='live'): Promise<GraphStep[]>
  async getStepByKey(graph, key), getStepById(id), completeGraphStep({projectId, stepDefId, actorId, skip?})

scripts/verify-workflow-engine.ts: reuse its `server-only` Module._load shim so a tsx CLI can import
lib/workflow-graph.ts (server-only throws otherwise — see 16-04 SUMMARY). Use require(), not static import.

Verified DB state (graph='live'): 11 definitions orderIndex 1..11 matching WORKFLOW_STEPS exactly;
edges are currently a LINEAR chain 1->2->...->11 (parallel/join MISSING — this plan adds it).
Real projects exist at currentStep 3 and 5 (in-flight) and 12 (delivered).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getLiveWorkflowSteps() adapter + pure helper variants (additive only)</name>
  <read_first>
    - lib/workflow.ts (full — every legacy export MUST remain untouched)
    - lib/workflow-graph.ts (getGraphSteps, toGraphStep mapping, GraphStep import from '@/lib/workflow')
  </read_first>
  <action>
    In lib/workflow-graph.ts add `export type LiveWorkflowStep = WorkflowStep & { stepDefId: string }` (import the WorkflowStep type from '@/lib/workflow') and `export async function getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>` that calls getGraphSteps('live') and maps each GraphStep g to `{ n: g.orderIndex, key: g.key, label: g.label, role: g.role, kind: g.kind, slug: g.slug ?? undefined, stepDefId: g.id }`, preserving orderIndex order. Do NOT change any existing export.

    In lib/workflow.ts add THREE pure, array-argument helpers alongside (not replacing) the legacy ones: `export function findStep(steps: WorkflowStep[], n: number): WorkflowStep | undefined` returning steps.find(s => s.n === n); `export function lastStepN(steps: WorkflowStep[]): number` returning the max s.n; `export function projectComplete(currentStep: number, lastN: number): boolean` returning currentStep > lastN. Keep WORKFLOW_STEPS, stepByN, LAST_STEP, isProjectComplete, FIRST_ACTION_STEP, canRoleActOnStep, stepHref exactly as they are. lib/workflow.ts MUST NOT gain any db or 'server-only' import.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && grep -q "getLiveWorkflowSteps" lib/workflow-graph.ts && grep -q "export function lastStepN" lib/workflow.ts && grep -q "export const WORKFLOW_STEPS" lib/workflow.ts && ! grep -q "server-only" lib/workflow.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - getLiveWorkflowSteps exists and returns LiveWorkflowStep[] mapping orderIndex->n and id->stepDefId
    - findStep, lastStepN, projectComplete exist in lib/workflow.ts as pure functions taking a steps array
    - All legacy exports still present and unchanged; lib/workflow.ts has no 'server-only'/db import
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Adapter + pure helpers exist additively; nothing legacy removed; project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the live graph edges to model the parallel/join (D-03)</name>
  <read_first>
    - db/seed-workflow-graph.ts (current linear edge loop n->n+1)
    - db/seed-workflow-test-graph.ts (fan-out/join edge pattern to mirror)
  </read_first>
  <action>
    Replace db/seed-workflow-graph.ts's linear edge generation with an explicit edge list (by step key) so the live graph encodes the parallel/join around the delivery cluster while every other step stays linear. Emit exactly these 11 edges: new_project->confirmation, confirmation->materials_readiness, materials_readiness->delivery_readiness, materials_readiness->delivery_project, delivery_readiness->project_check_report, delivery_project->project_check_report, project_check_report->approval_installation, approval_installation->installation_readiness, installation_readiness->sorting, sorting->close_out, close_out->sign_off. The old delivery_readiness->delivery_project edge MUST be gone. Keep every step definition and its orderIndex (1..11) EXACTLY as-is — only edges change. Keep the seed idempotent (delete-then-insert scoped to graph='live', edges before definitions). Do NOT touch graph='test'. Re-run `npm run db:seed-workflow-graph`.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npm run db:seed-workflow-graph >/dev/null 2>&1 && grep -q "materials_readiness" db/seed-workflow-graph.ts && grep -q "project_check_report" db/seed-workflow-graph.ts && echo SEEDED_OK</automated>
  </verify>
  <acceptance_criteria>
    - db/seed-workflow-graph.ts emits the 11-edge fan-out/join set above; no delivery_readiness->delivery_project edge remains
    - Step definitions/orderIndex unchanged (still 11 defs, 1..11); graph='test' untouched
    - Seed re-runs cleanly and idempotently (`npm run db:seed-workflow-graph` exits 0)
    - Actual DB edge correctness is asserted by Task 3's script
  </acceptance_criteria>
  <done>Live graph edges natively encode the delivery parallel/join; seed idempotent; orderIndex preserved.</done>
</task>

<task type="auto">
  <name>Task 3: verify-live-workflow.ts — parity + live-graph join assertions</name>
  <read_first>
    - scripts/verify-workflow-engine.ts (server-only Module._load shim via require(); throwaway-project + cleanup pattern)
    - lib/workflow-graph.ts (getLiveWorkflowSteps, getActionableSteps, getStepByKey, completeGraphStep)
    - lib/workflow.ts (WORKFLOW_STEPS — the parity reference this plan compares against)
  </read_first>
  <action>
    Create scripts/verify-live-workflow.ts (tsx CLI) reusing verify-workflow-engine.ts's server-only shim. It asserts, exiting nonzero on any failure: (1) PARITY — getLiveWorkflowSteps() deep-equals WORKFLOW_STEPS on n/key/label/role/kind/slug for all 11 steps in order (import WORKFLOW_STEPS from '@/lib/workflow' as the reference); (2) JOIN order A — create a throwaway project, mark delivery_readiness complete then assert project_check_report NOT yet in getActionableSteps, then mark delivery_project complete and assert project_check_report IS actionable; (3) JOIN order B — a second throwaway project completing delivery_project first then delivery_readiness, asserting the same join outcome. Complete predecessor steps via completeGraphStep using stepDefIds resolved by getStepByKey('live', key). Clean up all throwaway rows in a finally block (cascade delete). Add an `verify:live-workflow` script to package.json running `tsx scripts/verify-live-workflow.ts`. Do NOT mutate any real project or graph='test'.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npm run verify:live-workflow 2>&1 | tail -5 && npm run verify:live-workflow >/dev/null 2>&1 && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:live-workflow` exits 0 and prints a PARITY pass and both JOIN-order passes
    - Deliberately breaking parity (e.g. temporarily editing an in-memory expected label) makes it exit nonzero (rubber-stamp guard — executor confirms once, then reverts)
    - No real project row (currentStep 3/5/12) is modified; throwaway rows cleaned up
    - graph='test' untouched
  </acceptance_criteria>
  <done>A repeatable script proves the adapter equals the legacy array and the live join resolves in both orders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB graph='live' → app step resolution | The adapter becomes the app's future source of truth; a wrong mapping silently changes gating for every project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | getLiveWorkflowSteps mapping (orderIndex->n, role/kind/slug) | mitigate | Task 3 PARITY assertion deep-equals adapter output against the legacy WORKFLOW_STEPS before any caller depends on it |
| T-17-02 | Tampering | live edge re-seed (delivery cluster) | mitigate | Task 3 JOIN assertions prove both branch orders through getActionableSteps; seed scoped to graph='live', graph='test' untouched |
| T-17-03 | Denial of service | verify script writing throwaway rows | accept | Uniquely-named throwaway projects, cascade-deleted in finally; no real project touched (mirrors 16-04 harness) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run db:seed-workflow-graph` idempotent; `npm run verify:live-workflow` exits 0.
- No legacy export removed; lib/workflow.ts still client-safe (no server-only/db import).
- Real projects at currentStep 3/5/12 unchanged.
</verification>

<success_criteria>
- getLiveWorkflowSteps() is byte-identical to WORKFLOW_STEPS (proven), the live graph encodes the delivery parallel/join (proven both orders), and pure helpers exist additively — all callers still on the legacy array, repo compiles and behaves identically.
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-01-SUMMARY.md` when done.
</output>
