---
phase: 17-confirmation-sign-off-migration
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - lib/my-work.ts
  - actions/workflow.ts
  - actions/bypass.ts
  - actions/projects.ts
  - app/(app)/admin/analytics/page.tsx
autonomous: true
requirements: [WF-06]

must_haves:
  truths:
    - "getMyWork resolves pending work from the DB-sourced steps, not the WORKFLOW_STEPS literal"
    - "advanceProjectStep advances currentStep by exactly +1 and marks done at the same boundary as before (nextStep > lastStepN), for the same roles"
    - "Step completions written by advanceProjectStep and the bypass-approval path also record stepDefId/graph resolved from the live graph (additive, behavior-neutral)"
  artifacts:
    - path: "lib/my-work.ts"
      provides: "getMyWork reading getLiveWorkflowSteps + findStep/projectComplete"
      contains: "getLiveWorkflowSteps"
    - path: "actions/workflow.ts"
      provides: "advanceProjectStep on DB-sourced steps + stepDefId dual-write"
      contains: "getLiveWorkflowSteps"
  key_links:
    - from: "actions/workflow.ts:advanceProjectStep"
      to: "projects.currentStep"
      via: "nextStep = currentStep + 1"
      pattern: "currentStep \\+ 1"
    - from: "lib/my-work.ts:getMyWork"
      to: "lib/workflow-graph.ts:getLiveWorkflowSteps"
      via: "server fetch of live steps"
      pattern: "getLiveWorkflowSteps"
---

<objective>
Cut over the server-side ACTORS and LIBS that read step data — the ones that can call the async adapter directly (they are already `'use server'` or `server-only`) — from the legacy `WORKFLOW_STEPS`/`stepByN`/`LAST_STEP`/`isProjectComplete` to `getLiveWorkflowSteps()` + the pure helpers from plan 01. Behavior must stay byte-identical: same `currentStep + 1` advancement, same `nextStep > lastStepN` completion boundary (11), same role gates. Additionally record `stepDefId`/`graph` on the two completion-writing paths (advanceProjectStep, bypass approval) so live completions link to the live graph — additive and behavior-neutral (the integer `currentStep` remains the sole behavioral source of truth).

Purpose: Move the write/advancement core and the my-work computation onto the DB engine without any observable change, before the client and page layers follow.
Output: my-work, advancement, bypass, project creation, and analytics all reading live steps from the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md
@lib/my-work.ts
@actions/workflow.ts
@actions/bypass.ts
@actions/projects.ts

<interfaces>
From lib/workflow-graph.ts (plan 01): async getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>
  where LiveWorkflowStep = WorkflowStep & { stepDefId: string }; getStepByKey('live', key): Promise<GraphStep|undefined>
From lib/workflow.ts (plan 01, pure): findStep(steps, n), lastStepN(steps), projectComplete(currentStep, lastN),
  canRoleActOnStep(stepRole, userRole) [unchanged], FIRST_ACTION_STEP=2 [unchanged constant]

Legacy → new mapping this plan applies:
  stepByN(n)                     -> findStep(steps, n)
  LAST_STEP                      -> lastStepN(steps)   // === 11
  isProjectComplete(currentStep) -> projectComplete(currentStep, lastStepN(steps))
  WORKFLOW_STEPS (iteration)     -> const steps = await getLiveWorkflowSteps()

db/schema.ts projectStepCompletions columns: stepKey, stepN (legacy, keep), stepDefId (uuid nullable),
  graph (text default 'live'), skipped (bool). advanceProjectStep currently writes only stepKey/stepN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut over advanceProjectStep + bypass approval (with stepDefId dual-write)</name>
  <read_first>
    - actions/workflow.ts (advanceProjectStep, completeAckStepAction)
    - actions/bypass.ts (decideStepBypassAction completion path, requestStepBypassAction)
    - lib/workflow-graph.ts (getLiveWorkflowSteps, getStepByKey)
  </read_first>
  <action>
    In actions/workflow.ts advanceProjectStep: replace `stepByN(expectedStepN)` with `const steps = await getLiveWorkflowSteps(); const step = findStep(steps, expectedStepN)`; replace `LAST_STEP` with `lastStepN(steps)`. Keep the role gate `canRoleActOnStep(step.role, role)` and `nextStep = proj.currentStep + 1` and `done = nextStep > lastStepN(steps)` semantics identical. When inserting the projectStepCompletions row, also set `stepDefId: step.stepDefId` and `graph: 'live'` alongside the existing stepKey/stepN/completedBy/notes. In completeAckStepAction, replace `stepByN(input.expectedStepN)` with `findStep(await getLiveWorkflowSteps(), input.expectedStepN)` (keep the `step.kind !== 'ack'` guard).

    In actions/bypass.ts: in requestStepBypassAction replace `stepByN(stepN)` with `findStep(await getLiveWorkflowSteps(), stepN)` (keep canRoleActOnStep gate + workflowRoleLabel usage). In decideStepBypassAction replace `stepByN(req.stepN)` with a live-steps lookup, replace `LAST_STEP` with `lastStepN(steps)`, replace `isProjectComplete(proj.currentStep)` with `projectComplete(proj.currentStep, lastStepN(steps))`; on the approval completion insert also set `stepDefId` (from the resolved live step) and `graph: 'live'`. Preserve every guard (paused, currentStep===stepN, not complete) and the `nextStep = currentStep + 1` advancement exactly.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "stepByN\|LAST_STEP\|isProjectComplete\b" actions/workflow.ts && ! grep -q "stepByN\|LAST_STEP" actions/bypass.ts && grep -q "getLiveWorkflowSteps" actions/workflow.ts && grep -q "stepDefId" actions/workflow.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - actions/workflow.ts and actions/bypass.ts no longer import stepByN/LAST_STEP/isProjectComplete; use getLiveWorkflowSteps + findStep/lastStepN/projectComplete
    - advanceProjectStep still advances currentStep by +1 and marks done exactly when nextStep > 11, gated by canRoleActOnStep unchanged
    - Both completion inserts additionally write stepDefId + graph='live'; stepKey/stepN still written
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Advancement + bypass read live steps and link completions to the graph, with identical gating/boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Cut over getMyWork, createProjectAction, and analytics page</name>
  <read_first>
    - lib/my-work.ts (getMyWork: stepByN + canRoleActOnStep + isProjectComplete)
    - actions/projects.ts (createProjectAction WORKFLOW_STEPS deadline loop; FIRST_ACTION_STEP; isProjectComplete in pauseProjectAction)
    - app/(app)/admin/analytics/page.tsx (isProjectComplete usage)
  </read_first>
  <action>
    In lib/my-work.ts getMyWork: add `const steps = await getLiveWorkflowSteps()`; replace `isProjectComplete(p.currentStep)` with `projectComplete(p.currentStep, lastStepN(steps))` and `stepByN(p.currentStep)` with `findStep(steps, p.currentStep)`. Keep the paused filter, per-step deadline logic, and pending sort exactly.

    In actions/projects.ts createProjectAction: replace the `for (const s of WORKFLOW_STEPS)` deadline-parsing loop with iteration over `await getLiveWorkflowSteps()`, keeping the `if (s.n < FIRST_ACTION_STEP) continue` skip and all ordering validation identical. Keep FIRST_ACTION_STEP as the imported constant. Keep `currentStep: FIRST_ACTION_STEP` and the stepKey:'new_project'/stepN:1 completion insert unchanged (creation step). In pauseProjectAction replace `isProjectComplete(proj.currentStep)` with `projectComplete(proj.currentStep, lastStepN(await getLiveWorkflowSteps()))`.

    In app/(app)/admin/analytics/page.tsx: replace `isProjectComplete(p.currentStep)` with `projectComplete(p.currentStep, lastStepN(steps))` where `steps = await getLiveWorkflowSteps()` (page is a server component). Keep the `|| p.status === 'delivered'` clause.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "stepByN\|isProjectComplete\b" lib/my-work.ts && ! grep -q "WORKFLOW_STEPS" actions/projects.ts && grep -q "getLiveWorkflowSteps" lib/my-work.ts && grep -q "getLiveWorkflowSteps" actions/projects.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lib/my-work.ts, actions/projects.ts, admin/analytics/page.tsx read step data via getLiveWorkflowSteps + pure helpers; no WORKFLOW_STEPS/stepByN/isProjectComplete references remain in them
    - createProjectAction deadline ordering + FIRST_ACTION_STEP start + creation-step completion behavior unchanged
    - getMyWork paused/deadline/pending-sort behavior unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>My-work, project creation, pause, and analytics resolve steps from the DB with identical behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor → workflow write (advance/bypass) | Role-gated mutation of project state; a lost gate = privilege escalation across roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of privilege | advanceProjectStep / bypass role gate during refactor | mitigate | canRoleActOnStep(step.role, role) is preserved verbatim; step.role now sourced from DB adapter proven byte-identical in plan 01 |
| T-17-05 | Tampering | completion boundary drift (done = nextStep > lastStepN) | mitigate | lastStepN(steps) === 11 asserted by plan 01 parity; +1 advancement unchanged |
| T-17-06 | Repudiation | stepDefId dual-write | accept | Additive columns only; stepKey/stepN audit trail preserved; integer currentStep remains behavioral source of truth |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Grep confirms no legacy step-data reads remain in the five files.
- `npm test` (workflow pure-helper tests) still passes (helpers unchanged in this plan).
</verification>

<success_criteria>
- The advancement core, bypass approval, my-work computation, project creation, and analytics all resolve workflow steps from the DB graph with byte-identical behavior; completions now additionally link to the live graph via stepDefId.
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-02-SUMMARY.md` when done.
</output>
