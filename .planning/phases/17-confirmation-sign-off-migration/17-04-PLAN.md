---
phase: 17-confirmation-sign-off-migration
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/_components/workflow-steps-provider.tsx
  - app/(app)/layout.tsx
  - app/_components/trt-flow-diagram.tsx
  - app/(app)/about/page.tsx
autonomous: true
requirements: [WF-06]

must_haves:
  truths:
    - "A client WorkflowStepsProvider, seeded once server-side from getLiveWorkflowSteps(), exposes the live steps to any client component under the (app) layout via useWorkflowSteps()"
    - "The end-to-end flow diagram renders the same 11 steps in the same order from the DB, not from the WORKFLOW_STEPS literal"
  artifacts:
    - path: "app/_components/workflow-steps-provider.tsx"
      provides: "WorkflowStepsProvider + useWorkflowSteps() client context"
      contains: "useWorkflowSteps"
    - path: "app/(app)/layout.tsx"
      provides: "server-seeded provider wrapping the app shell"
      contains: "WorkflowStepsProvider"
    - path: "app/_components/trt-flow-diagram.tsx"
      provides: "flow diagram sourced from getLiveWorkflowSteps()"
      contains: "getLiveWorkflowSteps"
  key_links:
    - from: "app/(app)/layout.tsx"
      to: "app/_components/workflow-steps-provider.tsx"
      via: "initial={await getLiveWorkflowSteps()}"
      pattern: "WorkflowStepsProvider"
---

<objective>
Create the delivery vehicle that lets the remaining CLIENT components read live workflow steps without importing the `WORKFLOW_STEPS` literal: a `WorkflowStepsProvider` client context, seeded ONCE in the `(app)` layout from a server-side `getLiveWorkflowSteps()` fetch, exposing `useWorkflowSteps()`. Also cut over the one step-reading SERVER component that renders the end-to-end flow — `TrtFlowDiagram` — to `await getLiveWorkflowSteps()` directly.

This plan does not yet change the client consumers (board, header switcher, pending gate, new-project form) — that is plan 05, which depends on this provider existing. This plan only adds the provider, wires it, and migrates the flow diagram.

Purpose: Give client components a serializable, request-fresh source of live steps so they can drop the literal in plan 05.
Output: WorkflowStepsProvider wired in the layout; flow diagram on the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md
@app/(app)/layout.tsx
@app/_components/my-work-provider.tsx
@app/_components/trt-flow-diagram.tsx

<interfaces>
From lib/workflow-graph.ts (plan 01): async getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>
  LiveWorkflowStep = { n, key, label, role, kind, slug?, stepDefId } — fully serializable (safe as a prop).
From lib/workflow.ts: type WorkflowStep, workflowRoleLabel [unchanged].

Existing pattern to mirror: app/_components/my-work-provider.tsx — a 'use client' context seeded with an
`initial` prop from the server layout, exposing a hook. WorkflowStepsProvider is simpler: static data seeded
once, no polling.

Layout (app/(app)/layout.tsx) is an async server component already awaiting auth + getMyWork and wrapping
children in <MyWorkProvider>. Add getLiveWorkflowSteps() and nest <WorkflowStepsProvider initial={liveSteps}>.

TrtFlowDiagram (app/_components/trt-flow-diagram.tsx) is a pure SERVER component; make it async and
`await getLiveWorkflowSteps()`, iterating the returned steps exactly as it iterates WORKFLOW_STEPS today
(step.n badge, step.key -> DETAIL blurb, step.role -> ROLE_COLOR/workflowRoleLabel). It is rendered by
app/(app)/about/page.tsx — that render site must await/handle the now-async component.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkflowStepsProvider + useWorkflowSteps, wired in the (app) layout</name>
  <read_first>
    - app/_components/my-work-provider.tsx (context + initial-prop pattern to mirror)
    - app/(app)/layout.tsx (async server layout; where MyWorkProvider wraps children)
    - lib/workflow-graph.ts (getLiveWorkflowSteps)
  </read_first>
  <action>
    Create app/_components/workflow-steps-provider.tsx as a 'use client' module: a React context holding `WorkflowStep[]` (import the WorkflowStep type from '@/lib/workflow'; the serialized LiveWorkflowStep superset is assignable to it), a `WorkflowStepsProvider({ initial, children })` storing `initial` in state (no polling — static per request), and a `useWorkflowSteps(): WorkflowStep[]` hook reading the context (default []). Keep it minimal, mirroring MyWorkProvider's shape.

    In app/(app)/layout.tsx: import getLiveWorkflowSteps + WorkflowStepsProvider, add `const liveSteps = await getLiveWorkflowSteps()` next to the existing getMyWork call, and nest `<WorkflowStepsProvider initial={liveSteps}>` around the existing children (wrapping or inside MyWorkProvider — either nesting works; keep MyWorkProvider intact). Do not change any other layout behavior.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && grep -q "useWorkflowSteps" app/_components/workflow-steps-provider.tsx && grep -q "WorkflowStepsProvider" "app/(app)/layout.tsx" && grep -q "getLiveWorkflowSteps" "app/(app)/layout.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - workflow-steps-provider.tsx exports WorkflowStepsProvider + useWorkflowSteps and is a 'use client' module
    - layout.tsx fetches getLiveWorkflowSteps() server-side and wraps children in WorkflowStepsProvider with initial={liveSteps}
    - MyWorkProvider and all other layout behavior unchanged; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Any client component under the (app) shell can read the live steps via useWorkflowSteps().</done>
</task>

<task type="auto">
  <name>Task 2: Cut over TrtFlowDiagram to getLiveWorkflowSteps()</name>
  <read_first>
    - app/_components/trt-flow-diagram.tsx (iterates WORKFLOW_STEPS: step.n badge, step.key -> DETAIL, step.role color)
    - app/(app)/about/page.tsx (render site for TrtFlowDiagram)
  </read_first>
  <action>
    Make TrtFlowDiagram an async server component: replace `import { WORKFLOW_STEPS }` with getLiveWorkflowSteps from '@/lib/workflow-graph' (keep workflowRoleLabel + WorkflowRole from '@/lib/workflow'), add `const steps = await getLiveWorkflowSteps()`, and iterate `steps` everywhere the component currently iterates WORKFLOW_STEPS (map for the timeline, `steps.length - 1` for the last-item rail). Keep ROLE_COLOR, DETAIL (keyed by step.key), ROLES legend, and all markup identical. In app/(app)/about/page.tsx, ensure the now-async TrtFlowDiagram is rendered correctly (await it if invoked as a function, or render `<TrtFlowDiagram />` as a server child — whichever the file's pattern requires to compile).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "WORKFLOW_STEPS" app/_components/trt-flow-diagram.tsx && grep -q "getLiveWorkflowSteps" app/_components/trt-flow-diagram.tsx && npx next build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - TrtFlowDiagram iterates getLiveWorkflowSteps() output; no WORKFLOW_STEPS reference remains in it
    - The 11 steps render in the same order with the same badges/labels/blurbs/colors
    - about page compiles with the async component; `npx next build` succeeds
  </acceptance_criteria>
  <done>The end-to-end flow diagram is DB-sourced with identical output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server layout → client step context | Serialized step data crosses into the client bundle; it carries no secrets (labels/roles/keys only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Information disclosure | WorkflowStepsProvider serialized props | accept | Only non-sensitive step metadata (n/key/label/role/kind/slug) is sent to the client — identical to what the WORKFLOW_STEPS literal already shipped in client bundles |
| T-17-10 | Tampering | flow-diagram step order/labels | mitigate | Steps sourced from the adapter proven byte-identical in plan 01; ordering preserved by orderIndex |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx next build` compiles the shell + about route.
- No WORKFLOW_STEPS reference remains in trt-flow-diagram.tsx.
- Provider is seeded server-side; useWorkflowSteps available to descendants.
</verification>

<success_criteria>
- A server-seeded WorkflowStepsProvider exposes the live steps to client components, and the flow diagram renders the DB steps identically — with the client consumers ready to migrate in plan 05.
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-04-SUMMARY.md` when done.
</output>
