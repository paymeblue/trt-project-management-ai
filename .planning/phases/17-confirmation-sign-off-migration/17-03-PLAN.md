---
phase: 17-confirmation-sign-off-migration
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/(app)/checklists/[slug]/page.tsx
  - app/(app)/factory-pm/readiness/page.tsx
  - app/(app)/admin/approvals/page.tsx
  - app/(app)/admin/timeline/page.tsx
autonomous: true
requirements: [WF-06]

must_haves:
  truths:
    - "The checklist and readiness workflow-context gates resolve the target step from the DB-sourced steps, preserving the exact slug-match, currentStep-match, and role-turn checks"
    - "The admin timeline renders each project's current step, X/lastStepN, completion state, and act-href identically to before"
    - "The approvals page renders each bypass request's Step N/lastStepN + label identically"
  artifacts:
    - path: "app/(app)/checklists/[slug]/page.tsx"
      provides: "workflow-context gate on DB-sourced steps"
      contains: "getLiveWorkflowSteps"
    - path: "app/(app)/admin/timeline/page.tsx"
      provides: "timeline board on DB-sourced steps"
      contains: "getLiveWorkflowSteps"
  key_links:
    - from: "app/(app)/checklists/[slug]/page.tsx"
      to: "lib/workflow-graph.ts:getLiveWorkflowSteps"
      via: "server fetch + findStep(steps, stepN)"
      pattern: "getLiveWorkflowSteps"
---

<objective>
Cut over the four SERVER-RENDERED pages that read step data to gate or display workflow steps — the checklist launcher, the factory readiness launcher, the admin approvals queue, and the admin timeline — from the legacy `stepByN`/`LAST_STEP`/`isProjectComplete` to `getLiveWorkflowSteps()` + pure helpers. These are async server components, so they call the adapter directly. Every gate check (slug match, `proj.currentStep === stepN`, `canRoleActOnStep`) and every display (`Step N/LAST_STEP`, completion badge, act-href) must render identically.

Purpose: Bring the step-gating server pages onto the DB engine without changing what a user sees or is allowed to do.
Output: checklist, readiness, approvals, and timeline pages resolving steps from the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md
@app/(app)/checklists/[slug]/page.tsx
@actions/workflow.ts

<interfaces>
From lib/workflow-graph.ts (plan 01): async getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>
From lib/workflow.ts (plan 01, pure): findStep(steps, n), lastStepN(steps), projectComplete(currentStep, lastN),
  canRoleActOnStep [unchanged], stepHref(step, projectId) [unchanged], workflowRoleLabel [unchanged]

Legacy → new mapping: stepByN(n) -> findStep(steps, n); LAST_STEP -> lastStepN(steps);
  isProjectComplete(cs) -> projectComplete(cs, lastStepN(steps)). All four files are async server components.
Next 16: these pages already `await params`/`await searchParams` — keep that; add `await getLiveWorkflowSteps()`.

Note: stepHref takes a WorkflowStep; findStep returns WorkflowStep (LiveWorkflowStep is a superset), so
stepHref(step, projectId) works unchanged on the adapter's steps.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut over the checklist + factory readiness launchers</name>
  <read_first>
    - app/(app)/checklists/[slug]/page.tsx (workflow-context gate: stepByN + slug match + currentStep match + canRoleActOnStep)
    - app/(app)/factory-pm/readiness/page.tsx (same gate shape, lines around stepByN/canRoleActOnStep)
  </read_first>
  <action>
    In app/(app)/checklists/[slug]/page.tsx: after resolving projectId/stepN, add `const steps = await getLiveWorkflowSteps()` and replace `stepByN(stepN)` with `findStep(steps, stepN)`. Keep every branch identical: the `!proj || !step` notice, `step.slug !== slug` notice, `proj.currentStep !== stepN` notice (with the > vs < wording), `!canRoleActOnStep(step.role, role)` notice, and the success assignment of workflowProjectId/workflowStepN. Remove the stepByN import; import findStep + getLiveWorkflowSteps.

    In app/(app)/factory-pm/readiness/page.tsx: same treatment — `const steps = await getLiveWorkflowSteps()`, replace `stepByN(stepN)` with `findStep(steps, stepN)`, keep the notice branches and the canRoleActOnStep turn check verbatim.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "stepByN" "app/(app)/checklists/[slug]/page.tsx" && ! grep -q "stepByN" "app/(app)/factory-pm/readiness/page.tsx" && grep -q "getLiveWorkflowSteps" "app/(app)/checklists/[slug]/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both launchers resolve the workflow step via findStep(await getLiveWorkflowSteps(), stepN); no stepByN import remains
    - All workflow-notice branches and the canRoleActOnStep turn check are byte-identical in logic
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Checklist + readiness step gates run on DB-sourced steps with identical notices and gating.</done>
</task>

<task type="auto">
  <name>Task 2: Cut over the admin approvals + timeline pages</name>
  <read_first>
    - app/(app)/admin/approvals/page.tsx (stepByN + LAST_STEP + workflowRoleLabel display)
    - app/(app)/admin/timeline/page.tsx (stepByN, LAST_STEP, isProjectComplete, canRoleActOnStep, stepHref, history stepByN)
  </read_first>
  <action>
    In app/(app)/admin/approvals/page.tsx: add `const steps = await getLiveWorkflowSteps()`; replace `stepByN(r.stepN)` with `findStep(steps, r.stepN)` and `LAST_STEP` with `lastStepN(steps)` in the `Step {r.stepN}/{LAST_STEP}: {step?.label}` display. Keep everything else.

    In app/(app)/admin/timeline/page.tsx: add `const steps = await getLiveWorkflowSteps()`; replace `isProjectComplete(p.currentStep)` with `projectComplete(p.currentStep, lastStepN(steps))`, `stepByN(p.currentStep)` with `findStep(steps, p.currentStep)`, `LAST_STEP` with `lastStepN(steps)`, the history `stepByN(h.stepN)` with `findStep(steps, h.stepN)`, and keep `canRoleActOnStep(step.role, role)` and `stepHref(step, p.id)` exactly. Update imports accordingly.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "stepByN\|LAST_STEP\|isProjectComplete\b" "app/(app)/admin/timeline/page.tsx" && ! grep -q "stepByN\|LAST_STEP" "app/(app)/admin/approvals/page.tsx" && grep -q "getLiveWorkflowSteps" "app/(app)/admin/timeline/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - approvals + timeline resolve steps via getLiveWorkflowSteps + findStep/lastStepN/projectComplete; no legacy step-data imports remain
    - Timeline still shows current-step label, X/lastStepN, completion badge, act-href, and per-project history identically
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Approvals + timeline render from DB-sourced steps with identical output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-step launcher → checklist/readiness submission | The turn/slug/currentStep gate decides whether a submission advances the workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Elevation of privilege | checklist/readiness workflow-context gate | mitigate | slug-match + currentStep-match + canRoleActOnStep branches preserved verbatim; step now from adapter proven identical (plan 01) |
| T-17-08 | Information disclosure | timeline/approvals step display | accept | Read-only rendering; no data-scope change; step labels/roles identical to legacy array |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; no stepByN/LAST_STEP/isProjectComplete references remain in the four pages.
- `npx next build` compiles all four routes.
</verification>

<success_criteria>
- The four step-gating server pages resolve workflow steps from the DB graph, preserving every gate branch and every display string.
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-03-SUMMARY.md` when done.
</output>
