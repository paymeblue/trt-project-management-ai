---
phase: 19-new-roles-assignment
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - lib/workflow.ts
  - app/_components/sidebar-nav.tsx
  - app/(app)/factory-operations/dashboard/page.tsx
  - app/(app)/factory-manager/dashboard/page.tsx
  - app/(app)/about/page.tsx
  - app/_components/trt-flow-diagram.tsx
autonomous: true
requirements: [ROLE-01]
must_haves:
  truths:
    - "A user with role factory_operations lands on /factory-operations/dashboard with its own nav and tiles"
    - "A user with role factory_manager lands on /factory-manager/dashboard with its own nav and tiles"
    - "Neither new role's dashboard falls back to /production/dashboard any longer"
    - "The About page organogram and the flow diagram both name Factory Operations and Factory Manager"
  artifacts:
    - path: "app/(app)/factory-operations/dashboard/page.tsx"
      provides: "Factory Operations dashboard shell (Phase 15 pattern)"
      contains: "DashboardShell"
    - path: "app/(app)/factory-manager/dashboard/page.tsx"
      provides: "Factory Manager dashboard shell"
      contains: "DashboardShell"
  key_links:
    - from: "lib/workflow.ts"
      to: "/factory-operations/dashboard"
      via: "ROLE_DASHBOARD map"
      pattern: "factory_operations:\\s*'/factory-operations/dashboard'"
    - from: "app/_components/sidebar-nav.tsx"
      to: "factory-operations/factory-manager nav"
      via: "NAV record entries"
      pattern: "factory_operations|factory_manager"
---

<objective>
Give `factory_operations` and `factory_manager` their own dashboard shells (nav + landing page), completing `ROLE-01`. The role enum values already exist (`db/schema.ts` line 16); the gap is that both currently resolve to `/production/dashboard` via `ROLE_DASHBOARD` in `lib/workflow.ts` (lines 222-223) and have no nav entry or dashboard page. This replicates the exact Phase 15 pattern already used for `design`, `production`, `architect`, and `customer_care`.

Purpose: A newly-created Factory Operations or Factory Manager user gets a role-appropriate landing surface instead of borrowing Production's — the ROLE-01 success criterion. No novel UI design; it is a direct copy of the existing shell pattern (no UI-SPEC needed).
Output: Two new dashboard route files, two ROLE_DASHBOARD entries, two sidebar NAV entries, and matching About-page + flow-diagram role descriptions.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- The shell every role dashboard renders (app/_components/dashboard-shell.tsx). -->
export type Tile = { title: string; description: string; href?: string; status: 'ready' | 'soon' }
export default function DashboardShell(props: { userName: string; role: string; tiles: Tile[] })

<!-- Existing analog to copy verbatim (app/(app)/architect/dashboard/page.tsx) — server component, auth() + DashboardShell. -->
<!-- ROLE_DASHBOARD entries to change (lib/workflow.ts lines 219-224): -->
factory_operations: '/production/dashboard',  // -> '/factory-operations/dashboard'
factory_manager: '/production/dashboard',      // -> '/factory-manager/dashboard'

<!-- Sidebar NAV record shape (app/_components/sidebar-nav.tsx, NAV, ~line 13). Each new role gets a 4-item nav mirroring `production`/`architect`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the two dashboard shells, ROLE_DASHBOARD entries, and sidebar nav</name>
  <files>lib/workflow.ts, app/_components/sidebar-nav.tsx, app/(app)/factory-operations/dashboard/page.tsx, app/(app)/factory-manager/dashboard/page.tsx</files>
  <read_first>
    - app/(app)/architect/dashboard/page.tsx (the exact template to copy)
    - app/(app)/production/dashboard/page.tsx (Tile shape + "soon" status usage)
    - lib/workflow.ts (lines 210-228 — ROLE_DASHBOARD map + roleDashboard)
    - app/_components/sidebar-nav.tsx (lines 13-97 — NAV record, note production/architect entries)
  </read_first>
  <action>
    (a) In `lib/workflow.ts`, change the `ROLE_DASHBOARD` map: `factory_operations` from `'/production/dashboard'` to `'/factory-operations/dashboard'`, and `factory_manager` from `'/production/dashboard'` to `'/factory-manager/dashboard'`. Remove the now-inaccurate comment at lines 219-221 that says these reuse the Production dashboard.
    (b) Create `app/(app)/factory-operations/dashboard/page.tsx` by copying the architect dashboard exactly, changing `role="factory_operations"`, `userName` fallback to `'Factory Operations'`, the component name to `FactoryOperationsDashboardPage`, and the first tile description to reference the Production Process checklist (STG-13 lands here later) with `status: 'ready'` for Processes/Profile/About tiles and a "Project flows" tile noting production steps appear once assigned. Keep `export const dynamic = 'force-dynamic'`.
    (c) Create `app/(app)/factory-manager/dashboard/page.tsx` the same way with `role="factory_manager"`, `userName` fallback `'Factory Manager'`, component `FactoryManagerDashboardPage`, and a "Project flows" tile referencing Quality Control readiness forms (STG-14 lands here later).
    (d) In `app/_components/sidebar-nav.tsx`, add two entries to the `NAV` record — `factory_operations` and `factory_manager` — each a 4-item array mirroring the `production` entry: `{ href: '/<role-slug>/dashboard', icon: 'dashboard', label: 'Dashboard' }`, then the shared Processes / Profile / About items (icons `account_tree`/`person`/`info`). Use slug `factory-operations` and `factory-manager` to match the route dirs.
  </action>
  <acceptance_criteria>
    - `grep -c "factory_operations: '/factory-operations/dashboard'" lib/workflow.ts` == 1
    - `grep -c "factory_manager: '/factory-manager/dashboard'" lib/workflow.ts` == 1
    - `grep -c "/production/dashboard" lib/workflow.ts` == 0
    - `app/(app)/factory-operations/dashboard/page.tsx` and `app/(app)/factory-manager/dashboard/page.tsx` both exist and contain `DashboardShell` with the correct `role=` prop
    - `sidebar-nav.tsx` NAV record has `factory_operations` and `factory_manager` keys (grep both present)
    - `npx tsc --noEmit` passes; `npm run lint` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Both new roles route to and render their own dashboard shell with their own nav; neither falls back to Production.</done>
</task>

<task type="auto">
  <name>Task 2: Add Factory Operations and Factory Manager to the About organogram and flow diagram</name>
  <files>app/(app)/about/page.tsx, app/_components/trt-flow-diagram.tsx</files>
  <read_first>
    - app/(app)/about/page.tsx (lines 11-60 — the ROLES array; note it currently ends at Super Admin with no factory_operations/factory_manager)
    - app/_components/trt-flow-diagram.tsx (lines 11-24 — ROLE_COLOR already has factory_operations/factory_manager; the WorkflowRole ordering list ~lines 76-77; DETAIL map ~line 24)
    - .planning/STATE.md (Pending Todos — the /about + trt-flow-diagram growth note)
  </read_first>
  <action>
    (a) In `app/(app)/about/page.tsx`, add two entries to the `ROLES` array (before Super Admin, alongside Production): `{ name: 'Factory Operations', icon: 'precision_manufacturing', blurb: '...' }` describing that it runs the Production Process on the factory floor (optimization document, cutting/edging/upholstery/glass/accessories — its workflow steps roll out with Phase 22), and `{ name: 'Factory Manager', icon: 'verified', blurb: '...' }` describing that it performs Quality Control (uploads the Material/Accessories/Upholstery readiness forms) immediately before the existing Materials/Accessories Readiness step. Keep blurbs honest that steps are configured in a later phase.
    (b) In `app/_components/trt-flow-diagram.tsx`, confirm `ROLE_COLOR` already includes `factory_operations` and `factory_manager` (it does — no change needed there); ensure the role legend/ordering array (~lines 76-77) already lists both (it does). Only add DETAIL blurbs if a factory_operations/factory_manager step `key` is already present in the live graph and currently renders blank — otherwise make NO change to trt-flow-diagram.tsx and note in the summary that its role maps already cover the two roles.
  </action>
  <acceptance_criteria>
    - `grep -c "Factory Operations" "app/(app)/about/page.tsx"` >= 1
    - `grep -c "Factory Manager" "app/(app)/about/page.tsx"` >= 1
    - `app/_components/trt-flow-diagram.tsx` ROLE_COLOR still has both `factory_operations` and `factory_manager` keys (unchanged or intentionally left)
    - `npx tsc --noEmit` passes; `npm run lint` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>The About organogram lists all four front-of-funnel/factory roles including the two new shells, and the flow diagram's role maps cover them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> role dashboard route | A user reaching another role's dashboard URL should see only their own nav |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Elevation of Privilege | new /factory-operations & /factory-manager dashboards | accept | Follows the existing Phase 15 shell pattern exactly — nav is role-gated in sidebar-nav.tsx; these dashboards render only inert tiles (no operational data or mutations), identical to design/production/architect, so direct-URL access exposes nothing sensitive |
| T-19-06 | Information Disclosure | dashboard tiles | mitigate | Tiles link only to already-authorized shared surfaces (/processes, /profile, /about); no project or checklist data is rendered on these landing pages |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Manual (optional): sign in as a factory_operations / factory_manager demo user and confirm landing on the correct dashboard with the correct nav
</verification>

<success_criteria>
- factory_operations and factory_manager each land on their own dashboard with their own nav (ROLE-01)
- No role falls back to /production/dashboard
- About organogram and flow diagram cover both new roles
</success_criteria>

<output>
Create `.planning/phases/19-new-roles-assignment/19-02-SUMMARY.md` when done.
</output>
