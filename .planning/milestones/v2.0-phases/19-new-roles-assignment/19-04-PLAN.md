---
phase: 19-new-roles-assignment
plan: "04"
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - scripts/verify-role-assignment.ts
autonomous: true
requirements: [ROLE-02, ROLE-03, ROLE-06, ROLE-07]
must_haves:
  truths:
    - "ROLE-02 (multi-role assignment pool + assignee notification), ROLE-06 (architect role + dashboard), and ROLE-07 (targetRoles array) are verified against the actual shipped code, not assumed"
    - "ROLE-03 is confirmed: the role enum contains no super-admin title values"
    - "REQUIREMENTS.md and ROADMAP.md reflect the true completion state of every ROLE-01..07 item after Phase 19"
    - "Any genuine gap found during verification is recorded as a finding, not silently marked complete"
  artifacts:
    - path: "scripts/verify-role-assignment.ts"
      provides: "asserts assignUser pool-membership + targetRoles array behavior"
      contains: "targetRoles"
  key_links:
    - from: "scripts/verify-role-assignment.ts"
      to: "lib/workflow-graph assignUser"
      via: "pool membership assertion"
      pattern: "assignUser|targetRoles"
---

<objective>
Reconcile the stale bookkeeping for Phase 19's already-shipped items and finalize phase documentation. Per STATE.md's Decisions log (ad hoc commit 0d8bacd, 2026-07-09), `ROLE-02` (assignment step targets a multi-role pool + notifies the assignee), `ROLE-06` (`architect` as its own role with a dashboard), and `ROLE-07` (`workflow_step_definitions.targetRoles` widened from a single role to an array) are functionally shipped, and `ROLE-03` (no new role-enum values for super-admin titles) is already true. REQUIREMENTS.md still lists them "[ ] Pending" — this plan VERIFIES each against the real code (not assumption), then updates REQUIREMENTS.md + ROADMAP.md for all of ROLE-01..07 once 19-01/02/03 have landed.

Purpose: Avoid re-implementing shipped work while guaranteeing the "complete" marks are earned by actual verification. This plan runs last (Wave 3) so it can also confirm the newly-built ROLE-01/04/05 work before recording completion.
Output: A verification script for the assignment pool, and truthful REQUIREMENTS.md/ROADMAP.md state.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Already-shipped code to verify, do NOT rebuild: -->
<!-- lib/workflow-graph.ts assignUser — checks pool membership against targetRoles, not role equality (STATE.md Decisions, commit 0d8bacd). -->
<!-- db/schema.ts workflowStepDefinitions.targetRoles: roleEnum('target_role').array() (line 102) — the ROLE-07 array. -->
<!-- db/schema.ts roleEnum (line 16) — contains architect + factory_operations + factory_manager; NO super-admin title values (ROLE-03). -->
<!-- app/(app)/architect/dashboard/page.tsx — ROLE-06 dashboard shell (exists). -->
<!-- actions/workflow-graph.ts assignUser action + assignee notification path. -->
<!-- scripts/verify-design-pipeline.ts — existing harness exercising the multi-role assignment pool. -->
</interfaces>

<decision>
D-19-04-A: ROLE-01's roadmap wording "ops_factory" is reconciled at the requirement layer to the already-shipped enum value `factory_operations` (there is no `ops_factory` in the live enum and none is to be added). This is a documentation reconciliation only — no code/enum rename.
</decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the already-shipped ROLE-02 / ROLE-03 / ROLE-06 / ROLE-07 against real code</name>
  <files>scripts/verify-role-assignment.ts</files>
  <read_first>
    - lib/workflow-graph.ts (assignUser — confirm it validates the assignee's role is IN the step's targetRoles pool, not equality)
    - actions/workflow-graph.ts (assignUserAction + wherever the assignee notification is fired)
    - db/schema.ts (line 16 roleEnum; line 102 targetRoles array column)
    - app/(app)/architect/dashboard/page.tsx (ROLE-06 dashboard exists)
    - scripts/verify-design-pipeline.ts (existing multi-role pool harness — reuse its connection + assertion style)
  </read_first>
  <action>
    Create `scripts/verify-role-assignment.ts` (neon-http boilerplate as in the existing verify/migrate scripts) that asserts and prints PASS/FAIL for:
    (1) ROLE-07: at least one live `workflow_step_definitions` row has a `targetRoles` array of length >= 2 (the design/architect pool), proving the array widening is in effect; and the column is an array type.
    (2) ROLE-02: `assignUser` in lib/workflow-graph.ts gates on pool membership — assert by static check that a user whose role is IN a step's targetRoles can be assigned and a user whose role is NOT in the pool is rejected (reuse the logic exercised by scripts/verify-design-pipeline.ts; you may import and call the same engine function against a known live assignment step, or replicate its two-case assertion).
    (3) ROLE-03: `roleEnum` values contain NONE of the super-admin title strings (assert the enum array excludes `managing_director`, `executive_director`, `chief_operating_officer`, `head_of_operations`, `head_of_projects`, `chief_production_officer` — these live in `users.position`, never the role enum).
    (4) ROLE-06: `architect` is present in `roleEnum` AND `app/(app)/architect/dashboard/page.tsx` exists (fs check).
    Print an overall summary line. If ANY assertion FAILS, exit non-zero and print the specific gap — do NOT swallow it. Then run the existing `npx tsx scripts/verify-design-pipeline.ts` and confirm it still passes as corroboration.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/verify-role-assignment.ts` runs and prints a PASS for all four checks (or a specific FAIL that must then be escalated as a finding, not marked complete)
    - `npx tsx scripts/verify-design-pipeline.ts` passes (multi-role pool + two assignments)
    - The script exits non-zero on any failed assertion (grep for a `process.exit(1)` on failure)
    - `npx tsc --noEmit` passes on the new script
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-role-assignment.ts && npx tsx scripts/verify-design-pipeline.ts</automated>
  </verify>
  <done>ROLE-02/03/06/07 are confirmed genuinely met by the shipped code (or any real gap is surfaced as an explicit FAIL for escalation).</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md to the true post-Phase-19 state</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 233-247 ROLE-01..07 checkboxes; lines 290-301 v2.0 traceability table)
    - .planning/ROADMAP.md (lines 341-359 Phase 19 section; lines 430-433 progress table)
    - .planning/phases/19-new-roles-assignment/19-01-SUMMARY.md, 19-02-SUMMARY.md, 19-03-SUMMARY.md (the actual delivered state — only mark complete what these confirm)
  </read_first>
  <action>
    Only after Task 1 PASSes and the 19-01/02/03 summaries confirm delivery:
    (a) In `.planning/REQUIREMENTS.md`, flip ROLE-01 (dashboards — 19-02), ROLE-04 (position enum — 19-01), ROLE-05 (self-service/creation — 19-03) checkboxes from `[ ]` to `[x]`, and ROLE-02, ROLE-03, ROLE-06, ROLE-07 from `[ ]` to `[x]` with an inline note "verified 2026-07-11 (shipped ad hoc commit 0d8bacd; confirmed by scripts/verify-role-assignment.ts)". Add a note under ROLE-01 reconciling "ops_factory" wording to the shipped `factory_operations` value (D-19-04-A). Update the v2.0 traceability table row for ROLE-01..07 from "Pending" to "Complete".
    (b) In `.planning/ROADMAP.md`, change the Phase 19 checkbox (line 39) from `[ ]` to `[x]`, add a `**Status**: Complete` note to the Phase 19 detail section, list the four plans under a `Plans:` block (19-01 position enum, 19-02 dashboards, 19-03 position lifecycle + configurator, 19-04 reconciliation/verification), and update the progress table row (line 431) from `0/?` / `Not started` to `4/4` / `Complete` with the completion date 2026-07-11.
    (c) Do NOT alter any requirement text or success criteria — only status markers, the four plan entries, and the reconciliation notes.
  </action>
  <acceptance_criteria>
    - `grep -c "\[ \] \*\*ROLE-0" .planning/REQUIREMENTS.md` == 0 (no ROLE requirement left unchecked)
    - The v2.0 traceability table's ROLE-01..07 row reads Complete (grep confirms no "Pending" on that row)
    - ROADMAP.md Phase 19 checkbox is `[x]` and its progress-table row reads `4/4` / `Complete`
    - ROADMAP.md Phase 19 lists exactly the four plan entries (19-01..19-04)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\[ \] \*\*ROLE-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md and ROADMAP.md truthfully reflect Phase 19 completion, with the ROLE-02/03/06/07 marks backed by Task 1's verification and the ops_factory/factory_operations wording reconciled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation claim -> actual code state | Marking a requirement complete must reflect verified reality, not assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Repudiation | premature "complete" marks | mitigate | Task 2 gates every checkbox flip on Task 1's script PASS + the 19-01/02/03 summaries; a failed assertion blocks the doc update and surfaces a finding |
| T-19-11 | Spoofing | assignUser pool membership | mitigate | verify-role-assignment.ts asserts a non-pool role is rejected (ROLE-02), confirming the shipped gate is not merely cosmetic |
| T-19-10-SC | Tampering | npm/pip installs | mitigate | No new packages introduced — no legitimacy gate needed |
</threat_model>

<verification>
- `npx tsx scripts/verify-role-assignment.ts` PASS on all four checks
- `npx tsx scripts/verify-design-pipeline.ts` PASS
- No ROLE-0x requirement left `[ ]` in REQUIREMENTS.md
- ROADMAP.md Phase 19 marked complete with four plan entries
</verification>

<success_criteria>
- ROLE-02, ROLE-03, ROLE-06, ROLE-07 verified against shipped code and marked complete (or a real gap escalated)
- Phase 19 documentation truthfully reflects completion of ROLE-01..07
- "ops_factory" wording reconciled to the shipped factory_operations value with no code rename
</success_criteria>

<output>
Create `.planning/phases/19-new-roles-assignment/19-04-SUMMARY.md` when done, recording the verify-role-assignment.ts results and any findings.
</output>
