---
phase: 16-workflow-engine-core
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - lib/workflow.ts
  - lib/workflow-graph.ts
  - db/seed-workflow-graph.ts
  - package.json
autonomous: true
requirements: [WF-01, WF-02, WF-05]
must_haves:
  truths:
    - "The engine loads the step graph from the database, not from a literal array"
    - "actionable-step resolution reads the live graph so a step inserted into the tables is reflected without a code deploy"
    - "A join step is only actionable once ALL its predecessor branch steps are complete, regardless of order"
    - "Every existing lib/workflow.ts export still works so no current caller breaks in this phase"
  artifacts:
    - path: "lib/workflow-graph.ts"
      provides: "server-only DB read engine: getGraphSteps, getGraphEdges, getStepById/Key, getCompletedStepIds, getActionableSteps, getFirstActionStep, getLastStep"
      exports: ["getGraphSteps", "getActionableSteps", "getStepByKey"]
      min_lines: 60
    - path: "lib/workflow.ts"
      provides: "extended StepKind union + GraphStep type; existing sync API preserved"
      contains: "yes_no_upload"
    - path: "db/seed-workflow-graph.ts"
      provides: "structural seed of the current 11 live steps + linear edges into the graph tables"
      contains: "workflow_step_definitions"
  key_links:
    - from: "lib/workflow-graph.ts getActionableSteps"
      to: "workflow_step_edges"
      via: "predecessor-completeness query"
      pattern: "workflowStepEdges"
    - from: "db/seed-workflow-graph.ts"
      to: "lib/workflow.ts WORKFLOW_STEPS"
      via: "1:1 structural copy of key/label/role/kind/slug/order"
      pattern: "orderIndex"
---

<objective>
Build the server-only read engine (lib/workflow-graph.ts) that loads the step graph from the database and resolves which steps are actionable using explicit edges — so a join step becomes actionable only when all its branches complete (WF-05), and a step added directly to the data is reflected without a redeploy (WF-02). Extend lib/workflow.ts types for the new kinds/optional flag while keeping every existing export intact. Seed the 11 current live steps into the tables so the engine has real data (WF-01).

Purpose: This is the read half of the engine every consumer will eventually use.
Output: Read engine module, extended shared types, and a populated 'live' graph.

IMPORTANT SCOPE BOUNDARY: This plan does NOT rewire the ~20 existing callers (my-work, projects-board, header switcher, board, timeline) from the sync array API to the DB engine. Those keep working unchanged off the existing WORKFLOW_STEPS array. Flipping live callers to the DB engine with zero-regression verification is Phase 17's job (it is the highest-risk phase and is isolated for that reason). Keep the existing sync exports behaviorally identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md

<interfaces>
From db/schema.ts (after plan 01): workflowStepDefinitions { id, graph, stepKey, label, role, fulfillmentKind, checklistSlug, targetRole, isOptional, orderIndex }, workflowStepEdges { id, graph, fromStepId, toStepId }, workflowStepStates { ..., status }, projectStepCompletions now has { stepDefId, graph, skipped }.
From db/index.ts: db = drizzle(sql, { schema }) — server-only (import 'server-only' at top).
From lib/workflow.ts (current): StepKind = 'creation'|'checklist'|'readiness'|'ack'; WorkflowStep = { n, key, label, role, kind, slug? }; WORKFLOW_STEPS (11 rows); stepByN, LAST_STEP=11, FIRST_ACTION_STEP=2, isProjectComplete, canRoleActOnStep, stepHref, workflowRoleLabel — all synchronous, imported by client components (header-project-switcher.tsx, project-steps-board.tsx, pending-step-gate.tsx, trt-flow-diagram.tsx). The file header states it must stay free of server-only imports — HONOR THIS: do not import db or drizzle into lib/workflow.ts.
From db/seed-workflow-checklists.ts: the seed-script pattern — its own neon+drizzle client, config path .env.local, run via tsx, idempotent with onConflictDoNothing / delete-then-insert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared workflow types (client-safe, additive only)</name>
  <read_first>
    - lib/workflow.ts (read fully — you are extending types WITHOUT breaking the sync exports that client components depend on)
    - app/_components/project-steps-board.tsx (a client consumer — confirm which exports it imports so you do not change their shapes)
  </read_first>
  <action>
    In lib/workflow.ts, additively extend the shared types (keep the file free of server-only imports — no db import):
    1. Extend the StepKind union to 'creation' | 'checklist' | 'readiness' | 'ack' | 'yes_no_upload' | 'approval' | 'assignment'.
    2. Add a new exported type GraphStep = { id: string; graph: string; key: string; label: string; role: WorkflowRole; kind: StepKind; slug?: string | null; targetRole?: WorkflowRole | null; isOptional: boolean; orderIndex: number }. Keep the legacy WorkflowStep type unchanged for existing array callers.
    3. Add a pure exported helper graphStepHref(step: GraphStep, projectId: string): string | null — for kind 'checklist' with a slug return /checklists/[slug]?projectId=...&step=[key]; for the 3 new kinds return /workflow/step?projectId=...&step=[key] (the minimal renderers in plan 05 live under /workflow/step); for 'ack' return null. Keep the existing stepHref(step, projectId) for legacy WorkflowStep callers unchanged — do not touch it.
    Do NOT remove or change the signatures of WORKFLOW_STEPS, stepByN, LAST_STEP, FIRST_ACTION_STEP, isProjectComplete, canRoleActOnStep, workflowRoleLabel, Roles, or any label map. Additive only.
  </action>
  <acceptance_criteria>
    - grep for yes_no_upload in lib/workflow.ts returns 1 or more and the StepKind union includes all 7 values.
    - export type GraphStep is present with an isOptional: boolean field.
    - Every legacy export still present: grep count of WORKFLOW_STEPS, FIRST_ACTION_STEP, LAST_STEP, stepByN is greater than or equal to its pre-edit count.
    - grep for `from '@/db'` and `import 'server-only'` in lib/workflow.ts returns 0 (stays client-safe).
    - npx tsc --noEmit reports no new errors in any existing consumer of lib/workflow.ts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && grep -c "yes_no_upload" lib/workflow.ts && grep -c "from '@/db'" lib/workflow.ts && npx tsc --noEmit 2>&1 | grep -iE "workflow|project-steps-board|header-project" | head</automated>
  </verify>
  <done>StepKind + GraphStep extended; all legacy exports intact; lib/workflow.ts still client-safe; typecheck clean for consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Create the server-only read engine lib/workflow-graph.ts</name>
  <read_first>
    - db/schema.ts (the tables from plan 01 — exact column names for the queries)
    - db/index.ts (how the server db client is imported)
    - lib/workflow.ts (import the GraphStep type + WorkflowRole/StepKind unions from here — do NOT re-declare them)
    - lib/my-work.ts (the shape the eventual Phase 17 consumer needs — design return types to be drop-in there)
  </read_first>
  <action>
    Create lib/workflow-graph.ts starting with `import 'server-only'`. Import db from '@/db', the three graph tables + projectStepCompletions from '@/db/schema', and eq/and/inArray from 'drizzle-orm'. Import GraphStep + types from '@/lib/workflow'. Implement:
    1. getGraphSteps(graph = 'live'): Promise<GraphStep[]> — select all workflowStepDefinitions where graph = graph, ordered by orderIndex asc, mapped to GraphStep (fulfillmentKind -> kind, checklistSlug -> slug).
    2. getStepByKey(graph, key): Promise<GraphStep | undefined> and getStepById(id): Promise<GraphStep | undefined>.
    3. getGraphEdges(graph = 'live'): Promise<{ fromStepId: string; toStepId: string }[]>.
    4. getCompletedStepIds(projectId): Promise<Set<string>> — select stepDefId from projectStepCompletions where projectId = projectId AND stepDefId is not null (skipped rows count as satisfied — include them). Return a Set of stepDefId strings.
    5. getActionableSteps(projectId, graph = 'live'): Promise<GraphStep[]> — the join-readiness core (WF-05): a step is actionable iff (a) it is NOT already in getCompletedStepIds, AND (b) EVERY incoming edge's fromStepId IS in getCompletedStepIds (a step with zero incoming edges is an entry step and is actionable). Compute from getGraphSteps + getGraphEdges + getCompletedStepIds. This makes a join step (2+ incoming edges) actionable only once ALL its predecessors are complete, regardless of completion order.
    6. getFirstActionStep(graph = 'live'): Promise<GraphStep | undefined> — the entry step with the lowest orderIndex that has no incoming edges. getLastStep(graph = 'live'): Promise<GraphStep | undefined> — the step with the highest orderIndex that has no outgoing edges.
    All functions are async and read the DB live on each call (no module-level array cache) so inserted rows are reflected without a redeploy (WF-02).
  </action>
  <acceptance_criteria>
    - lib/workflow-graph.ts starts with `import 'server-only'` and imports db from '@/db'.
    - Exports getGraphSteps, getStepByKey, getStepById, getGraphEdges, getCompletedStepIds, getActionableSteps, getFirstActionStep, getLastStep.
    - getActionableSteps references workflowStepEdges and computes predecessor-completeness against the getCompletedStepIds Set (grep confirms both `workflowStepEdges` and the completed-set usage appear in the function body).
    - No literal step array is declared in this file (grep for a hardcoded steps array returns nothing; data comes only from db queries).
    - npx tsc --noEmit passes with no errors in lib/workflow-graph.ts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && head -1 lib/workflow-graph.ts && grep -c "workflowStepEdges\|getActionableSteps" lib/workflow-graph.ts && npx tsc --noEmit 2>&1 | grep -i "workflow-graph.ts" | head</automated>
  </verify>
  <done>The read engine loads the graph, edges, and completions from the DB and correctly derives actionable steps (including join readiness) with no hardcoded array.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the current 11 live steps into the graph tables (structural seed)</name>
  <read_first>
    - lib/workflow.ts (WORKFLOW_STEPS — the exact 11 rows: n, key, label, role, kind, slug — copy these 1:1)
    - db/seed-workflow-checklists.ts (the exact seed-script scaffold: neon client, config .env.local, tsx entrypoint, idempotent pattern)
    - package.json (the scripts block — add a new db:seed-workflow-graph entry next to the existing db:seed-* scripts)
  </read_first>
  <action>
    Create db/seed-workflow-graph.ts following the db/seed-workflow-checklists.ts scaffold exactly (own neon+drizzle client, config path .env.local, main().catch process.exit(1)). Seed the 'live' graph as a mechanical 1:1 structural copy of WORKFLOW_STEPS: for each of the 11 steps insert a workflowStepDefinitions row with graph='live', stepKey = step.key, label = step.label, role = step.role, fulfillmentKind = step.kind, checklistSlug = step.slug ?? null, targetRole = null, isOptional = false, orderIndex = step.n. Then insert linear workflowStepEdges: one edge from step n to step n+1 for n = 1..10 (resolve the definition ids after insert). Make it idempotent: delete existing graph='live' edges and definitions first (edges before definitions to respect FKs), then re-insert. Add a package.json script `"db:seed-workflow-graph": "tsx db/seed-workflow-graph.ts"`. Run it. This is a STRUCTURAL seed only — it copies the existing steps' shape, NOT the Phase 17 verified content migration (no checklist item content changes here).
  </action>
  <acceptance_criteria>
    - db/seed-workflow-graph.ts exists and inserts 11 workflow_step_definitions rows (graph='live') plus 10 linear edges.
    - package.json contains the db:seed-workflow-graph script.
    - Running `npm run db:seed-workflow-graph` exits 0 and is idempotent (a second run also exits 0 and leaves exactly 11 definitions + 10 edges, not 22/20).
    - The seeded step keys match WORKFLOW_STEPS exactly (new_project, confirmation, ..., sign_off).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npm run db:seed-workflow-graph 2>&1 | tail -15 && npm run db:seed-workflow-graph 2>&1 | tail -5</automated>
  </verify>
  <done>The 'live' graph holds the 11 current steps and 10 linear edges; re-running the seed is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client component → lib/workflow.ts | client bundle imports this module; it must never pull in server-only DB code |
| engine → database | read queries scoped by graph name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | lib/workflow.ts leaking DATABASE_URL/db into the client bundle | mitigate | Keep lib/workflow.ts free of `import 'server-only'`/db imports; the DB engine lives only in lib/workflow-graph.ts which starts with `import 'server-only'` |
| T-16-04 | Tampering | actionable-step resolution bypassed by wrong graph scoping | mitigate | Every engine query filters by `graph`; test graph rows cannot satisfy live-graph predecessor checks |
</threat_model>

<verification>
- lib/workflow.ts stays client-safe (no db/server-only imports) and all legacy exports remain.
- lib/workflow-graph.ts reads the graph/edges/completions from the DB with no hardcoded array.
- getActionableSteps returns join steps only when all predecessors are complete.
- The 'live' graph is seeded with 11 steps + 10 edges, idempotently.
</verification>

<success_criteria>
The engine can read the step graph from the database and compute actionable steps (including native parallel/join readiness), and the current 11-step workflow exists as data — with zero disruption to existing synchronous callers.
</success_criteria>

<output>
Create `.planning/phases/16-workflow-engine-core/16-02-SUMMARY.md` when done.
</output>
