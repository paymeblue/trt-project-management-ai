---
phase: 17-confirmation-sign-off-migration
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-04"]
files_modified:
  - app/_components/project-steps-board.tsx
  - app/_components/header-project-switcher.tsx
  - app/_components/pending-step-gate.tsx
  - app/(app)/admin/projects/new/new-project-form.tsx
autonomous: true
requirements: [WF-06]

must_haves:
  truths:
    - "The project board renders each step's done/current/locked state, act-href, and step count identically, sourcing steps from useWorkflowSteps() instead of the WORKFLOW_STEPS literal"
    - "The header switcher and pending-step gate resolve the current step (label/role/href) from useWorkflowSteps() with identical output"
    - "The new-project form renders a deadline input for every actionable step (n >= FIRST_ACTION_STEP) from useWorkflowSteps(), with identical ordering validation"
  artifacts:
    - path: "app/_components/project-steps-board.tsx"
      provides: "board rendering from useWorkflowSteps()"
      contains: "useWorkflowSteps"
    - path: "app/_components/header-project-switcher.tsx"
      provides: "switcher from useWorkflowSteps()"
      contains: "useWorkflowSteps"
  key_links:
    - from: "app/_components/project-steps-board.tsx"
      to: "app/_components/workflow-steps-provider.tsx:useWorkflowSteps"
      via: "hook read of live steps"
      pattern: "useWorkflowSteps"
---

<objective>
Cut over the four remaining CLIENT components — the project steps board, the header project switcher, the forcing pending-step gate, and the new-project form — from importing the `WORKFLOW_STEPS` literal (and the legacy array-based `stepByN`/`LAST_STEP`/`isProjectComplete`) to reading the live steps via `useWorkflowSteps()` (plan 04's provider) plus the pure helpers `findStep`/`lastStepN`/`projectComplete` from plan 01. Behavior — done/current/locked state, act-hrefs, step counts, deadline inputs, ordering validation — must stay byte-identical.

Purpose: Retire the last live consumers of the hardcoded array so plan 06 can delete the literal.
Output: every client step-reader on the DB-sourced steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md
@app/_components/project-steps-board.tsx
@app/_components/header-project-switcher.tsx
@app/_components/pending-step-gate.tsx
@app/(app)/admin/projects/new/new-project-form.tsx

<interfaces>
From app/_components/workflow-steps-provider.tsx (plan 04): useWorkflowSteps(): WorkflowStep[]
From lib/workflow.ts (plan 01, pure — client-safe, no server import):
  findStep(steps, n), lastStepN(steps), projectComplete(currentStep, lastN),
  canRoleActOnStep [unchanged], stepHref(step, projectId) [unchanged], workflowRoleLabel [unchanged],
  FIRST_ACTION_STEP=2 [unchanged constant]

Legacy → new mapping in client components:
  import { WORKFLOW_STEPS } + iterate     -> const steps = useWorkflowSteps(); iterate steps
  stepByN(n)                              -> findStep(steps, n)
  LAST_STEP                               -> lastStepN(steps)
  isProjectComplete(currentStep)          -> projectComplete(currentStep, lastStepN(steps))
  WORKFLOW_STEPS.find(s => s.n === cs)    -> findStep(steps, cs)

All four are 'use client' components rendered under the (app) layout, so useWorkflowSteps() is in scope.
Call the hook at the top level of each component (hooks rules) and thread `steps`/`lastN` into local
functions (e.g. board's currentStepLabel/needsViewer) as arguments or via closure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut over the project steps board</name>
  <read_first>
    - app/_components/project-steps-board.tsx (StepsModal iterates WORKFLOW_STEPS; currentStepLabel/needsViewer use WORKFLOW_STEPS.find + LAST_STEP; done = step.n < currentStep; isProjectComplete usages)
    - app/_components/workflow-steps-provider.tsx (useWorkflowSteps)
  </read_first>
  <action>
    In app/_components/project-steps-board.tsx: remove the `WORKFLOW_STEPS`, `LAST_STEP`, `stepHref? (keep)`, `isProjectComplete` imports of the LITERAL/legacy helpers and instead call `const steps = useWorkflowSteps()` at the top of the components that need them (the default-exported ProjectStepsBoard and StepsModal — pass `steps` and `lastN = lastStepN(steps)` down as props to StepsModal, or call the hook in StepsModal directly since it is rendered under the provider). Replace: the `WORKFLOW_STEPS.map(...)` in StepsModal with `steps.map(...)` (keep `done = step.n < project.currentStep`, `current = step.n === project.currentStep`, canRoleActOnStep, stepHref, workflowRoleLabel exactly); `WORKFLOW_STEPS.find(s => s.n === p.currentStep)` in currentStepLabel/needsViewer with `findStep(steps, p.currentStep)`; `LAST_STEP` with `lastN`; every `isProjectComplete(p.currentStep)` with `projectComplete(p.currentStep, lastN)`. Keep stepHref and canRoleActOnStep imports from '@/lib/workflow'. All markup, the Countdown/Flag/Bypass/Ack subcomponents, and the poll unchanged.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "WORKFLOW_STEPS\|isProjectComplete\b" app/_components/project-steps-board.tsx && grep -q "useWorkflowSteps" app/_components/project-steps-board.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The board reads steps via useWorkflowSteps() and uses findStep/lastStepN/projectComplete; no WORKFLOW_STEPS or isProjectComplete(literal) reference remains
    - done/current/locked, act-href, "X/lastN" count, NEEDS YOU, paused/delivered labels all render identically
    - Hooks are called at component top level (no conditional hook calls); `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The project board renders from DB-sourced steps with byte-identical behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Cut over the header switcher + pending-step gate</name>
  <read_first>
    - app/_components/header-project-switcher.tsx (stepByN + stepHref + canRoleActOnStep + LAST_STEP)
    - app/_components/pending-step-gate.tsx (stepByN + stepHref)
  </read_first>
  <action>
    In app/_components/header-project-switcher.tsx: add `const steps = useWorkflowSteps()`; replace `stepByN(selected.stepN)` and the dropdown `stepByN(p.stepN)` with `findStep(steps, ...)`, and `LAST_STEP` with `lastStepN(steps)`. Keep canRoleActOnStep, stepHref, workflowRoleLabel, dismiss/dropdown behavior exactly.

    In app/_components/pending-step-gate.tsx: add `const steps = useWorkflowSteps()`; replace `stepByN(item.stepN)` with `findStep(steps, item.stepN)`. Keep stepHref(step, item.projectId), the ack branch, dismissals, and isStepRoute logic exactly. Ensure the hook is called before the early `if (!item) return null` is fine — but hooks must run unconditionally, so call useWorkflowSteps() at the top with the other hooks (it is already after other hooks; keep it above any early return that could skip it).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "stepByN\|LAST_STEP" app/_components/header-project-switcher.tsx && ! grep -q "stepByN" app/_components/pending-step-gate.tsx && grep -q "useWorkflowSteps" app/_components/header-project-switcher.tsx && grep -q "useWorkflowSteps" app/_components/pending-step-gate.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both components resolve the current step via findStep(useWorkflowSteps(), stepN); no stepByN/LAST_STEP reference remains
    - Switcher shows the same "Step N/lastN: label · your turn/role" and Act href; the gate shows the same step title/role/deadline and ack-vs-href action
    - useWorkflowSteps() is called unconditionally (above any early return); `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Header switcher + forcing gate resolve steps from the provider with identical output.</done>
</task>

<task type="auto">
  <name>Task 3: Cut over the new-project form</name>
  <read_first>
    - app/(app)/admin/projects/new/new-project-form.tsx (ACTIONABLE_STEPS = WORKFLOW_STEPS.filter(n >= FIRST_ACTION_STEP); bounds/labelOf/onChange; deadline inputs; FIRST_ACTION_STEP/LAST_STEP display)
  </read_first>
  <action>
    In app/(app)/admin/projects/new/new-project-form.tsx: remove the module-level `const ACTIONABLE_STEPS = WORKFLOW_STEPS.filter(...)` and instead compute it inside the component from the hook: `const steps = useWorkflowSteps(); const actionableSteps = steps.filter(s => s.n >= FIRST_ACTION_STEP)`. Replace every ACTIONABLE_STEPS reference (bounds, labelOf, onChange, the render map) with actionableSteps, and `LAST_STEP` in the helper text with `lastStepN(steps)`. Keep FIRST_ACTION_STEP as the imported constant, keep workflowRoleLabel, and keep all deadline-ordering validation and toast behavior identical. Ensure hooks (useActionState/useState/useEffect and useWorkflowSteps) stay at the top; deriving actionableSteps from the hook value is fine.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "WORKFLOW_STEPS\|LAST_STEP" "app/(app)/admin/projects/new/new-project-form.tsx" && grep -q "useWorkflowSteps" "app/(app)/admin/projects/new/new-project-form.tsx" && npx next build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - The form derives actionable steps from useWorkflowSteps() filtered by FIRST_ACTION_STEP; no WORKFLOW_STEPS/LAST_STEP reference remains
    - A deadline input renders for every step n >= 2 with identical labels, ordering validation, and toast messages
    - `npx tsc --noEmit` and `npx next build` pass
  </acceptance_criteria>
  <done>The new-project form renders per-step deadline inputs from DB-sourced steps identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client step context → workflow UI gating | The board/switcher/gate decide which action a user is offered; the server still re-checks on submit (actions gate independently) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Elevation of privilege | client-side "your turn"/act-href derivation | mitigate | Client gating is advisory only; the server actions (plan 02) re-verify canRoleActOnStep + currentStep before any write — unchanged by this plan |
| T-17-12 | Tampering | board done/current/locked (step.n vs currentStep) | mitigate | Integer comparison logic preserved verbatim; steps sourced from the provider seeded by the plan-01-verified adapter |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npx next build` compiles the shell.
- No WORKFLOW_STEPS / legacy stepByN / LAST_STEP / literal isProjectComplete reference remains in the four client files.
- Hooks are unconditional (no lint hook-rule violations).
</verification>

<success_criteria>
- All four client step-readers render from the DB-sourced steps via useWorkflowSteps() with byte-identical done/current/locked, act-hrefs, counts, and deadline inputs.
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-05-SUMMARY.md` when done.
</output>
