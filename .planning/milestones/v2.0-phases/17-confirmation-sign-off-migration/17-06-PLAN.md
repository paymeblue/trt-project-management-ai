---
phase: 17-confirmation-sign-off-migration
plan: 06
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05"]
files_modified:
  - db/workflow-live-steps.ts
  - db/seed-workflow-graph.ts
  - lib/workflow.ts
  - tests/lib/workflow.test.ts
  - tests/lib/workflow-live.test.ts
autonomous: false
requirements: [WF-06]

must_haves:
  truths:
    - "The hardcoded WORKFLOW_STEPS literal, the LAST_STEP constant, and the legacy array-based stepByN/isProjectComplete are gone from lib/workflow.ts — no step data remains in a literal in the app's shared module"
    - "The canonical 11-step definition lives only in the seed-data module db/workflow-live-steps.ts, consumed solely by the seed script (bootstrap data, not app runtime)"
    - "grep across the codebase finds zero remaining references to WORKFLOW_STEPS, LAST_STEP, or the legacy stepByN/isProjectComplete signatures outside the seed-data module and tests"
    - "A real in-flight project (currentStep 3) and a real completed project (currentStep 12) render identically in gate/board/my-work/timeline/flow-diagram before and after the cutover"
  artifacts:
    - path: "db/workflow-live-steps.ts"
      provides: "canonical LIVE_WORKFLOW_STEPS bootstrap data (the relocated 11-step definition)"
      contains: "LIVE_WORKFLOW_STEPS"
    - path: "lib/workflow.ts"
      provides: "client-safe module with pure helpers only — no WORKFLOW_STEPS literal, no LAST_STEP, no legacy stepByN/isProjectComplete"
      contains: "export function lastStepN"
    - path: "tests/lib/workflow-live.test.ts"
      provides: "structure test asserting the 11 live steps + order via the seed-data module (and/or getLiveWorkflowSteps parity)"
      contains: "LIVE_WORKFLOW_STEPS"
  key_links:
    - from: "db/seed-workflow-graph.ts"
      to: "db/workflow-live-steps.ts:LIVE_WORKFLOW_STEPS"
      via: "seed reads canonical bootstrap data"
      pattern: "LIVE_WORKFLOW_STEPS"
---

<objective>
Close the migration: relocate the canonical 11-step definition out of the app's shared `lib/workflow.ts` into a dedicated seed-data module (`db/workflow-live-steps.ts`) consumed ONLY by the seed script, then delete the `WORKFLOW_STEPS` literal, the `LAST_STEP` constant, and the legacy array-based `stepByN`/`isProjectComplete` from `lib/workflow.ts` (keeping the pure helpers, `FIRST_ACTION_STEP`, types, and all role/label helpers). Update the tests to target the seed-data module and the DB adapter. Finally, run the full verification suite and a human before/after check that a real in-flight project renders identically.

After this plan, WF-06 is satisfied: no step data remains in a literal in the app's runtime path; the DB `graph='live'` is the single source of truth, read via `getLiveWorkflowSteps()`; and pre- and post-migration projects behave identically.

Purpose: Retire the hardcoded array (the explicit Phase 17 scope boundary) and prove zero regression.
Output: literal removed, seed-data module, updated tests, green verification, human-approved parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@AGENTS.md
@lib/workflow.ts
@db/seed-workflow-graph.ts
@tests/lib/workflow.test.ts
@scripts/verify-live-workflow.ts

<interfaces>
Canonical 11 steps (relocate verbatim from the current WORKFLOW_STEPS literal):
  1 new_project/operations/creation; 2 confirmation/site_pm/checklist/confirmation;
  3 materials_readiness/factory_pm/readiness; 4 delivery_readiness/site_pm/checklist/delivery_site_readiness;
  5 delivery_project/factory_pm/checklist/delivery_project; 6 project_check_report/factory_pm/checklist/project_check_report;
  7 approval_installation/operations/checklist/approval_to_commence_installation;
  8 installation_readiness/site_pm/checklist/installation_readiness; 9 sorting/site_pm/checklist/sorting;
  10 close_out/site_pm/checklist/close_out; 11 sign_off/super_admin/ack.

Seed (db/seed-workflow-graph.ts) currently imports WORKFLOW_STEPS from '../lib/workflow'. After this plan it
imports LIVE_WORKFLOW_STEPS from './workflow-live-steps'. verify-live-workflow.ts (plan 01) imports
WORKFLOW_STEPS for its parity reference — repoint it to LIVE_WORKFLOW_STEPS too.

Keep in lib/workflow.ts: WorkflowStep/WorkflowRole/StepKind/GraphStep/UserRole types, Roles, FIRST_ACTION_STEP,
findStep, lastStepN, projectComplete, canRoleActOnStep, stepHref, graphStepHref, workflowRoleLabel,
userRoleLabel, roleDashboard, isAdminRole, canEditChecklist, REQUIRED_PHOTOS, MyWork/ActiveProject/PendingWork.
Remove ONLY: WORKFLOW_STEPS, LAST_STEP, stepByN, isProjectComplete.

Real DB projects (verified): currentStep 3 (in-flight, not_delivered), 5 (in-flight), 12 (delivered x2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate seed data + delete the literal from lib/workflow.ts</name>
  <read_first>
    - lib/workflow.ts (WORKFLOW_STEPS literal + LAST_STEP + stepByN + isProjectComplete to remove; everything else to keep)
    - db/seed-workflow-graph.ts (imports WORKFLOW_STEPS from '../lib/workflow')
    - scripts/verify-live-workflow.ts (imports WORKFLOW_STEPS as parity reference)
  </read_first>
  <action>
    Create db/workflow-live-steps.ts exporting `export const LIVE_WORKFLOW_STEPS: WorkflowStep[]` — the exact 11-step array moved verbatim from lib/workflow.ts (import the WorkflowStep type from '@/lib/workflow'). This module is bootstrap/seed data; the app runtime never imports it.

    In db/seed-workflow-graph.ts, change the import from `WORKFLOW_STEPS` ('../lib/workflow') to `LIVE_WORKFLOW_STEPS` ('./workflow-live-steps') and use it wherever WORKFLOW_STEPS was used (definitions loop; the edge list from plan 01 stays as-is). Re-run `npm run db:seed-workflow-graph` to confirm it still produces the same 11 defs + join edges.

    In scripts/verify-live-workflow.ts, repoint the parity reference import from WORKFLOW_STEPS to LIVE_WORKFLOW_STEPS.

    In lib/workflow.ts, DELETE the `WORKFLOW_STEPS` literal, the `LAST_STEP` constant, the legacy `stepByN(n)` function, and the legacy `isProjectComplete(currentStep)` function. Keep FIRST_ACTION_STEP (redefine it as a plain `export const FIRST_ACTION_STEP = 2` if it currently derives from WORKFLOW_STEPS). Keep every other export. lib/workflow.ts must remain client-safe (no db/server-only import).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && ! grep -q "WORKFLOW_STEPS\|export const LAST_STEP\|export function stepByN\|export function isProjectComplete" lib/workflow.ts && grep -q "LIVE_WORKFLOW_STEPS" db/workflow-live-steps.ts && grep -q "LIVE_WORKFLOW_STEPS" db/seed-workflow-graph.ts && npm run db:seed-workflow-graph >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - db/workflow-live-steps.ts exports LIVE_WORKFLOW_STEPS (the verbatim 11 steps); seed + verify-live-workflow import it
    - lib/workflow.ts no longer contains WORKFLOW_STEPS, LAST_STEP, legacy stepByN, or legacy isProjectComplete; still client-safe
    - `npm run db:seed-workflow-graph` still yields 11 defs + the join edges; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The hardcoded array is retired from the app module and lives only as seed bootstrap data.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests + prove zero remaining literal references</name>
  <read_first>
    - tests/lib/workflow.test.ts (array-structure tests reference WORKFLOW_STEPS/LAST_STEP/stepByN/isProjectComplete)
  </read_first>
  <action>
    Split tests/lib/workflow.test.ts: KEEP the pure-helper tests (canRoleActOnStep, stepHref with a literal step object, workflowRoleLabel, userRoleLabel, roleDashboard, isAdminRole, canEditChecklist, REQUIRED_PHOTOS) but rewrite them against the surviving exports — replace legacy `stepByN(2)` usages with `findStep(LIVE_WORKFLOW_STEPS, 2)`, `LAST_STEP` with `lastStepN(LIVE_WORKFLOW_STEPS)`, `isProjectComplete(x)` with `projectComplete(x, lastStepN(LIVE_WORKFLOW_STEPS))`, importing LIVE_WORKFLOW_STEPS from '@/db/workflow-live-steps'. Create tests/lib/workflow-live.test.ts holding the structure assertions (11 steps, exact key/role order, sign_off is step 11 super_admin ack, materials_readiness is the first factory_pm step, no design/production step) targeting LIVE_WORKFLOW_STEPS. Ensure `npm test` passes. Then grep the whole app (excluding db/workflow-live-steps.ts, tests, and node_modules) to prove zero references to WORKFLOW_STEPS / legacy stepByN / LAST_STEP / literal isProjectComplete remain.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npm test >/dev/null 2>&1 && REM=$(grep -rln "WORKFLOW_STEPS" --include="*.ts" --include="*.tsx" app lib actions | grep -v node_modules); test -z "$REM" && ! grep -rq "LAST_STEP" --include="*.ts" --include="*.tsx" app lib actions && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes; structure tests now target LIVE_WORKFLOW_STEPS; pure-helper tests use findStep/lastStepN/projectComplete
    - grep over app/ lib/ actions/ finds ZERO WORKFLOW_STEPS or LAST_STEP references
    - No test references the deleted legacy exports
  </acceptance_criteria>
  <done>Tests target the seed-data module + DB engine; the literal is provably gone from the app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Before/after zero-regression verification of real projects</name>
  <read_first>
    - scripts/verify-live-workflow.ts (parity + join)
    - scripts/verify-workflow-engine.ts (test-graph engine regression)
  </read_first>
  <what-built>
    The full Confirmation→Sign Off tail now runs on the DB `graph='live'` engine: my-work, advancement, bypass,
    checklist/readiness launchers, timeline, approvals, the board, header switcher, forcing gate, new-project
    form, and flow diagram all read steps from getLiveWorkflowSteps()/useWorkflowSteps(); the WORKFLOW_STEPS
    literal is retired. Automated proof: `npm test`, `npm run verify:workflow-engine`, and
    `npm run verify:live-workflow` (parity + parallel/join in both orders) all pass.
  </what-built>
  <how-to-verify>
    1. Run and confirm all pass: `npm test` && `npm run verify:workflow-engine` && `npm run verify:live-workflow`.
    2. Start the dev server (`npm run dev`). Sign in and open, for the REAL in-flight project at currentStep 3
       (materials_readiness, factory_pm) and the completed project at currentStep 12:
       - Admin timeline (`/admin/timeline`): current-step label, "X/11" count, delivered badge, and Act link
         match what they showed before the cutover.
       - The projects board (site-pm/factory-pm `/projects`): done/current/locked steps, "NEEDS YOU", countdowns
         identical; open the steps modal and confirm the delivery cluster (steps 4-6) still lists in order.
       - The forcing gate + header switcher: for a user whose turn it is, the same step/label/role/Act href appears.
       - About page flow diagram: the 11 steps render in the same order with the same role colors/blurbs.
       - Create a NEW project via `/admin/projects/new`: per-step deadline inputs for steps 2-11 render and
         ordering validation still fires; the created project starts at currentStep 2 and walks 2→3→… identically.
    3. Confirm NO project's currentStep changed (the in-flight ones are still at 3 and 5).
  </how-to-verify>
  <resume-signal>Type "approved" if all pre/post views and the three verification commands match, or describe any drift.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retired literal → DB source of truth | After removal, any missed caller would fail to compile (TS) — the compiler is the enforcement boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Tampering | removal of WORKFLOW_STEPS/LAST_STEP while a caller still depends on it | mitigate | tsc + grep gates in Task 1/2 prove zero remaining references before/after removal; waves 2-3 migrated every caller first |
| T-17-14 | Repudiation | in-flight project currentStep unintentionally mutated | mitigate | No task writes projects.currentStep; Task 3 human check confirms steps 3/5/12 unchanged |
| T-17-15 | Tampering | seed re-run altering live data | mitigate | Seed is idempotent, scoped to graph='live' definitions/edges only; never touches projects rows |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm test`, `npm run verify:workflow-engine`, `npm run verify:live-workflow` all green.
- grep proves zero WORKFLOW_STEPS/LAST_STEP references in app/ lib/ actions/.
- Human before/after check approves identical rendering for real projects; no currentStep mutated.
</verification>

<success_criteria>
- WF-06 satisfied: the hardcoded array is retired, the DB graph is the sole source of truth read via getLiveWorkflowSteps()/useWorkflowSteps(), the delivery parallel/join is native and verified, and pre-/post-migration projects behave byte-identically (human-approved).
</success_criteria>

<output>
Create `.planning/phases/17-confirmation-sign-off-migration/17-06-SUMMARY.md` when done.
</output>
