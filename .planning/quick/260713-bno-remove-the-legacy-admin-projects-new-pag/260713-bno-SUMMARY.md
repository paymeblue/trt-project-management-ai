---
phase: quick-260713-bno
plan: 01
subsystem: ui
tags: [nextjs, server-actions, workflow, drizzle]

# Dependency graph
requires:
  - phase: Ad hoc (2026-07-09, commit f72573d)
    provides: "customer_care role + /customer-care/projects/new intake page (createProjectIntentAction), the v2.0 intent-first replacement flow"
provides:
  - "Removal of the legacy Operations/Super Admin direct-create route that bypassed Customer Care's intent step"
affects: [admin-dashboard, sidebar-nav, actions/projects]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - actions/projects.ts
    - app/(app)/admin/dashboard/page.tsx
    - app/_components/sidebar-nav.tsx

key-decisions:
  - "Deleted app/(app)/admin/projects/new/ (page.tsx + new-project-form.tsx) outright rather than redirecting it, per plan objective — no replacement UI needed since Operations discovers new projects via My Work/dashboard once Customer Care creates the intent"

requirements-completed: [STG-01]

# Metrics
duration: 5min
completed: 2026-07-13
---

# Quick Task 260713-bno: Remove Legacy Admin Projects/New Page Summary

**Deleted the legacy `/admin/projects/new` direct-create route, its form, and the dead `createProjectAction` server action (plus its exclusive helpers), enforcing the v2.0 intent-first intake flow.**

## Performance

- **Duration:** 5 min (commit-to-commit)
- **Tasks:** 2
- **Files modified:** 5 (2 deleted, 3 edited)

## Accomplishments
- Removed `app/(app)/admin/projects/new/page.tsx` and `new-project-form.tsx` — Operations/Super Admin can no longer bypass Customer Care's project-intent step
- Pruned the now-orphaned `createProjectAction`, `CreateProjectState`, `FIVE_DAY_MAX_STEP_NS`, `FIVE_DAYS_MS`, and `checkFiveDayCap` from `actions/projects.ts`, while retaining `createProjectIntentAction`, `triggerEntryAutoAssign`, and every other action/import still in use
- Removed the "New Project" tile from the admin dashboard `TILES` array
- Removed the `/admin/projects/new` item from the super_admin sidebar's Projects group (the `customer_care` group's own `/customer-care/projects/new` link was left untouched)

## Task Commits

1. **Task 1: Reference audit, then delete the legacy route and prune the dead server action** - `842b32d` (feat)
2. **Task 2: Remove the dashboard tile and sidebar link, then verify green** - `3bfc000` (feat)

## Files Created/Modified
- `app/(app)/admin/projects/new/page.tsx` - Deleted (legacy direct-create route)
- `app/(app)/admin/projects/new/new-project-form.tsx` - Deleted (legacy form)
- `actions/projects.ts` - Removed `createProjectAction` and its exclusive helpers; retained all other exports
- `app/(app)/admin/dashboard/page.tsx` - Removed the "New Project" tile
- `app/_components/sidebar-nav.tsx` - Removed the super_admin Projects group's "New Project" item

## Decisions Made
None beyond the plan — followed as specified.

## Deviations from Plan

None — plan executed exactly as written. A repo-wide grep audit (Task 1) confirmed the only application-code references to `/admin/projects/new`, `createProjectAction`, `NewProjectForm`, `new-project-form`, and `CreateProjectState` were the five files already listed in the plan's `files_modified`.

One non-code artifact needed clearing: `npx tsc --noEmit` initially failed on stale generated route types under `.next/` (gitignored build cache, pointing at the just-deleted page). This isn't an application-code deviation — removed the `.next/` directory (safe, regenerated by `npm run build`) and re-ran; all three verification commands then passed clean.

## Issues Encountered
None beyond the stale `.next/` type-cache noted above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Legacy direct-create flow fully retired; `/customer-care/projects/new` (createProjectIntentAction) is now the sole project-creation entry point, matching v2.0 STG-01
- `tsc --noEmit`, `eslint`, and `next build` all pass clean
- No blockers for Phase 20 (Payment & Timeline Gating)

---
*Phase: quick-260713-bno*
*Completed: 2026-07-13*
