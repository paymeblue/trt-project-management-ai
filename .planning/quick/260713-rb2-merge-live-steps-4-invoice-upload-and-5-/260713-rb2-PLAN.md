---
phase: quick-260713-rb2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-merge-invoice-upload-timeline.ts
  - db/workflow-live-steps.ts
  - db/seed-workflow-graph.ts
  - scripts/verify-live-workflow.ts
  - actions/projects.ts
  - lib/workflow.ts
  - app/(app)/workflow/step/page.tsx
  - app/_components/workflow-kinds/yes-no-upload-step.tsx
  - app/(app)/admin/invoice-timeline/invoice-timeline-form.tsx
  - app/(app)/admin/invoice-timeline/page.tsx
  - app/_components/pending-step-gate.tsx
  - app/_components/trt-flow-diagram.tsx
autonomous: true
requirements: [RB2-01, RB2-02, RB2-03, RB2-04]
must_haves:
  truths:
    - "The live workflow graph has exactly 22 steps (was 23), a single linear chain new_project -> ... -> sign_off."
    - "There is ONE Operations-owned step at orderIndex 4 (stepKey invoice_upload) that both uploads the invoice AND sets the delivery timeline, completing once."
    - "The merged step is actionable by an Operations-role user OR a super_admin (no head_of_operations position requirement)."
    - "Opening the merged step shows part 1 of 2 (invoice upload) until the upload is recorded, then part 2 of 2 (delivery timeline) whose submit completes the step and advances the project straight to Design Initiation."
    - "npm run verify:live-workflow passes against the new 22-step graph."
    - "No live code path references stepKey 'invoice_timeline' or the '/admin/invoice-timeline' route; nothing dangles."
    - "Step 13 (project_review_authorisation) flow-diagram description names both the routing role (Operations) and the acting title (Chief Production Officer), with its authorization gate unchanged."
  artifacts:
    - path: "scripts/migrate-merge-invoice-upload-timeline.ts"
      provides: "Idempotent live-DB migration collapsing invoice_timeline into invoice_upload and compacting orderIndex 6..23 -> 5..22"
    - path: "app/(app)/workflow/step/page.tsx"
      provides: "2-part wizard rendering for the merged step (part 1 upload -> part 2 timeline)"
    - path: "db/workflow-live-steps.ts"
      provides: "22-step canonical LIVE_WORKFLOW_STEPS matching the new live graph"
  key_links:
    - from: "app/(app)/workflow/step/page.tsx"
      to: "workflow_step_states.fulfilledKinds"
      via: "DB read to decide part 1 vs part 2"
    - from: "actions/projects.ts setInvoiceTimelineAction"
      to: "lib/workflow-graph.ts completeGraphStep"
      via: "single completion of the merged step"
---

<objective>
Merge live workflow steps 4 (Invoice upload — currently mis-assigned to customer_care) and 5 (Set Delivery Timeline — operations/head_of_operations) into ONE Operations-owned step at orderIndex 4, presented as a two-part wizard (part 1 = upload invoice; part 2 = set delivery date + per-step deadlines) that completes ONCE and advances the project straight to Design Initiation. Also fix step 13's flow-diagram copy so its description names both the routing role and the acting title (copy-only; authorization unchanged).

Purpose: The invoice + timeline are one operational act by Operations, not two hops owned by two roles. This corrects a wrong role assignment (customer_care -> operations) and removes a redundant step.
Output: A 22-step live graph, a reworked merged-step UX, an idempotent migration, and updated seed/parity/copy so `npm run verify:live-workflow` and the full build stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Working root (resolve every path against this): `/Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm`

IMPORTANT — read every file FRESH from disk before editing it. Another executor is concurrently committing to `main`; line numbers are intentionally omitted. Do NOT trust any remembered offsets. Never touch `app/_components/workflow-configurator-editor.tsx` (concurrent work).

@.planning/STATE.md

Key files to re-read before editing:
@db/workflow-live-steps.ts
@db/seed-workflow-graph.ts
@scripts/verify-live-workflow.ts
@scripts/migrate-insert-invoice-timeline-step.ts
@lib/workflow-graph.ts
@lib/workflow.ts
@actions/projects.ts
@actions/workflow-graph.ts
@app/(app)/workflow/step/page.tsx
@app/(app)/admin/invoice-timeline/page.tsx
@app/(app)/admin/invoice-timeline/invoice-timeline-form.tsx
@app/_components/workflow-kinds/yes-no-upload-step.tsx
@app/_components/trt-flow-diagram.tsx
@app/_components/pending-step-gate.tsx

<engine_facts>
Verified while planning (re-verify by reading, do not assume):
- `completeGraphStep` (lib/workflow-graph.ts) gates completion only on kinds in `STATE_GATED_KINDS = ['yes_no_upload','approval','assignment']`. `timeline_setting` is NOT state-gated, so it never blocks completion — it only affects which sub-forms `/workflow/step` renders via `stepRequiredKinds(step)` = `[kind, ...additionalKinds]`.
- `workflow_step_states.fulfilledKinds` (text[] column `fulfilled_kinds`) records which kinds have been recorded for a project+step. `submitYesNoUpload` appends `'yes_no_upload'`.
- `/workflow/step` already renders one sub-form per required kind; `YesNoUploadStep.submit()` currently AUTO-calls `completeStepAction` after recording — that auto-complete must NOT fire for the merged step's part 1 (it would complete before the timeline is set).
- Role gate: `canRoleActOnStep('operations', role)` == `isAdminRole(role)` == operations OR super_admin. `requiredPosition` is strict-equality for ALL roles (including super_admin) in both `authorizeStep` (actions/workflow-graph.ts) and the `/workflow/step` page — so a non-`head_of_operations` super_admin would be blocked by `requiredPosition='head_of_operations'`.
- `WorkflowStepsProvider` is mounted in `app/(app)/layout.tsx`, so `useWorkflowSteps()` is available on `/workflow/step`.
- `advanceProjectStep` (actions/workflow.ts) is still used elsewhere — do NOT delete it; only stop calling it from `setInvoiceTimelineAction`.
- The parity check (verify-live-workflow.ts) compares ONLY `n/key/label/role/kind/slug` — `additionalKinds`/`requiredPosition` are invisible to it.
- `db/schema.ts`: `workflowStepDefinitions.additionalKinds` = `fulfillment_kind[]` (`additional_kinds`); `workflowStepStates.fulfilledKinds` = `text[]` (`fulfilled_kinds`).
- No component links to `/admin/invoice-timeline` (grep-confirmed: only `pending-step-gate.tsx` names the path string, and `stepHref`/`graphStepHref` for `timeline_setting`).
</engine_facts>

<decisions>
DECISION D-01 (requiredPosition): The merged step is `requiredPosition = null` (role=operations only). Rationale: requirement 1 explicitly wants "the head of operations OR the operations admin who is a super admin" to act, and the position gate is strict-equality for every role — `head_of_operations` would block a super_admin whose position isn't that exact slug. `role=operations` already admits operations-role users AND super_admins via `isAdminRole`. This is the recommended choice and matches the owner's intent.

DECISION D-02 (merge mechanism): Survivor is `invoice_upload` (orderIndex 4). It becomes `role='operations'`, `fulfillmentKind='yes_no_upload'`, `additionalKinds=['timeline_setting']`, `requiredPosition=null`. `additionalKinds=['timeline_setting']` is the trigger that makes `/workflow/step` render the 2-part wizard — it uses the engine's EXISTING multi-kind machinery (no new engine machinery invented). `invoice_timeline` is deleted. Completion is gated by the engine on `yes_no_upload` only; the timeline form is the SOLE caller of `completeGraphStep` for this step, so both parts are required in practice via UX sequencing (part 2 only appears once part 1's `yes_no_upload` is fulfilled).

DECISION D-03 (merged step label): Use exactly `Invoice & Delivery Timeline` everywhere (migration, seed, LIVE_WORKFLOW_STEPS) — these MUST be byte-identical or parity fails.

DECISION D-04 (part-2 route): Fold the timeline form into `/workflow/step` (single page). Remove the `/admin/invoice-timeline` PAGE route (`page.tsx`); KEEP and reuse `invoice-timeline-form.tsx` by importing it into `/workflow/step`. No component links to the removed route, so nothing dangles.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape the live graph — idempotent migration + seed + canonical array + parity comment</name>
  <files>scripts/migrate-merge-invoice-upload-timeline.ts, db/workflow-live-steps.ts, db/seed-workflow-graph.ts, scripts/verify-live-workflow.ts</files>
  <action>
Re-read `scripts/migrate-insert-invoice-timeline-step.ts` and `db/seed-workflow-graph.ts` fresh for the exact by-key edge-rewiring + dotenv/neon/drizzle style, then:

(a) CREATE `scripts/migrate-merge-invoice-upload-timeline.ts` — an idempotent, `npx tsx`-runnable migration against graph='live'. Mirror the existing migration scripts' setup (dotenv `.env.local`, `neon`, `drizzle`, `import * as schema from '../db/schema'`). It does NOT import from `lib/`, so it needs NO server-only shim (that shim is only required when a script imports the engine — this one talks to the DB directly).
  Steps, in order:
  1. SAFETY: `select count(*)` from `projects`. If count !== 0, print a refusal and `process.exit(1)` — this migration assumes zero in-flight projects (no currentStep/deadline/completion remap). Do NOT proceed.
  2. IDEMPOTENCY: look up `invoice_timeline` step def in graph='live'. If it does NOT exist, print "already merged — nothing to do" and return cleanly (exit 0).
  3. Load by key (graph='live'): `invoice_upload`, `invoice_timeline`, `design_initiation`. If any is missing, refuse and exit 1.
  4. Update the survivor `invoice_upload`: set `label='Invoice & Delivery Timeline'` (D-03), `role='operations'`, `fulfillmentKind='yes_no_upload'`, `additionalKinds=['timeline_setting']`, `requiredPosition=null`, `updatedAt=now`. Keep its orderIndex = 4.
  5. Rewire edges by id: delete the edge `invoice_upload -> invoice_timeline` and the edge `invoice_timeline -> design_initiation` (both graph='live'); then insert `invoice_upload -> design_initiation` with `.onConflictDoNothing()`. Assert you deleted exactly the two expected edges (refuse + exit if not, following the precedent's exact-count guard).
  6. Delete the `invoice_timeline` step def by id. Check whether `workflow_step_edges` FK-references `workflow_step_definitions`; if so, delete any remaining edges referencing invoice_timeline's id BEFORE deleting the def. With zero projects there are no states/completions to worry about.
  7. COMPACT orderIndex: for every graph='live' step with `orderIndex > 4` (design_initiation onward, formerly 6..23), decrement `orderIndex` by 1 so the graph becomes contiguous 1..22 with the merged step at 4. Process in ASCENDING orderIndex order so no two rows collide during the update (mirror the precedent's ordered-shift approach). Log each shift.
  8. Print a final summary — step count should now be 22.

(b) EDIT `db/workflow-live-steps.ts` (`LIVE_WORKFLOW_STEPS`):
  - Remove the `invoice_timeline` entry entirely.
  - Update the merged step (n:4, key:'invoice_upload') to `label: 'Invoice & Delivery Timeline'`, `role: 'operations'`, `kind: 'yes_no_upload'`. (The `WorkflowStep` type carries no additionalKinds/requiredPosition — those live only in the DB/seed; leave them out here.)
  - Renumber `n` for every step from the former n:6 (design_initiation) through n:23 (sign_off) DOWN by 1, producing a contiguous 1..22 (design_initiation=5, ..., sign_off=22).
  - Add a new dated header-comment block (matching the file's existing history style) documenting the 23->22 merge and pointing to `scripts/migrate-merge-invoice-upload-timeline.ts`.

(c) EDIT `db/seed-workflow-graph.ts` so a fresh bootstrap reproduces the identical 22-step shape:
  - The seed inserts from `LIVE_WORKFLOW_STEPS`, so the merged step's role/kind/label already flow through. ADD a small per-key config (mirroring the existing `ASSIGNMENT_STEP_CONFIG` pattern) that sets `additionalKinds: ['timeline_setting']` on `invoice_upload`, and wire it into the `.values({ ... additionalKinds: ... })` insert. Do NOT set requiredPosition for it (null is correct).
  - Update the `EDGES` array: remove `['invoice_upload','invoice_timeline']` and `['invoice_timeline','design_initiation']`; add `['invoice_upload','design_initiation']`. Keep `['brief_taking','invoice_upload']`.
  - Update the "23 steps" prose in the header/logs to 22.

(d) EDIT `scripts/verify-live-workflow.ts`: update the header comment's "for all 23 steps" to 22. The count assertion is dynamic (`LIVE_WORKFLOW_STEPS.length`), so no logic change is needed.

(e) RUN the migration against the live DB: `npx tsx scripts/migrate-merge-invoice-upload-timeline.ts`. Confirm it reports 22 steps. Run it a SECOND time and confirm it reports "already merged — nothing to do" (idempotency proof).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsx scripts/migrate-merge-invoice-upload-timeline.ts && npx tsx scripts/migrate-merge-invoice-upload-timeline.ts && npm run verify:live-workflow</automated>
  </verify>
  <done>Migration runs idempotently (2nd run is a no-op); the live graph is 22 steps with the merged Operations step at orderIndex 4; `npm run verify:live-workflow` prints RESULT: PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Rework server action + routing + wizard UX for the merged step</name>
  <files>actions/projects.ts, lib/workflow.ts, app/(app)/workflow/step/page.tsx, app/_components/workflow-kinds/yes-no-upload-step.tsx, app/(app)/admin/invoice-timeline/invoice-timeline-form.tsx, app/(app)/admin/invoice-timeline/page.tsx, app/_components/pending-step-gate.tsx</files>
  <action>
Re-read each file fresh before editing.

(a) `app/_components/workflow-kinds/yes-no-upload-step.tsx` — add an optional prop `completeOnSubmit?: boolean` (default `true`, preserving all existing callers). When `completeOnSubmit === false`, `submit()` records via `submitYesNoUploadAction` and then, on success, calls `router.refresh()` (to reveal part 2) INSTEAD of calling `completeStepAction`. Do NOT render the standalone "Complete step" button when `completeOnSubmit === false` (part 1 must never self-complete the merged step). Keep default behavior byte-for-byte for existing callers.

(b) `app/(app)/workflow/step/page.tsx` — add wizard mode for the merged step:
  - Compute `requiredKinds = stepRequiredKinds(step)`. If `requiredKinds` includes `'timeline_setting'`, render the 2-part wizard instead of the generic stacked multi-kind view:
    * Read `workflow_step_states.fulfilledKinds` for `(projectId, step.id)` (import `workflowStepStates`; query by projectId + stepDefId).
    * If `fulfilledKinds` does NOT include `'yes_no_upload'` → render Part 1: `<YesNoUploadStep projectId stepDefId completeOnSubmit={false} />` (no `redirectTo`), under a heading "Part 1 of 2 — Upload the invoice".
    * Else → render Part 2: `<InvoiceTimelineForm projectId={projectId} />` (import from `@/app/(app)/admin/invoice-timeline/invoice-timeline-form`), under a heading "Part 2 of 2 — Set the delivery timeline".
  - Leave the existing generic rendering untouched for all other steps.
  - Keep the existing role/requiredPosition/assignee gates as-is (they already allow operations/super_admin when requiredPosition is null).

(c) `app/(app)/admin/invoice-timeline/invoice-timeline-form.tsx` — re-key from the deleted `invoice_timeline` to the merged `invoice_upload`: change the `steps.find((s) => s.key === 'invoice_timeline')` lookup to `'invoice_upload'`, so "remaining steps" = every step with `n >` the merged step's n (design_initiation onward). No other behavior change. It keeps using `setInvoiceTimelineAction` and redirects to `/admin/timeline` on success.

(d) `actions/projects.ts` `setInvoiceTimelineAction` — rework to complete the MERGED step once, per D-01/D-02:
  - Relax the gate: keep `isAdminRole(role)` (operations OR super_admin) but REMOVE the `Positions.HeadOfOperations` position check (requiredPosition is null now). Remove the `Positions` import if nothing else in the file uses it.
  - Resolve the merged step by key `'invoice_upload'` (not `'invoice_timeline'`). Validate `proj.currentStep === mergedStep.n`.
  - Keep the delivery-date + per-step deadline parsing/validation, but filter "steps after" using the merged step's n.
  - Replace the `advanceProjectStep(...)` call with a single completion of the merged step via the graph engine: `completeGraphStep({ projectId, stepDefId: mergedStep.stepDefId, actorId: userId })` (import `completeGraphStep` from `@/lib/workflow-graph`; `getLiveWorkflowSteps()` already yields `stepDefId`). Wrap it so a thrown `'step-not-fulfilled'` (part 1 not done) surfaces a clear error. This advances `projects.currentStep` via `syncProjectCurrentStepAfterCompletion` — ONE completion, project moves 4 -> Design Initiation. Remove the `advanceProjectStep` import from this file (it stays defined in actions/workflow.ts for its other callers).
  - Keep the final `revalidatePath('/admin/timeline')` + `redirect('/admin/timeline')`.

(e) `lib/workflow.ts` — remove the two `if (step.kind === 'timeline_setting') return '/admin/invoice-timeline...'` branches (one in `stepHref`, one in `graphStepHref`). They pointed at the route deleted in (f), and no live step has `timeline_setting` as its PRIMARY kind anymore (it appears only as an additionalKind, handled inside `/workflow/step`). The merged step's primary kind is `yes_no_upload`, so it already routes to `/workflow/step`.

(f) DELETE `app/(app)/admin/invoice-timeline/page.tsx` (the route). Keep `invoice-timeline-form.tsx` (now imported by `/workflow/step`).

(g) `app/_components/pending-step-gate.tsx` — remove the `pathname.startsWith('/admin/invoice-timeline')` line from `isStepRoute` (route no longer exists; `/workflow/step` is already covered).

After edits, grep the whole repo for `invoice_timeline`, `invoice-timeline`, and `setInvoiceTimelineAction` and account for every hit — the only surviving references should be: historical prose in `db/workflow-live-steps.ts` header comments, old `scripts/migrate-*.ts` history files, and `invoice-timeline-form.tsx` + its `setInvoiceTimelineAction` usage (now keyed to invoice_upload). There must be NO remaining live `invoice_timeline` STEP lookup or `/admin/invoice-timeline` link/route.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && test -z "$(grep -rn 'invoice_timeline' app lib actions | grep -v 'workflow-live-steps.ts')" && echo NO-LIVE-HITS</automated>
  </verify>
  <done>Type-check and lint pass; the merged step renders part 1 (upload) then part 2 (timeline) and completes once via completeGraphStep; no live code references the removed invoice_timeline step or /admin/invoice-timeline route.</done>
</task>

<task type="auto">
  <name>Task 3: Flow-diagram copy — merged step 4, remove invoice_timeline, fix step 13 (copy-only)</name>
  <files>app/_components/trt-flow-diagram.tsx</files>
  <action>
Re-read `app/_components/trt-flow-diagram.tsx` fresh. In the `DETAIL` map:
  - REMOVE the `invoice_timeline:` entry entirely (that step no longer exists).
  - REPLACE the `invoice_upload:` blurb so it names the correct owner and the two-part nature, e.g.: "Operations (the Head of Operations or an Operations super-admin) uploads the invoice, then sets the delivery date and a deadline for every remaining step — one step done in two parts." (RB2-01/RB2-02).
  - FIX the `project_review_authorisation:` blurb (requirement 4, RB2-04, COPY ONLY — do NOT change any role/position/gate anywhere): make it name both the routing role and the acting title so the "Operations" badge and the text stop contradicting, e.g.: "Operations routes it to the Chief Production Officer, who reviews the drawing and confirms the review." Leave the step's role=operations + requiredPosition=chief_production_officer gate completely untouched (this file has no gating logic — it is display copy).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && test "$(grep -c 'invoice_timeline' app/_components/trt-flow-diagram.tsx)" = "0" && grep -q 'Chief Production Officer' app/_components/trt-flow-diagram.tsx && echo OK</automated>
  </verify>
  <done>The invoice_timeline DETAIL entry is gone; the invoice_upload blurb names Operations as owner and the two-part nature; step 13's blurb names both Operations (routing) and Chief Production Officer (acting) with no gating change.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite verification + dangling-reference sweep</name>
  <files>(no source edits — verification gate; fix any failure in the owning file above)</files>
  <action>
Run the full verification suite from the repo root and confirm each passes. If any fails, fix it in the appropriate file from Tasks 1–3 (never in `app/_components/workflow-configurator-editor.tsx`) and re-run.

1. `npx tsc --noEmit` — no type errors.
2. `npm run lint` — clean.
3. `npm test` — all vitest suites pass.
4. `npm run verify:live-workflow` — RESULT: PASS against the 22-step graph.
5. `npm run build` — production build succeeds (the deleted `/admin/invoice-timeline` route must not break the build; the `/workflow/step` wizard must compile).

Then the dangling-reference sweep — run and manually account for EVERY hit:
  `grep -rn "invoice_timeline\|invoice-timeline\|setInvoiceTimelineAction" app lib actions db scripts`
Acceptable remaining hits ONLY: (a) historical prose in `db/workflow-live-steps.ts` header comments; (b) old `scripts/migrate-*.ts` history files; (c) `invoice-timeline-form.tsx` and its `setInvoiceTimelineAction` import/usage (now keyed to invoice_upload). Any OTHER live hit (a step lookup by 'invoice_timeline', a link/route to '/admin/invoice-timeline', or the deleted page.tsx) is a defect to fix.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && npm test && npm run verify:live-workflow && npm run build</automated>
  </verify>
  <done>tsc, lint, vitest, verify:live-workflow, and next build all pass; the dangling-reference sweep shows only the acceptable historical/reused hits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action (submitYesNoUpload / setInvoiceTimeline / completeStep) | Untrusted actor identity + form input crosses here; role/position/step-state gates enforce authorization server-side |
| migration script → live Neon DB | Privileged one-shot mutation of production step definitions/edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-rb2-01 | Elevation of Privilege | Merged step part 1 (`/workflow/step`) + `submitYesNoUploadAction` | mitigate | `authorizeStep` + page-level `canRoleActOnStep('operations')` (=isAdminRole) gate acting to operations/super_admin; requiredPosition=null is the deliberate D-01 decision, not an omission |
| T-rb2-02 | Elevation of Privilege | `setInvoiceTimelineAction` (part 2 completion) | mitigate | Keep `isAdminRole(role)` server-side gate; complete via `completeGraphStep`, which throws `step-not-fulfilled` if the invoice upload (part 1) was never recorded — a spoofed direct part-2 submit cannot skip part 1 |
| T-rb2-03 | Tampering | Migration script vs. in-flight project data | mitigate | Script asserts `projects count == 0` and aborts otherwise; idempotency guard prevents double-application; edge deletes are exact-count-checked before proceeding |
| T-rb2-04 | Repudiation | Merged step completion | accept | Existing `projectStepCompletions.completedBy` records the actor id, unchanged by this work |
| T-rb2-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed by this plan; if any task adds a dependency, stop and run the package legitimacy gate |
</threat_model>

<verification>
- `npm run verify:live-workflow` PASS (22-step parity + dualRoles confirmation both orders).
- `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build` all green.
- Migration is idempotent (2nd run = no-op) and refuses to run when projects exist.
- Grep sweep for invoice_timeline / invoice-timeline / setInvoiceTimelineAction shows only acceptable historical/reused hits.
</verification>

<success_criteria>
- Live graph = 22 steps, single linear chain, merged Operations step at orderIndex 4.
- Merged step is actionable by operations OR super_admin (requiredPosition=null) and renders as a 2-part wizard completing once (upload → timeline → Design Initiation).
- Step 13 flow-diagram copy names both routing role and acting title; its authorization gate is unchanged.
- No dangling references; `workflow-configurator-editor.tsx` untouched.
</success_criteria>

<output>
Create `.planning/quick/260713-rb2-merge-live-steps-4-invoice-upload-and-5-/260713-rb2-SUMMARY.md` when done, recording: final step count, the merged step's exact attributes, the requiredPosition=null decision, files changed, and each verification command's result.
</output>
