---
phase: quick-260711-cleanup-deferred-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/seed-workflow-graph.ts
  - db/schema.ts
autonomous: true
requirements: [DEFERRED-01, DEFERRED-02]
must_haves:
  truths:
    - "db/seed-workflow-graph.ts references only the 23 current live step keys — no retired keys (payment_confirmation, design_meeting, delivery_readiness, delivery_project, project_check_report)"
    - "seed script's EDGES form a valid chain over the current 23-step live graph and would run without an undefined-key crash if executed against a fresh DB"
    - "a second `npm run db:push` immediately after the fix emits zero statements touching message_reactions (no DROP/ADD of the composite unique constraint)"
    - "`npx tsc --noEmit`, `npm run lint`, `npm test`, and `npm run verify:live-workflow` all pass after both fixes"
  artifacts:
    - path: "db/seed-workflow-graph.ts"
      provides: "Fresh-DB structural seed for graph='live' matching the current 23-step canonical graph"
      contains: "delivery_project_check"
    - path: "db/schema.ts"
      provides: "messageReactions table with an idempotent composite unique constraint"
      contains: "message_reactions"
  key_links:
    - from: "db/seed-workflow-graph.ts"
      to: "db/workflow-live-steps.ts"
      via: "LIVE_WORKFLOW_STEPS import + EDGES key list must match the 23 step keys"
      pattern: "LIVE_WORKFLOW_STEPS"
    - from: "db/schema.ts messageReactions"
      to: "drizzle-kit push introspection"
      via: "composite unique constraint name/column-order stability"
      pattern: "unique\\(.*\\)\\.on\\(t\\.messageId"
---

<objective>
Close two small deferred items logged from quick-260711-gs6:

1. `db/seed-workflow-graph.ts` still hardcodes retired step keys in its EDGES list
   (`payment_confirmation`, `design_meeting`, `delivery_readiness`, `delivery_project`,
   `project_check_report`). It is a fresh-DB seed tool not exercised by tsc/lint/test, so it
   compiles but would crash at runtime (`idByKey.get(...)` returns undefined) if ever re-run.
   Bring its EDGES + comments in line with the CURRENT 23-step live graph.

2. `message_reactions` composite unique constraint churns on every `npm run db:push`
   (DROP + ADD, same name, zero schema.ts diff), making db:push non-idempotent at the
   SQL-statement level. Find the root cause and fix it so a second db:push emits zero
   message_reactions statements.

Purpose: Keep the DB tooling honest — the seed script should be re-runnable against a fresh
DB without crashing, and db:push should be truly idempotent so real churn is never masked by
cosmetic noise.

Output: Corrected `db/seed-workflow-graph.ts` and `db/schema.ts`; both verified green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260711-gs6-finish-phase-22e-dualroles-receiverrole/deferred-items.md
@db/workflow-live-steps.ts
@db/seed-workflow-graph.ts
@scripts/verify-live-workflow.ts

<interfaces>
Current canonical 23-step live graph (db/workflow-live-steps.ts, in order):
n1  new_project
n2  assign_designer_brief         (assignment)
n3  brief_taking
n4  invoice_upload
n5  invoice_timeline
n6  design_initiation             (assignment)
n7  kickoff_meeting
n8  design_stage
n9  ops_design_confirmation
n10 confirmation_correction
n11 internal_approval
n12 send_for_production           (approval)
n13 project_review_authorisation
n14 production_process
n15 confirmation
n16 factory_manager_readiness
n17 materials_readiness           (readiness, dualRoles=[factory_pm, site_pm] on live DB row)
n18 delivery_project_check        (was delivery_project + project_check_report, merged Phase 22d)
n19 approval_installation
n20 installation_readiness
n21 sorting
n22 close_out
n23 sign_off

RETIRED keys that must NOT appear anywhere in the seed script: payment_confirmation
(removed 22c), design_meeting (removed 22d), delivery_readiness (merged into
materials_readiness 22e), delivery_project + project_check_report (merged into
delivery_project_check 22d).

Per STATE.md (Phase 22e note): "the graph's only remaining parallel branch/join collapsed
to linear." The live graph is now a fully linear chain — no fan-out/join edges remain.

messageReactions table (db/schema.ts:386-392):
  pgTable('message_reactions', { id, messageId (FK->messages, cascade), userId (FK->users),
  emoji, createdAt }, (t) => [unique().on(t.messageId, t.userId, t.emoji)])
Constraint auto-named message_reactions_message_id_user_id_emoji_unique (46 chars — under the
63-char Postgres limit, so this is NOT the truncation bug fixed for FKs in Phase 16/gs6).

Non-churning composite uniques for comparison use the OBJECT form with an explicit key:
  workflowStepDefinitions: `{ graphStepKeyUq: unique().on(t.graph, t.stepKey) }` (schema.ts:128)
  projectStepCompletions:  `{ projectStepUq: unique().on(t.projectId, t.stepN) }` (schema.ts:205)

Drizzle stack: drizzle-orm ^0.45.2, drizzle-kit ^0.31.10, @neondatabase/serverless ^1.1.0,
dialect postgresql (drizzle.config.ts). No `drizzle/` migration files — the project uses
`drizzle-kit push` directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix db/seed-workflow-graph.ts to match the current 23-step live graph</name>
  <files>db/seed-workflow-graph.ts</files>
  <action>
    First, read-only cross-check the ACTUAL live graph shape before editing — do NOT run the
    seed script (it deletes + reseeds graph='live' and would destroy the live graph). Run a
    read-only query against the live DB to confirm the current edge topology is fully linear:
    write a throwaway inline `tsx -e` (or a temporary scratch script under the scratchpad, not
    committed) that connects via DATABASE_URL from .env.local, selects workflow_step_definitions
    (stepKey, orderIndex) and workflow_step_edges (fromStepId, toStepId) for graph='live', joins
    edges to definition keys, and prints the from->to key pairs ordered by orderIndex. Confirm it
    matches a single linear chain new_project -> ... -> sign_off across all 23 keys with no
    fan-out/join. If the live DB shows any non-linear edge, replicate THAT exact topology instead
    of assuming linear.

    Then rewrite the EDGES array in db/seed-workflow-graph.ts to the confirmed current topology
    (expected: the 22 sequential pairs n->n+1 over the 23 keys listed in <interfaces>). Remove
    every retired key (payment_confirmation, design_meeting, delivery_readiness, delivery_project,
    project_check_report). Ensure every key referenced in EDGES exists in LIVE_WORKFLOW_STEPS so
    `idByKey.get(fromKey)!` / `idByKey.get(toKey)!` never resolve undefined.

    Update the now-stale prose: the file header (references "11 live steps" and the old
    Delivery Readiness / Delivery Project / Project Check Report parallel-join), the "18 step
    definitions" comment (line ~57), the EDGES comment (lines ~80-84 describing the fan-out), and
    the edgeCount log line (~118 mentioning "fan-out ... and join ->project_check_report"). Make
    them describe the current 23-step linear graph. Keep ASSIGNMENT_STEP_CONFIG as-is
    (assign_designer_brief + design_initiation are still the two assignment-kind steps).

    Do NOT run `npm run db:seed-workflow-graph` at any point — verification is source-read +
    tsc/lint only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('db/seed-workflow-graph.ts','utf8'); const retired=['payment_confirmation','design_meeting','delivery_readiness','delivery_project','project_check_report']; const hit=retired.filter(k=>s.includes(k)); if(hit.length){console.error('RETIRED KEYS STILL PRESENT:',hit);process.exit(1)} if(!s.includes('delivery_project_check')){console.error('missing current key delivery_project_check');process.exit(1)} console.log('OK')"</automated>
  </verify>
  <done>
    EDGES contains only current 23-step keys, forms the confirmed live topology, no retired key
    appears anywhere in the file, delivery_project_check is present, tsc + lint pass, and the seed
    was NOT run against the live DB.
  </done>
</task>

<task type="auto">
  <name>Task 2: Diagnose and fix the message_reactions unique-constraint db:push churn</name>
  <files>db/schema.ts</files>
  <action>
    Reproduce and diagnose first. Run `npx drizzle-kit push --verbose` and capture the exact
    statements drizzle-kit emits for message_reactions (expected: a DROP CONSTRAINT +
    ADD CONSTRAINT pair for message_reactions_message_id_user_id_emoji_unique with no schema.ts
    change). Compare against how the non-churning composite uniques are declared —
    workflowStepDefinitions uses object form `{ graphStepKeyUq: unique().on(...) }` (schema.ts:128)
    and projectStepCompletions uses `{ projectStepUq: unique().on(...) }` (schema.ts:205), while
    messageReactions uses the anonymous array form `[unique().on(t.messageId, t.userId, t.emoji)]`
    (schema.ts:392) with no explicit constraint name. Determine whether the churn is driven by
    (a) drizzle-kit introspecting the live constraint's column order differently than declared, or
    (b) the anonymous/auto-named constraint round-tripping unstably. Note this is NOT the 63-char
    truncation bug (name is 46 chars) already fixed for FKs.

    Apply the minimal fix that makes it idempotent — most likely giving the constraint an explicit
    stable name via `unique('<name>').on(t.messageId, t.userId, t.emoji)` matching the live DB's
    actual constraint name (query the live DB read-only for the exact current name and column
    order first, so the ADD/DROP stops firing), and/or aligning the declared column order to the
    introspected order. Prefer a fix that requires NO destructive DB migration: the resulting
    schema must be identical, only the push diff should go quiet.

    SAFETY: Running db:push here IS expected (this task must verify via push), but abort
    immediately and report if any push statement would DROP or RENAME a real column/table, or
    TRUNCATE data — the only acceptable statements are the message_reactions constraint churn
    (before the fix) and zero statements (after). Never accept data loss to silence the diff.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npx drizzle-kit push --verbose 2>&1 | tee /tmp/push1.log >/dev/null; npx drizzle-kit push --verbose 2>&1 | tee /tmp/push2.log >/dev/null; if grep -qi "message_reactions" /tmp/push2.log; then echo "FAIL: message_reactions still churns on 2nd push"; grep -i message_reactions /tmp/push2.log; exit 1; fi; echo "OK: 2nd push clean for message_reactions"</automated>
  </verify>
  <done>
    Root cause identified and documented in the SUMMARY; schema.ts fixed with a minimal,
    non-destructive change; two consecutive `drizzle-kit push` runs emit zero message_reactions
    statements on the second run; tsc + lint + test all pass; no real column/table/data was
    dropped, renamed, or truncated.
  </done>
</task>

</tasks>

<verification>
Run after BOTH tasks complete:
- `npx tsc --noEmit` — clean
- `npm run lint` — clean
- `npm test` — all pass (baseline 77 passed, 1 todo)
- `npm run db:push` twice in a row — second run emits zero non-additive statements, specifically
  zero message_reactions constraint churn (abort + report on any drop/rename/truncate of real
  data)
- `npm run verify:live-workflow` — PARITY 23/23 + both dualRoles confirmation orders still pass
- Grep confirms no retired step keys remain in db/seed-workflow-graph.ts
- Confirm db/seed-workflow-graph.ts was NEVER executed against the live DB
</verification>

<success_criteria>
- db/seed-workflow-graph.ts references only the 23 current live step keys; EDGES matches the
  confirmed live topology; would run crash-free against a fresh DB
- A second consecutive `npm run db:push` produces zero message_reactions statements
- tsc, lint, test, and verify:live-workflow all green
- No live data destroyed: seed script not run against live DB; no destructive push statements
  accepted
</success_criteria>

<output>
Create `.planning/quick/260711-cleanup-deferred-items/260711-SUMMARY.md` when done, documenting
the message_reactions churn root cause and the fix applied.
</output>
