---
phase: quick-260727-g7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/workflow-graph.ts
  - actions/workflow.ts
  - actions/checklists.ts
  - actions/readiness.ts
  - app/(app)/checklists/[slug]/page.tsx
  - app/(app)/factory-pm/readiness/page.tsx
  - tests/actions/workflow.test.ts
autonomous: true
requirements: [QT-260727-g7a]

must_haves:
  truths:
    - "A caller whose users.position differs from a legacy step's requiredPosition cannot advance that step (no completion row written, project currentStep unchanged)"
    - "A caller whose users.position matches requiredPosition advances the step exactly as before"
    - "A wrong-position caller sees a denial notice on the checklist/readiness page BEFORE filling the form, worded like the /workflow/step page"
    - "A wrong-position caller who posts the form anyway gets an explicit error and no checklist/readiness row is recorded"
    - "Steps with requiredPosition unset behave byte-identically to today (no extra DB query)"
    - "Graph-engine behavior (actions/workflow-graph.ts authorizeStep) is unchanged"
  artifacts:
    - path: "lib/workflow-graph.ts"
      provides: "stepPositionMismatch() + POSITION_MISMATCH_MESSAGE shared gate"
      contains: "stepPositionMismatch"
    - path: "actions/workflow.ts"
      provides: "position enforcement in advanceProjectStep + confirmDualRoleStepAs"
      contains: "stepPositionMismatch"
    - path: "tests/actions/workflow.test.ts"
      provides: "wrong-position reject + right-position allow coverage"
      contains: "requiredPosition"
  key_links:
    - from: "actions/workflow.ts"
      to: "lib/workflow-graph.ts stepPositionMismatch"
      via: "await before any completion insert"
      pattern: "await stepPositionMismatch"
    - from: "app/(app)/checklists/[slug]/page.tsx"
      to: "lib/workflow-graph.ts stepPositionMismatch"
      via: "workflowNotice gate chain"
      pattern: "stepPositionMismatch"
---

<objective>
Close the authorization hole where the LEGACY workflow engine (checklist / readiness / ack sole-kind steps) never consults `workflow_step_definitions.required_position`. Today any user with the step's ROLE can submit and advance a position-restricted legacy step — browser-confirmed on live step 19 `approval_installation` (`requiredPosition='operations_manager_admin'`), where an operations user holding `chief_production_officer` was allowed through.

Purpose: the Configurator lets admins set `requiredPosition` on ANY step; the graph engine honors it, the legacy engine silently ignores it. Fix must be generic, not step-19-specific.
Output: one shared server-side position gate consulted by both legacy write paths (`advanceProjectStep`, `confirmDualRoleStepAs`), plus pre-submit page + action gates so a blocked user is never stranded after recording a form that cannot advance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md

Work in `/Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm` only.

@lib/workflow-graph.ts
@actions/workflow.ts
@actions/checklists.ts
@actions/readiness.ts
@app/(app)/checklists/[slug]/page.tsx
@app/(app)/factory-pm/readiness/page.tsx
@actions/workflow-graph.ts
@tests/actions/workflow.test.ts

<interfaces>
Already present — do NOT re-derive:

`LiveWorkflowStep` (lib/workflow-graph.ts:70-81) ALREADY carries `requiredPosition?: string | null`
(mapped at line 94 in `getLiveWorkflowSteps`). No read-side extension is needed.

The reference implementation to mirror (actions/workflow-graph.ts:87-93, authorizeStep — DO NOT EDIT):
  const requiredPos = ... step.requiredPosition
  if (requiredPos) {
    const [actingUser] = await db.select({ position: users.position }).from(users).where(eq(users.id, userId)).limit(1)
    if (actingUser?.position !== requiredPos) -> reject 'position-mismatch'
  }

User-facing wording already used by app/(app)/workflow/step/page.tsx:129 and
actions/workflow-graph.ts:49 (ENGINE_ERROR_MESSAGES['position-mismatch']):
  "This step is restricted to a specific title, and your account is not set to it."

lib/workflow-graph.ts already imports `db`, `users`, `eq` — no new imports needed there.
`users.position` is free text (db/schema.ts:55); slugs live in the `positions` table.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared position gate + legacy-engine enforcement</name>
  <files>lib/workflow-graph.ts, actions/workflow.ts</files>
  <action>
In `lib/workflow-graph.ts`, add two exports near the existing assignee-gate helpers (`assigneeGatedRoles` / `getStepAssigneeGate`), so the legacy engine gets the same treatment the graph engine already has:

1. `POSITION_MISMATCH_MESSAGE` — a string constant holding exactly: "This step is restricted to a specific title, and your account is not set to it." (byte-identical to app/(app)/workflow/step/page.tsx:129 and ENGINE_ERROR_MESSAGES['position-mismatch']; deliberately duplicated rather than importing from actions/workflow-graph.ts, since that module is `'use server'` and every export there must be an async action).
2. `async function stepPositionMismatch(userId: string, step: { requiredPosition?: string | null }): Promise<boolean>` — returns `false` immediately (ZERO db queries) when `step.requiredPosition` is falsy; otherwise selects `users.position` fresh from the DB for `userId` and returns `true` when it does not strictly equal `step.requiredPosition`. Fresh-from-DB is mandatory, not session/JWT — mirror authorizeStep's stated reason (position changes post-signup via the self-service profile flow, so a JWT claim would silently under- or over-authorize).

Add a dense why-comment above the helper citing quick task 260727-g7a: the legacy engine (checklist/readiness/ack sole-kind steps) never consulted `requiredPosition`, so live step 19 `approval_installation` was actionable by any operations/super_admin account regardless of title; the graph engine (actions/workflow-graph.ts authorizeStep) already enforced it. Note the gate is generic because admins can set `requiredPosition` on any step via the Configurator.

In `actions/workflow.ts`, import `stepPositionMismatch` and `POSITION_MISMATCH_MESSAGE` from `@/lib/workflow-graph` and call the gate in BOTH write paths, positioned immediately AFTER the existing role check and BEFORE the existing assignee-gate block (fail on the cheapest/most-general gate first, and never touch a write):

- `advanceProjectStep` (after `canRoleActOnStep(...)` at line 41): `if (await stepPositionMismatch(userId, step)) return false`. Comment that a bare `false` here is intentionally consistent with every other refusal in this function, and that stranding is prevented by the pre-submit gates added in Task 2 (the caller never gets far enough to record a form).
- `confirmDualRoleStepAs` (after the `step.dualRoles` membership check at line 129): return `{ ok: false, advanced: false, message: POSITION_MISMATCH_MESSAGE }`. Comment that no live dual-role step sets `requiredPosition` today, so this is currently a no-op — applied symmetrically so a Configurator change cannot reopen the hole on the dual path.

Do NOT modify `actions/workflow-graph.ts` (authorizeStep stays untouched). Do not change any function signature or return type.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && grep -n "stepPositionMismatch" actions/workflow.ts | grep -c "await" && git diff --stat actions/workflow-graph.ts | grep -c . </automated>
  </verify>
  <done>`stepPositionMismatch` + `POSITION_MISMATCH_MESSAGE` exported from lib/workflow-graph.ts; awaited in both `advanceProjectStep` and `confirmDualRoleStepAs` before any insert/update; `git diff actions/workflow-graph.ts` is empty; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-submit visibility (anti-stranding) on pages + submit actions</name>
  <files>app/(app)/checklists/[slug]/page.tsx, app/(app)/factory-pm/readiness/page.tsx, actions/checklists.ts, actions/readiness.ts</files>
  <action>
Deny BEFORE the user fills a form that cannot advance, and reject at submit rather than recording-without-advancing. Four call sites, all using `stepPositionMismatch` / `POSITION_MISMATCH_MESSAGE` from `@/lib/workflow-graph` (Task 1):

1. `app/(app)/checklists/[slug]/page.tsx` — in the `workflowNotice` gate chain (lines 64-89), add a new `else if (await stepPositionMismatch(userId, step))` branch immediately AFTER the `!canActOnGraphStep(...)` branch and BEFORE the `else` that sets `workflowProjectId`/`workflowStepN`. Set `workflowNotice = POSITION_MISMATCH_MESSAGE`. Leaving `workflowProjectId`/`workflowStepN` null is what neutralizes the wizard's workflow linkage — do not add other UI.

2. `app/(app)/factory-pm/readiness/page.tsx` — same insertion in its `workflowNotice` chain (after the `!canActOnGraphStep(...)` branch, before the success `else`).

3. `actions/checklists.ts` — inside the `if (projectId && input?.expectedStepN)` authorization block (lines 151-174), after the slug/role check and alongside the assignee gate, add: if `await stepPositionMismatch(userId, step)` return `{ status: 'error', message: POSITION_MISMATCH_MESSAGE }`. It MUST sit above the `try` that inserts the `checklists` row so nothing is persisted for a caller who can never advance.

4. `actions/readiness.ts` — same, inside its `if (input?.projectId && input?.expectedStepN)` block (lines 68-87), above the `readinessForms` insert: return `{ status: 'error', message: POSITION_MISMATCH_MESSAGE }`.

Every insertion gets a short why-comment tagged `quick task 260727-g7a` explaining the anti-stranding intent: the server gate in Task 1 is authoritative, these exist so the user is told before (not after) doing the work.

Keep the diff minimal — no refactoring of the surrounding gate chains, no copy changes to existing branches.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && grep -c "stepPositionMismatch" "app/(app)/checklists/[slug]/page.tsx" "app/(app)/factory-pm/readiness/page.tsx" actions/checklists.ts actions/readiness.ts</automated>
  </verify>
  <done>All four files call `stepPositionMismatch`; both action-layer checks sit above their respective insert; both page checks sit inside the existing `workflowNotice` chain; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression tests + full verification</name>
  <files>tests/actions/workflow.test.ts</files>
  <behavior>
    - advanceProjectStep: a caller whose `users.position` differs from the step's `requiredPosition` returns `false`, writes NO completion row (`insertValuesMock` not called) and never updates the project (`setMock` not called).
    - advanceProjectStep: the same step with a MATCHING position returns `true` and advances normally.
    - advanceProjectStep: unchanged existing tests still pass — steps without `requiredPosition` perform no position query.
    - confirmDualRoleStepAs: a wrong-position caller gets `{ ok: false, advanced: false, message: POSITION_MISMATCH_MESSAGE }` with no `workflowStepStates` write.
  </behavior>
  <action>
Extend `tests/actions/workflow.test.ts` following its existing patterns (vi.hoisted mocks, `vi.resetModules()` per test, dynamic `await import('@/actions/workflow')`).

Add a fixture alongside `liveStepDefRows` / `dualRoleStepDefRows`: `positionStepDefRows` — the same rows with ONE legacy-engine step patched with `requiredPosition: 'head_designer'` (use step 2 `assign_designer_brief`, role `design`, matching the role/step pairing existing advanceProjectStep tests already use). Comment that `requiredPosition` lives on the DB row, not the static LIVE_WORKFLOW_STEPS seed — same reason `dualRoleStepDefRows` exists — and that step 19 `approval_installation` is merely today's live instance of a gate admins can set on any step via the Configurator.

Keep `stepPositionMismatch` REAL (do not add it to the partial `@/lib/workflow-graph` mock) so the test exercises the actual `users.position` fetch. Drive it by sequencing the shared `selectLimitMock`: in `advanceProjectStep` the `.limit()` calls happen in order (1) project row, (2) acting user's position — so use `selectLimitMock.mockResolvedValueOnce([{ id: 'p1', currentStep: 2, status: 'not_delivered' }]).mockResolvedValueOnce([{ position: '<slug>' }])`. Set `selectOrderByMock.mockResolvedValue(positionStepDefRows)` in each new test. Add a comment explaining the call ordering the sequencing depends on.

New tests, in a `describe('requiredPosition gate (quick task 260727-g7a)')` block nested under the existing `describe('advanceProjectStep')`:
- "rejects a right-role caller holding the WRONG position, before any DB write" — position `'chief_production_officer'` vs required `'head_designer'`; expect `false`, `insertValuesMock` not called, `setMock` not called.
- "allows a right-role caller holding the required position" — position `'head_designer'`; expect `true`, completion inserted, `currentStep: 3`.

And one under `describe('confirmDualRoleStepAs')`: patch `materials_readiness` with BOTH `dualRoles` and `requiredPosition`, wrong-position caller expects `{ ok: false, advanced: false, message: <POSITION_MISMATCH_MESSAGE text> }` and `workflowStepStatesInsertMock` not called. Note in a comment that no live dual step sets `requiredPosition` today — this locks in symmetric enforcement.

Then run the full verification suite.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>`npx tsc --noEmit`, `npm run lint`, and `npm test` all exit 0; the new wrong-position tests fail if the Task 1 gate is removed (confirm by reasoning over the assertions, not by reverting).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action (`submitChecklist`, `submitReadinessAction`, `completeAckStepAction`) | Authenticated but role/position-untrusted actor requests a workflow advance; `expectedStepN`/`slug` are client-supplied |
| server action → DB write (`projectStepCompletions`, `projects.currentStep`) | Irreversible workflow advance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g7a-01 | Elevation of Privilege | `advanceProjectStep` (actions/workflow.ts) | mitigate | Gate on fresh `users.position` vs `step.requiredPosition` before any insert (Task 1) |
| T-g7a-02 | Elevation of Privilege | `confirmDualRoleStepAs` (actions/workflow.ts) | mitigate | Same gate applied symmetrically on the dual-role write path (Task 1) |
| T-g7a-03 | Spoofing | position claim source | mitigate | Position read fresh from `users` table, never from the JWT/session (stale claim would under/over-authorize) |
| T-g7a-04 | Tampering | client-supplied `projectId`/`expectedStepN`/`slug` | mitigate | Existing server-side step resolution via `getLiveWorkflowSteps()`+`findStep` retained; new gate runs on the server-resolved step, never on client input |
| T-g7a-05 | Repudiation | checklist recorded but step not advanced | mitigate | Pre-submit action gates reject above the insert, so no orphan evidence row implies a completed step (Task 2) |
| T-g7a-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies in this task |
</threat_model>

<verification>
1. `npx tsc --noEmit` — clean
2. `npm run lint` — clean
3. `npm test` — all green, including the new 260727-g7a cases
4. `git diff --stat actions/workflow-graph.ts` — empty (graph engine untouched)
5. `git diff` review — every new block carries a `260727-g7a` why-comment; no unrelated edits
</verification>

<success_criteria>
- A right-role / wrong-position caller cannot advance a legacy step that sets `requiredPosition` — no completion row, no `currentStep` change (proven by test).
- A right-role / right-position caller advances exactly as before (proven by test).
- The blocked caller sees "This step is restricted to a specific title, and your account is not set to it." on the checklist and readiness pages before filling anything, and on submit if they post anyway — no silently-recorded, non-advancing form.
- Steps without `requiredPosition` are byte-identical in behavior and issue zero additional queries.
- `actions/workflow-graph.ts` is unmodified.
</success_criteria>

<output>
Create `.planning/quick/260727-g7a-enforce-requiredposition-on-legacy-check/260727-g7a-SUMMARY.md` when done
</output>

