---
phase: quick-260727-pd3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BUG-1, BUG-2, BUG-3, BUG-4, BUG-5]
files_modified:
  - lib/workflow.ts
  - lib/workflow-graph.ts
  - lib/my-work.ts
  - actions/workflow.ts
  - actions/checklists.ts
  - actions/readiness.ts
  - app/_components/pending-step-gate.tsx
  - app/_components/header-project-switcher.tsx
  - app/_components/trt-flow-diagram.tsx
  - app/_components/checklist-wizard.tsx
  - app/_components/readiness-form.tsx
  - app/(app)/checklists/[slug]/page.tsx
  - app/(app)/factory-pm/readiness/page.tsx
  - tests/lib/workflow.test.ts
  - tests/actions/workflow.test.ts
  - tests/actions/readiness.test.ts

must_haves:
  truths:
    - "A site_pm viewing the Action-required modal on a dual-role step is told their OWN role (Site PM), not the step's primary role"
    - "A role that has already confirmed a dual-role step stops appearing in that user's pending work (no modal, no Act chip)"
    - "Both dual-role step banners name BOTH roles and state that both must confirm independently — neither promises the project will advance"
    - "Arriving at a dual-role step page shows current progress (e.g. 1 of 2 confirmed — Site PM done, waiting on Factory PM) BEFORE submitting"
    - "After submitting the first half, the success screen says how many confirmations exist and which role is still outstanding"
    - "The step-role badge in the flow diagram and the header 'Waiting on …' string both read 'Factory PM & Site PM' for a dual-role step"
    - "No authorization behaviour changes: confirmDualRoleStepAs's dualRoles membership check, assignee gate, position gate and atomic array_append upsert are byte-identical in effect"
  artifacts:
    - path: "lib/workflow.ts"
      provides: "dualRoleStatus() — the single pure formatter for dual-role labels + progress copy"
      contains: "export function dualRoleStatus"
    - path: "lib/workflow-graph.ts"
      provides: "getDualRoleConfirmations() — reads workflow_step_states.confirmedRoles for one project step"
      contains: "export async function getDualRoleConfirmations"
    - path: "tests/lib/workflow.test.ts"
      provides: "Unit coverage for dualRoleStatus across 0/1/2 confirmations and the non-dual fallback"
      contains: "dualRoleStatus"
  key_links:
    - from: "lib/my-work.ts"
      to: "getDualRoleConfirmations"
      via: "bounded prefetch inside the existing `for (const p of active)` loop, alongside getApprovalState"
      pattern: "getDualRoleConfirmations"
    - from: "actions/workflow.ts advanceOrConfirmDualRole"
      to: "actions/checklists.ts + actions/readiness.ts"
      via: "additive object return { advanced, dualRole }"
      pattern: "advanceOrConfirmDualRole"
    - from: "app/_components/checklist-wizard.tsx"
      to: "SubmitChecklistState.dualRole.text"
      via: "success-screen render"
      pattern: "dualRole"
---

<objective>
Make dual-role steps (live step 17 `materials_readiness`: `dualRoles=['factory_pm','site_pm']`) legible everywhere: the viewer's OWN role, both roles named, 1-of-2 progress, and who must act next.

Purpose: today a site_pm who has already confirmed step 17 is told "Your role: Factory PM", is nagged forever by the Action-required modal, is promised the project "will advance", and never sees that 1 of 2 confirmations already exists. Every one of these is a copy/plumbing defect, not an authorization defect.

Output: one new pure helper (`dualRoleStatus`), one new DB reader (`getDualRoleConfirmations`), a dual-role exclusion in the pending filter, an additive result shape plumbed from `confirmDualRoleStepAs` to both submit actions, and corrected copy in 6 UI surfaces.

Non-goals (explicitly out of scope): ANY authorization change. `factory_manager` is a different role from `factory_pm` and must NOT be added to any dual-role or authorization set — their step is 16 `factory_manager_readiness`. The atomic `array_append` CASE upsert in `confirmDualRoleStepAs` must not be refactored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Live facts already verified today against the DB and running app — do NOT re-investigate:
- Step 17 `materials_readiness`: `role='factory_pm'`, `dualRoles=['factory_pm','site_pm']`, `kind='readiness'`, `slug='delivery_site_readiness'`. `stepRequiredKinds(step).length === 1`, so it takes the `advanceOrConfirmDualRole` branch in both submit actions (never the partial-fulfillment branch).
- On the live test project: `workflow_step_states.status='pending'`, `confirmed_roles=['site_pm']`, `projects.current_step=17`. The Site PM half is done; only the Factory PM half is outstanding.
- `PendingStepGate` already receives `viewerRole` as a prop (`app/(app)/layout.tsx:151`) — BUG-1 needs no new plumbing, only the right label call.
- `useWorkflowSteps()` supplies `LiveWorkflowStep[]` (includes `dualRoles`) to client components, so `header-project-switcher.tsx` can read `step.dualRoles` with no cast.
- `trt-flow-diagram.tsx` is an async server component rendering `getLiveWorkflowSteps()` output — `step.dualRoles` is present there too.
- No `tests/lib/my-work*.test.ts` exists; there is no established mocking harness for `getMyWork`. Do NOT build one. `tests/actions/workflow.test.ts` already has dual-role fixtures (`dualRoleStepDefRows`) and is the right home for the plumbed return-shape coverage.
</context>

<interfaces>
<!-- Existing contracts the executor must build against. Do not re-derive by exploring. -->

lib/workflow-graph.ts:
```ts
export type LiveWorkflowStep = WorkflowStep & {
  stepDefId: string
  dualRoles?: WorkflowRole[] | null
  requiredPosition?: string | null
  receiverRequiredPosition?: string | null
  additionalKinds?: StepKind[] | null
}

// The pattern getDualRoleConfirmations must mirror exactly (lib/workflow-graph.ts:779):
export async function getApprovalState(
  projectId: string,
  stepDefId: string,
): Promise<{ status: string; sentBy: string | null; sentByName: string | null } | null>
```

lib/workflow.ts:
```ts
export function workflowRoleLabel(role: WorkflowRole): string
export function canActOnGraphStep(
  step: { role: WorkflowRole; dualRoles?: WorkflowRole[] | null },
  userRole: UserRole,
): boolean
```

actions/workflow.ts (current shapes — task 3 widens both):
```ts
export async function confirmDualRoleStepAs(opts: {
  projectId: string; expectedStepN: number; notes?: string | null; userId: string; role: UserRole
}): Promise<{ ok: boolean; advanced: boolean; message?: string }>

export async function advanceOrConfirmDualRole(tabToken: string | null, opts: {
  projectId: string; expectedStepN: number; notes?: string | null
}): Promise<boolean>
```

actions/checklists.ts + actions/readiness.ts result states (both gain `dualRole`):
```ts
export type SubmitChecklistState = { status: 'idle' | 'success' | 'error'; message?: string; advanced?: boolean }
```

db/schema.ts: `workflowStepStates.confirmedRoles` is `text('confirmed_roles').array()` -> `string[] | null`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared dualRoleStatus helper and the confirmedRoles reader</name>
  <files>lib/workflow.ts, lib/workflow-graph.ts, tests/lib/workflow.test.ts</files>
  <behavior>
    tests/lib/workflow.test.ts — `describe('dualRoleStatus')`:
    - Non-dual step ({ role: 'factory_pm' }, no dualRoles): `isDual === false`, `rolesLabel === 'Factory PM'`, `progressText === null`, `recordedText === null`.
    - Dual step, `confirmedRoles = null`: `isDual === true`, `rolesLabel === 'Factory PM & Site PM'`, `confirmedCount === 0`, `total === 2`, `outstanding` deep-equals `['factory_pm','site_pm']`, `progressText` mentions both roles and "independently".
    - Dual step, `confirmedRoles = ['site_pm']` (the live case): `confirmedCount === 1`, `outstanding` deep-equals `['factory_pm']`, `outstandingLabel === 'Factory PM'`, `progressText === '1 of 2 confirmed — Site PM done, waiting on Factory PM.'`, `recordedText === 'Recorded — 1 of 2 confirmations. Waiting on Factory PM.'`.
    - Dual step, `confirmedRoles = ['site_pm','factory_pm']`: `confirmedCount === 2`, `outstanding` is `[]`, `outstandingLabel === null`, both `progressText` and `recordedText` equal 'Both roles confirmed — step complete.'
    - Unknown/extra entries in confirmedRoles that are not in dualRoles are ignored (do not inflate confirmedCount past total).
    - Label ordering follows the `dualRoles` array order, not the confirmation order.
  </behavior>
  <action>
    In lib/workflow.ts, add ONE new exported pure helper (no db, no imports beyond what the file already has) directly under `canActOnGraphStep`, plus its exported result type:

    `DualRoleStatus` fields: `isDual: boolean`, `roles: WorkflowRole[]`, `rolesLabel: string`, `confirmedCount: number`, `total: number`, `outstanding: WorkflowRole[]`, `outstandingLabel: string | null`, `progressText: string | null`, `recordedText: string | null`.

    `dualRoleStatus(step: { role: WorkflowRole; dualRoles?: WorkflowRole[] | null }, confirmedRoles?: readonly string[] | null): DualRoleStatus`.

    Rules: when `step.dualRoles` is null/empty, `isDual=false`, `roles=[step.role]`, `rolesLabel=workflowRoleLabel(step.role)`, `total=1`, both text fields null (callers that only want a badge label read `rolesLabel` and get today's single-role behaviour unchanged). When dual, `roles=step.dualRoles` in array order, `rolesLabel` = roles mapped through `workflowRoleLabel` joined with `' & '`, `confirmedCount` = count of `roles` present in `confirmedRoles` (so stray values cannot inflate it), `outstanding` = `roles` not in `confirmedRoles`, `outstandingLabel` = outstanding labels joined with `' & '` or null when empty.

    Copy (exact strings — these are the single source of truth for every surface, do not re-word at call sites):
    - count 0: progressText = `Both ${rolesLabel} must confirm this step independently — neither has confirmed yet.`
    - 0 < count < total: progressText = `${count} of ${total} confirmed — ${confirmedLabel} done, waiting on ${outstandingLabel}.` where confirmedLabel is the confirmed roles' labels joined with `' & '` in `roles` order.
    - count === total: progressText = recordedText = `Both roles confirmed — step complete.`
    - recordedText when outstanding remains: `Recorded — ${count} of ${total} confirmations. Waiting on ${outstandingLabel}.`

    Dense why-comment above it in the repo's existing style, tagged `quick task 260727-pd3`: it exists so the flow diagram badge, the header "Waiting on …" string, both step-page banners and both submit success screens can never drift apart, and so the dual-role wording ("Factory PM & Site PM", either may act first) is authored once. State plainly that this is presentation only — `canActOnGraphStep`/`confirmDualRoleStepAs` remain the authorization boundary.

    In lib/workflow-graph.ts, add `getDualRoleConfirmations(projectId: string, stepDefId: string): Promise<WorkflowRole[]>` immediately after `getApprovalState`, mirroring its exact shape: single `.select({ confirmedRoles: workflowStepStates.confirmedRoles })` on `workflowStepStates` filtered by `and(eq(projectId), eq(stepDefId))` with `.limit(1)`, returning `(row?.confirmedRoles ?? []) as WorkflowRole[]`. No join, no extra columns. Comment it as the read side of the array_append upsert in actions/workflow.ts.

    Do NOT touch `workflowRoleLabel`, `canRoleActOnStep`, `canActOnGraphStep`, or ROLE_LABELS.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx vitest run tests/lib/workflow.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`dualRoleStatus` and `getDualRoleConfirmations` exported; new unit tests pass; tsc clean; no existing lib/workflow.ts export changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix viewer-role label, stop nagging confirmed roles, name both roles on badges (BUG-1, BUG-2, BUG-5)</name>
  <files>app/_components/pending-step-gate.tsx, lib/my-work.ts, app/_components/trt-flow-diagram.tsx, app/_components/header-project-switcher.tsx</files>
  <action>
    BUG-1 — `app/_components/pending-step-gate.tsx:130`: replace `Your role: {workflowRoleLabel(step.role)}` with the VIEWER's own role via `userRoleLabel(viewerRole)` (the component already receives `viewerRole`; import `userRoleLabel` from `@/lib/workflow` and drop the now-unused `workflowRoleLabel` import if nothing else uses it). Why-comment: `step.role` is the step's PRIMARY role, so on a dual-role step a site_pm was told "Your role: Factory PM".

    BUG-5 — `app/_components/trt-flow-diagram.tsx:148`: swap `workflowRoleLabel(step.role)` for `dualRoleStatus(step).rolesLabel`. Leave the legend at line 114 on `workflowRoleLabel(role)` (it iterates single roles) and leave `ROLE_COLOR[step.role]` alone — the accent colour stays keyed to the primary role.

    BUG-5 — `app/_components/header-project-switcher.tsx`: in `waitingOn(step)`, keep the existing `step.requiredPosition` branch FIRST and unchanged (position precedence must not regress; no live dual-role step sets requiredPosition today). Replace the fallback `Waiting on ${workflowRoleLabel(step.role)}` with a `dualRoleStatus(step)` call: `Waiting on ${status.rolesLabel}` and, when `status.isDual`, append ` (either can act first)`. This is the wording the user asked for — a factory_manager reading the header must be able to see step 17 belongs to Factory PM & Site PM, not to them.

    BUG-2 — `lib/my-work.ts`: exclude an already-confirmed viewer from `pending` on a dual-role step, mirroring the existing approval-kind exclusion at lines 131-145.
    - Declare `const dualRoleConfirmedByProjectId = new Map<string, WorkflowRole[]>()` next to `approvalStateByProjectId`.
    - Populate it INSIDE the existing `for (const p of active)` loop (same bounded-prefetch shape as `getApprovalState`, so only projects whose CURRENT step actually has `dualRoles` pay a round trip): `if (step?.dualRoles?.length) dualRoleConfirmedByProjectId.set(p.id, await getDualRoleConfirmations(p.id, step.stepDefId))`. Do NOT add any query inside the `.filter()` callback.
    - In the filter, after the approval-kind block, add: if `step.dualRoles?.length` and the map's value for this project includes `role`, `return false`.
    - Why-comment tagged `quick task 260727-pd3`, matching the tone of the 260714-iuj comment: this is a visibility/nagging fix only — `confirmDualRoleStepAs` re-checks everything server-side and its `role = ANY(confirmed_roles)` CASE already makes a second confirmation a no-op, so hiding it here removes a false prompt rather than a capability. Note explicitly that `activeProjects` is deliberately NOT filtered (the project stays visible in the switcher; only the "Act"/forced-gate signal goes away).
    - Import `getDualRoleConfirmations` from `@/lib/workflow-graph` alongside `getApprovalState`.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && grep -rn "workflowRoleLabel(step.role)" app lib | grep -v '^$' ; test -z "$(grep -rn 'workflowRoleLabel(step.role)' app lib)"</automated>
  </verify>
  <done>Gate shows the viewer's own role; no `workflowRoleLabel(step.role)` call remains in app/ or lib/; dual-role confirmed viewers are dropped from `pending` via a prefetched map (zero queries inside the filter callback); tsc + lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Plumb dual-role progress out of the submit actions (BUG-4 server half)</name>
  <files>actions/workflow.ts, actions/checklists.ts, actions/readiness.ts, tests/actions/workflow.test.ts, tests/actions/readiness.test.ts</files>
  <behavior>
    tests/actions/workflow.test.ts (reuse the existing `dualRoleStepDefRows` fixtures in `describe('confirmDualRoleStepAs')`):
    - First confirmation (`returningMock` -> `[{ confirmedRoles: ['factory_pm'] }]`): result still has `ok: true, advanced: false` and the unchanged `message`, and now also carries `confirmedRoles: ['factory_pm']` and `outstandingRoles: ['site_pm']`. The existing `expect(res).toEqual({...})` assertion at ~line 306 MUST be updated to include the new fields (or switched to `toMatchObject`) — it is currently exact-match and will fail otherwise.
    - Completing confirmation (`['factory_pm','site_pm']`): `advanced: true`, `outstandingRoles: []`.
    - The atomicity assertions (`set.confirmedRoles` is a SQL fragment with `queryChunks`, `setMock` not called on the first confirmation) must still pass untouched — proof the upsert was not refactored.
    - New: `advanceOrConfirmDualRole` on a dual-role step returns `{ advanced: false, dualRole: { confirmedCount: 1, total: 2, text: 'Recorded — 1 of 2 confirmations. Waiting on Site PM.' } }`, and on a NON-dual step returns `{ advanced: <boolean>, dualRole: null }`.

    tests/actions/readiness.test.ts: `advanceMock` currently resolves a bare boolean at lines 50 and 98 and is asserted at 107. Update the mock resolutions to the new object shape; the `toHaveBeenCalledWith(null, { projectId: 'p1', expectedStepN: 3 })` argument assertion is unchanged.
  </behavior>
  <action>
    `actions/workflow.ts` — `confirmDualRoleStepAs`: widen the declared return to `{ ok: boolean; advanced: boolean; message?: string; confirmedRoles?: WorkflowRole[]; outstandingRoles?: WorkflowRole[] }` and populate the two new fields on the two SUCCESS returns only (the partial-confirmation return at ~line 195 and the completed return at ~line 222), computing outstanding from the `confirmedRoles` the upsert already returned. Every early error return keeps its current shape. Do not change any guard, the position check, the assignee gate, the `array_append` CASE upsert, `notifyNextStepOfficers`, or the existing `message` strings — `scripts/verify-live-workflow.ts` asserts against them.

    `actions/workflow.ts` — `advanceOrConfirmDualRole`: change the return type from `Promise<boolean>` to `Promise<{ advanced: boolean; dualRole: { confirmedCount: number; total: number; text: string } | null }>`. On the dual-role branch, build `dualRole` from `dualRoleStatus(step, res.confirmedRoles)` using its `recordedText` for `text` (never hand-format here — one formatter, task 1). On the non-dual branch return `{ advanced: await advanceProjectStep(...), dualRole: null }`. Update the doc-comment: callers no longer "just want a single boolean" — they need to tell the user whether their submission actually completed the step.

    `actions/checklists.ts` — add `dualRole?: { confirmedCount: number; total: number; text: string } | null` to `SubmitChecklistState`. At line ~242, destructure the new object into the existing `advanced` variable plus a `dualRole` variable, and include `dualRole` in the final success return at line ~251. The `requiredKinds.length > 1` partial-fulfillment branch is untouched and leaves `dualRole` null.

    `actions/readiness.ts` — same additive treatment on its state type and the line ~124 call site + line ~131 return.

    Keep the changes strictly additive: `advanced` keeps its exact current meaning (true only when the project moved to the next step), so every existing consumer of `state.advanced` (auto-redirect effects in checklist-wizard.tsx:61 and readiness-form.tsx:48) behaves identically.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx vitest run tests/actions/workflow.test.ts tests/actions/readiness.test.ts tests/actions/escalation-amend.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Both actions return `dualRole`; `advanceOrConfirmDualRole` returns the additive object and both callers are updated; dual-role action tests (incl. the atomicity assertions) pass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Correct the banners and success screens (BUG-3, BUG-4 UI half)</name>
  <files>app/(app)/checklists/[slug]/page.tsx, app/(app)/factory-pm/readiness/page.tsx, app/_components/checklist-wizard.tsx, app/_components/readiness-form.tsx</files>
  <action>
    `app/(app)/checklists/[slug]/page.tsx` — inside the existing `else` branch (~line 85, where `step` is already resolved and the viewer has passed every guard), add `workflowDualText` alongside `workflowStepMulti`: when `step.dualRoles?.length`, call `getDualRoleConfirmations(projectId, step.stepDefId)` and set it to `dualRoleStatus(step, confirmed).progressText`. Then rewrite the banner at lines 149-155 into a three-way branch, dual-role FIRST (the current code branches only on `workflowStepMulti`, which is FALSE for step 17 — that is exactly why the false "will advance" promise shows today):
    1. `workflowDualText` -> `This step needs BOTH {rolesLabel} to confirm independently — submitting here records only your half. {workflowDualText}` (carry `rolesLabel` through from the same `dualRoleStatus` result rather than recomputing).
    2. `workflowStepMulti` -> existing string, unchanged.
    3. else -> existing "will advance the project to its next step." string, unchanged.
    Give the dual-role banner a visually distinct but existing-palette treatment (the amber `border-amber-200 bg-amber-50 text-amber-800` used by `workflowNotice`) so it reads as a caveat rather than a confirmation.

    `app/(app)/factory-pm/readiness/page.tsx` — same treatment for the banner at line 90-94: resolve the dual-role text in the existing guarded `else` branch (~line 47) and render the both-roles caveat instead of the unconditional "Submitting this form will advance the project to its next step." when the step is dual-role. Non-dual readiness steps keep today's copy verbatim.

    `app/_components/checklist-wizard.tsx` — in the `state.status === 'success'` screen (~lines 108-118), when `state.dualRole` is present render `state.dualRole.text` as the body copy (it already reads "Recorded — 1 of 2 confirmations. Waiting on Factory PM." or "Both roles confirmed — step complete."), falling back to today's `state.advanced ? … : …` strings when it is null. Do NOT reformat or re-word on the client — render the server string. The `state.advanced && returnTo` auto-redirect effect and the "Back to projects" link stay gated on `advanced` exactly as now, so a first-half confirmation leaves the user on the page reading the message.

    `app/_components/readiness-form.tsx` — identical change to its `state.status === 'success'` screen (~lines 103-113).

    Every edit gets a short why-comment tagged `quick task 260727-pd3`. No emojis. Next 16 conventions in these files (`await params`, `await searchParams`) are already correct — do not regress them.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && npm test</automated>
  </verify>
  <done>Neither dual-role page promises advancement; both show current 1-of-2 progress before submit; both success screens report confirmations remaining and who is next; full suite green with no regressions (was 337 passing, expect 337 + the new cases).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action (`submitChecklistAction`, `submitReadinessAction`) | untrusted `projectId`/`expectedStepN` cross here; `verifySessionForAction` + `confirmDualRoleStepAs` guards are authoritative and unchanged by this task |
| server component -> rendered page (`checklists/[slug]`, `factory-pm/readiness`) | dual-role progress copy is newly rendered here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260727pd3-01 | Information disclosure | new dual-role progress copy on both step pages | mitigate | render `progressText` ONLY inside the existing guarded `else` branch, i.e. after `canActOnGraphStep` + assignee gate + position gate have already passed; it names roles, never user identities |
| T-260727pd3-02 | Elevation of privilege | `lib/my-work.ts` pending filter | accept | the filter only ever REMOVES items from pending; it cannot grant action rights, and `confirmDualRoleStepAs` re-validates dualRoles membership, assignee gate and position server-side on every write |
| T-260727pd3-03 | Tampering | `confirmDualRoleStepAs` atomic `array_append` upsert | mitigate | return shape is widened additively only; the existing test asserting `set.confirmedRoles` is a SQL fragment (not a JS array) must remain green as proof the read-then-write race was not reintroduced |
| T-260727pd3-SC | Tampering | npm installs | accept | no new dependencies — this task adds no packages, so the package legitimacy gate does not apply |
</threat_model>

<verification>
1. `npx tsc --noEmit` — clean.
2. `npm run lint` — clean.
3. `npm test` — all green, no regressions against the current 337 passing.
4. `test -z "$(grep -rn 'workflowRoleLabel(step.role)' app lib)"` — no surface still labels a dual-role step with only its primary role.
5. `grep -rn 'will advance the project to its next step' app` — remaining hits must all sit in a non-dual-role branch.
6. Manual sanity against the live test project (current_step=17, confirmed_roles=['site_pm']): as site_pm, no Action-required modal and no Act chip for that project; as factory_pm, the readiness page reads "1 of 2 confirmed — Site PM done, waiting on Factory PM."
</verification>

<success_criteria>
- Viewer's OWN role shown in the pending gate.
- A role that already confirmed a dual-role step is never nagged again for it.
- Both dual-role roles named in the flow-diagram badge and the header "Waiting on …" string, from ONE shared helper.
- Pre-submit progress and post-submit confirmation count + outstanding role both surfaced, from ONE shared formatter.
- Zero authorization changes; `factory_manager` untouched; `array_append` upsert untouched.
- tsc, lint, and the full test suite green.
</success_criteria>

<output>
Create `.planning/quick/260727-pd3-dual-role-step-visibility-viewer-role-la/260727-pd3-SUMMARY.md` when done.
</output>
