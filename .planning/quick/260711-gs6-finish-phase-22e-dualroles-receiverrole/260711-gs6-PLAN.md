---
phase: quick-260711-gs6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.ts
  - lib/workflow.ts
  - lib/workflow-graph.ts
  - actions/workflow.ts
  - actions/readiness.ts
  - actions/checklists.ts
  - app/_components/header-project-switcher.tsx
  - app/_components/pending-step-gate.tsx
  - app/_components/project-steps-board.tsx
  - app/(app)/admin/timeline/page.tsx
  - app/(app)/layout.tsx
  - lib/my-work.ts
  - db/workflow-live-steps.ts
  - db/workflow-graph.ts
  - actions/workflow-graph.ts
  - scripts/verify-live-workflow.ts
  - scripts/migrate-merge-readiness-dualroles.ts
  - app/_components/workflow-configurator-shared.tsx
  - actions/workflow-config.ts
  - tests/actions/readiness.test.ts
  - tests/actions/workflow.test.ts
  - tests/lib/workflow.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [Phase-22e-adhoc-dualRoles-receiverRole]

must_haves:
  truths:
    - "The live Neon DB has workflow_step_definitions.receiver_role, workflow_step_definitions.dual_roles, and workflow_step_states.confirmed_roles columns"
    - "The merged Materials/Delivery Readiness step in the live graph has dualRoles = [factory_pm, site_pm] and requires BOTH roles to confirm before advancing"
    - "A super admin can set and view receiverRole and dualRoles per step in the Workflow Configurator without running a script"
    - "npx tsc --noEmit, npm run lint, npm test, and npm run verify:live-workflow all pass after the migration"
    - "The STATE.md Decisions log records this ad hoc work in the established [Ad hoc, ...] style"
  artifacts:
    - path: "scripts/migrate-merge-readiness-dualroles.ts"
      provides: "Additive, same-ids, idempotent migration that sets the dualRoles merge on the live graph"
      contains: "dualRoles"
    - path: "app/_components/workflow-configurator-shared.tsx"
      provides: "Editable receiverRole + dualRoles fields in StepFieldsPanel"
      contains: "dualRoles"
    - path: "actions/workflow-config.ts"
      provides: "Pass-through of receiverRole + dualRoles from the UI to the write engine"
      contains: "receiverRole"
    - path: "db/workflow-live-steps.ts"
      provides: "Parity reference matching the post-migration live graph"
    - path: "scripts/verify-live-workflow.ts"
      provides: "Verification harness updated for the post-merge (linear) graph + dualRoles confirmation"
    - path: ".planning/STATE.md"
      provides: "Ad hoc Decisions-log bullet documenting this work"
  key_links:
    - from: "app/_components/workflow-configurator-shared.tsx"
      to: "actions/workflow-config.ts updateConfigStepAction"
      via: "receiverRole + dualRoles form fields"
      pattern: "receiverRole|dualRoles"
    - from: "actions/workflow-config.ts"
      to: "lib/workflow-graph.ts updateGraphStep/createGraphStep"
      via: "receiverRole + dualRoles options"
      pattern: "receiverRole|dualRoles"
    - from: "scripts/migrate-merge-readiness-dualroles.ts"
      to: "workflow_step_definitions.dual_roles on materials_readiness"
      via: "same-ids in-place update"
      pattern: "dualRoles"
---

<objective>
Finish the uncommitted Phase 22e work (dualRoles + receiverRole on workflow steps) that already
compiles, lints, and tests clean in the working tree. Bring it to a shipped state: push the new
schema columns to the live Neon DB, set the real dualRoles data on the live graph via an additive
migration, expose receiverRole + dualRoles as editable Configurator fields, verify end-to-end, log
the ad hoc decision, and commit.

Purpose: The engine + types for dual-role confirmation and cross-role receive already exist in code
but are inert — the DB has no columns, no live step uses them, and a super admin cannot set them
without a script. This plan makes the feature real and self-serviceable.

Output: Live schema columns, one migration script, Configurator UI + action wiring, an updated parity
reference + verification harness, a STATE.md Decisions bullet, and atomic per-task commits.

⚠ LIVE-DB RISK: `db:push` (Task 1) and the migration (Task 2) mutate the LIVE production Neon
database used for demos. Verification runs IMMEDIATELY after the push and again IMMEDIATELY after the
migration — never deferred to the end — so any problem is caught before more changes stack on top.
As of planning there are ZERO in-flight projects (empty `projects` table), which lowers remap risk,
but the migration must still be same-ids, idempotent, and remap-safe in case a project exists at
execution time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# Feature under finalization (all currently uncommitted; compiles/lints/tests clean)
@db/schema.ts
@lib/workflow.ts
@lib/workflow-graph.ts
@actions/workflow.ts
@actions/workflow-graph.ts

# Migration + verification patterns to follow EXACTLY
@scripts/migrate-insert-design-stages.ts
@scripts/migrate-v2-production-pipeline.ts
@scripts/verify-live-workflow.ts

# Configurator UI conventions (targetRoles / requiredPosition are the models to mirror)
@app/_components/workflow-configurator-shared.tsx
@actions/workflow-config.ts

# Canonical parity reference (24 steps as seeded live)
@db/workflow-live-steps.ts

<verified_live_state>
Read-only inspection of the LIVE graph performed during planning (graph='live', 24 steps, 0 projects):

- The ONLY parallel branch/join in the entire live graph is:
    16 factory_manager_readiness (factory_manager, checklist)
      |- 17 materials_readiness (factory_pm, readiness, slug=null)
      |- 18 delivery_readiness  (site_pm, checklist, slug=delivery_site_readiness)
    both -> 19 delivery_project_check (factory_pm, checklist)
- The ONLY approval-kind step is 12 send_for_production (operations sends,
  requiredPosition=head_of_operations, receiverRequiredPosition=chief_production_officer).
  There is NO factory_pm-sends / site_pm-receives approval step in the live graph.
- `projects` table is EMPTY (no in-flight currentStep to remap right now).
- The 3 new columns (receiver_role, dual_roles, confirmed_roles) do NOT yet exist in the live DB —
  a live query for them errors with `column "receiver_role" does not exist`.
- scripts/verify-live-workflow.ts is STALE: it looks up `delivery_project` and `project_check_report`
  (merged away in Phase 22d) and will throw on the current 24-step graph. It MUST be updated in Task 2.
</verified_live_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push additive schema columns to live Neon DB and commit the feature diff</name>
  <files>db/schema.ts, lib/workflow.ts, lib/workflow-graph.ts, actions/workflow.ts, actions/readiness.ts, actions/checklists.ts, app/_components/header-project-switcher.tsx, app/_components/pending-step-gate.tsx, app/_components/project-steps-board.tsx, app/(app)/admin/timeline/page.tsx, app/(app)/layout.tsx, lib/my-work.ts, db/workflow-live-steps.ts, db/workflow-graph.ts, actions/workflow-graph.ts, tests/actions/readiness.test.ts, tests/actions/workflow.test.ts, tests/lib/workflow.test.ts</files>
  <action>
The three new columns are all additive + nullable: workflow_step_definitions.receiver_role (roleEnum,
nullable), workflow_step_definitions.dual_roles (roleEnum array, nullable), and
workflow_step_states.confirmed_roles (text array, nullable). Push them to the live DB with
`npm run db:push` (the project's drizzle-kit push script — confirmed in package.json).

CRITICAL SAFETY GATE: this run must be PURELY ADDITIVE. If drizzle-kit push interactively prompts to
DROP, RENAME, or TRUNCATE anything, or reports a data-loss statement, ABORT immediately (answer
No / Ctrl-C) and report the exact prompt — do not proceed. Only column additions are acceptable.
Prefer running non-interactively; if the CLI blocks on a prompt for the additive columns, accept ONLY
the additions.

After a clean push, commit the entire existing uncommitted Phase 22e feature diff (the files listed
above — schema, engine, actions, consumers, and their test updates) as the foundational commit, since
the code and the schema it depends on ship together. Do NOT include the migration, Configurator, or
STATE.md changes here — those are separate tasks/commits. Commit message prefix: `feat:`.
  </action>
  <verify>
    <automated>npm run db:push 2>&1 | tee /tmp/gs6-push.log; grep -Eiq "drop|rename|truncate|data.loss|data loss" /tmp/gs6-push.log && { echo "ABORT: non-additive operation detected"; exit 1; }; npm run db:push 2>&1 | grep -Eiq "no changes|nothing to|up to date|No schema changes" && echo "SCHEMA IN SYNC"</automated>
  </verify>
  <done>`db:push` applied only the 3 additive nullable columns; a second `db:push` reports no changes; the columns exist in the live DB; the existing Phase 22e feature diff is committed as one atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2: Write + run the dualRoles merge migration, update parity reference + verify harness, verify immediately</name>
  <files>scripts/migrate-merge-readiness-dualroles.ts, db/workflow-live-steps.ts, scripts/verify-live-workflow.ts</files>
  <action>
Write scripts/migrate-merge-readiness-dualroles.ts following the EXACT pattern of
scripts/migrate-insert-design-stages.ts and scripts/migrate-v2-production-pipeline.ts: dotenv +
neon() + drizzle(neon-http), an idempotency guard at the top, a pre-flight anchor-lookup that
REFUSES TO RUN (process.exit(1)) if expected steps are missing, same-ids in-place updates (never
delete+recreate a step whose id real completions reference), and a remap of projects.currentStep /
project_step_deadlines.stepN / project_step_completions.stepN for every shifted orderIndex.

FLAGGED DECISION — RESOLVE AGAINST LIVE DATA AT EXECUTION, DO NOT GUESS:
Planning verified the merge target below, but the executor MUST re-run a read-only inspection of the
live graph FIRST (projects/steps/edges may have changed since planning) and reconcile against
db/seed-workflow-graph.ts + the prior migration scripts before writing the mutation. If the live shape
differs from what planning found, stop and reconcile — do not force the planned shape onto a different
graph.

  MERGE (dualRoles) — verified target:
  - Survivor = `materials_readiness` (kind=readiness, role=factory_pm). Set dualRoles=[factory_pm,
    site_pm] and checklistSlug='delivery_site_readiness' on it (id preserved). This matches
    stepHref() in lib/workflow.ts: a dualRoles 'readiness' step routes factory_pm to the rich
    /factory-pm/readiness form and any other dualRole (site_pm) to /checklists/<slug>.
  - Remove `delivery_readiness` (the site_pm branch now lives ON the survivor via dualRoles). Before
    deleting, reattribute any project_step_completions rows keyed to delivery_readiness's stepDefId to
    the survivor's id so no audit row is orphaned (there are none as of planning, but handle it).
  - Rewire the branch/join into a linear chain: factory_manager_readiness -> materials_readiness ->
    delivery_project_check. Delete the two old fan-out edges and the delivery_readiness->
    delivery_project_check join edge; keep/insert exactly the two linear edges.
  - Shift orderIndex of every step after the removed one down by 1 (delivery_project_check 19->18 ...
    sign_off 24->23), same-ids, and remap projects/deadlines/completions accordingly (use the
    old-orderIndex -> stepKey -> new-orderIndex remap pattern from migrate-v2-production-pipeline.ts;
    the 2-phase temp-offset trick for the (projectId, stepN) unique constraint applies to deadlines).

  receiverRole — FLAGGED, NO LIVE MIGRATION TARGET:
  - Planning verified NO factory_pm-sends/site_pm-receives approval step exists in the live graph (the
    only approval step is send_for_production, operations->chief_production_officer). Do NOT invent one
    and do NOT retag an unrelated checklist step as approval. The receiverRole capability ships via the
    Configurator UI only (Task 3). Note this explicitly in the migration's header comment and in the
    Task 4 STATE.md bullet. Only IF the executor's live inspection reveals such an approval step now
    exists, set its receiverRole='site_pm'; otherwise skip receiverRole in the migration entirely.

Then update db/workflow-live-steps.ts (LIVE_WORKFLOW_STEPS) to match the post-migration graph:
remove the delivery_readiness entry, add slug='delivery_site_readiness' to materials_readiness,
renumber n for every subsequent step, and append a new header-comment note in the same style as the
existing history ("Shrank from 24 to 23 steps (v2.0 Phase 22e, ...): merged materials_readiness +
delivery_readiness into one dual-confirmation step...").

Then update scripts/verify-live-workflow.ts: the current JOIN test references the removed
delivery_project/project_check_report and is stale. Keep the PARITY group (now 23 steps). REPLACE the
two JOIN-order groups with a dualRoles-confirmation test on the merged materials_readiness step using
confirmDualRoleStep (from actions/workflow.ts): assert the step does NOT advance after only one role
confirms and DOES advance after both, in BOTH confirmation orders (factory_pm-then-site_pm and
site_pm-then-factory_pm), using the same throwaway-project + finally-cleanup harness discipline already
in the file. Keep the server-only require() shim intact.

Run the migration against the live DB, then run the verify harness. Commit the migration + parity ref
+ verify script as one atomic commit. Commit message prefix: `feat:` or `fix:`.
  </action>
  <verify>
    <automated>npx tsx scripts/migrate-merge-readiness-dualroles.ts && npx tsx scripts/migrate-merge-readiness-dualroles.ts 2>&1 | grep -Eiq "already|nothing to do" && npm run verify:live-workflow 2>&1 | grep -q "RESULT: PASS"</automated>
  </verify>
  <done>Migration ran successfully and is idempotent (second run is a no-op); materials_readiness has dualRoles=[factory_pm, site_pm] + slug=delivery_site_readiness; delivery_readiness is removed with edges rewired linear and no orphaned completions; LIVE_WORKFLOW_STEPS matches (23 steps); `npm run verify:live-workflow` prints RESULT: PASS covering PARITY + both dualRoles confirmation orders.</done>
</task>

<task type="auto">
  <name>Task 3: Expose receiverRole + dualRoles as editable Configurator fields</name>
  <files>app/_components/workflow-configurator-shared.tsx, actions/workflow-config.ts</files>
  <action>
The write engine (createGraphStep/updateGraphStep in lib/workflow-graph.ts) already accepts
receiverRole and dualRoles — only the action layer and UI don't surface them yet. Wire them through
end-to-end, mirroring the EXACT conventions already used for targetRoles (multi-role checkbox group)
and requiredPosition (conditional sub-panel):

In actions/workflow-config.ts:
- Add receiverRole?: WorkflowRole | null and dualRoles?: WorkflowRole[] | null to UpdateStepInput and
  AddStepInput.
- Pass them through updateConfigStepAction -> updateGraphStep and addConfigStepAction ->
  createGraphStep, using the same empty-array -> null normalization the existing targetRoles
  pass-through uses.

In app/_components/workflow-configurator-shared.tsx (StepFieldsPanel):
- dualRoles: a multi-role checkbox group identical in style to the existing targetRoles group, shown
  when kind is 'readiness' or 'checklist' (the legacy-engine kinds dualRoles applies to — see the
  db/schema.ts comment). Seed from step.dualRoles ?? []. Add its diff to the `dirty` check and include
  it in the save() payload. Plain-language label, e.g. "Require BOTH roles to confirm
  (dual-confirmation)".
- receiverRole: a single-select (reuse ROLE_OPTIONS plus a "— none —" empty option), shown only when
  kind === 'approval'. Seed from step.receiverRole ?? ''. Add to `dirty` and save() payload. Plain
  label, e.g. "Who receives/approves? (a different role than the sender)".
- Keep empty-value -> null normalization consistent so clearing a field persists as null.

Do NOT change engine behavior, and keep receiverRequiredPosition out of scope (this task is
receiverRole + dualRoles only). Commit the UI + action wiring as one atomic commit. Prefix: `feat:`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "dualRoles" app/_components/workflow-configurator-shared.tsx && grep -q "receiverRole" app/_components/workflow-configurator-shared.tsx && grep -q "receiverRole" actions/workflow-config.ts && grep -q "dualRoles" actions/workflow-config.ts && npm run lint</automated>
  </verify>
  <done>StepFieldsPanel renders an editable dualRoles checkbox group (for readiness/checklist kinds) and a receiverRole select (for approval kind); both persist through updateConfigStepAction/addConfigStepAction into the engine; `npx tsc --noEmit` and `npm run lint` pass.</done>
</task>

<task type="auto">
  <name>Task 4: Full end-to-end verification, STATE.md Decisions bullet, final commit</name>
  <files>.planning/STATE.md</files>
  <action>
Run the complete verification suite and confirm every gate is green:
- `npx tsc --noEmit`
- `npm run lint`
- `npm test`
- `npm run verify:live-workflow`

Then append ONE bullet to the STATE.md Decisions section (the `### Decisions` list, after the existing
"[Ad hoc, 2026-07-10, commit 0d8bacd]" entries), matching that exact style and voice. It must record:
Phase 22e (dualRoles + receiverRole) shipped ad hoc ahead of formal phase execution; the live graph
shrank 24 -> 23 steps via scripts/migrate-merge-readiness-dualroles.ts (additive same-ids merge of
materials_readiness + delivery_readiness into one factory_pm+site_pm dual-confirmation readiness step,
no project orphaned, the only parallel branch/join collapsed to linear); confirmDualRoleStep now gates
that step on both roles; receiverRole was added to schema + Configurator but had NO live migration
target (the only approval step is send_for_production, operations->chief_production_officer, not a
factory_pm/site_pm delivery approval) so it ships UI-only for future use; Configurator now exposes
receiverRole + dualRoles for super-admin self-service. Note it verified green (tsc/lint/test +
verify:live-workflow PARITY + both dualRoles confirmation orders).

Do NOT edit any other part of STATE.md (the orchestrator updates the Quick Tasks table separately).
Reference the commit hash of Task 2's migration commit in the bullet if known, otherwise leave a
`commit <pending>` placeholder consistent with prior entries. Commit the STATE.md change. Prefix:
`docs:`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test && npm run verify:live-workflow 2>&1 | grep -q "RESULT: PASS" && grep -Eq "Phase 22e|dualRoles" .planning/STATE.md</automated>
  </verify>
  <done>All four gates pass (tsc, lint, test, verify:live-workflow=PASS); STATE.md Decisions has one new [Ad hoc, ...] bullet documenting the Phase 22e work in the established style; the STATE.md change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning/executor -> live Neon prod DB | Schema push + data migration mutate the live demo database directly (irreversible without a restore) |
| super-admin browser -> Configurator server actions | New receiverRole/dualRoles fields cross into requireUnlockedAdmin-gated writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gs6-01 | Tampering / DoS (data loss) | `npm run db:push` on live DB | mitigate | Additive nullable columns only; abort on any drop/rename/truncate prompt; idempotent re-push must report no changes (Task 1 verify) |
| T-gs6-02 | Tampering (graph corruption) | merge migration on live graph | mitigate | Same-ids in-place updates, idempotency guard + pre-flight refuse-to-run, completion reattribution before delete, currentStep/deadline/completion remap; verify:live-workflow gate run immediately after (Task 2) |
| T-gs6-03 | Elevation of privilege | Configurator receiverRole/dualRoles writes | accept | Already behind requireUnlockedAdmin() + PIN gate; new fields add no new entry point, only new columns on an existing gated action |
| T-gs6-SC | Tampering | npm/drizzle-kit installs | accept | No new packages installed; drizzle-kit already in devDependencies |
</threat_model>

<verification>
- `db:push` is purely additive (3 nullable columns); a second push reports no changes.
- Migration is idempotent (second run is a no-op) and leaves no orphaned project_step_completions.
- `npm run verify:live-workflow` prints RESULT: PASS covering PARITY (23 steps) + both dualRoles
  confirmation orders on the merged step.
- `npx tsc --noEmit`, `npm run lint`, `npm test` all pass.
- Configurator surfaces receiverRole (approval kind) + dualRoles (readiness/checklist kinds) and
  persists them into the engine.
- STATE.md Decisions log has one new [Ad hoc, ...] bullet in the established style.
</verification>

<success_criteria>
- Live Neon DB has receiver_role + dual_roles (workflow_step_definitions) and confirmed_roles
  (workflow_step_states).
- The merged Materials/Delivery Readiness step has dualRoles=[factory_pm, site_pm] and advances only
  after both roles confirm.
- A super admin can set/view receiverRole + dualRoles in the Configurator without a script.
- tsc, lint, test, and verify:live-workflow all green.
- Ad hoc work recorded in STATE.md Decisions.
- All work committed atomically per task.
</success_criteria>

<output>
Create `.planning/quick/260711-gs6-finish-phase-22e-dualroles-receiverrole/260711-gs6-SUMMARY.md` when done.
</output>
