---
phase: quick-260714-r7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/admin/timeline/table-utils.ts
  - app/(app)/admin/timeline/table-utils.test.ts
  - app/_components/admin-timeline-table.tsx
  - app/(app)/admin/timeline/page.tsx
autonomous: true
requirements:
  - TL-01  # Advanced data table of projects with full column set + preserved super_admin audit link
  - TL-02  # Grouping toggle: by month / by year / by step
  - TL-03  # Filters: search, step select (live steps), created/deadline date ranges, status/payment
  - TL-04  # Server fetches once, client filters in-memory, no new deps
  - TL-05  # Preserve every current feature (history expander, operations act-link gating)

must_haves:
  truths:
    - "Admin sees a projects data table with name, location, current step (n/last + label), status, payment status, deadline, created date"
    - "super_admin still sees the 'View →' audit link per row, unchanged in href and gating"
    - "Admin can toggle grouping between By Month, By Year, and By Step and rows re-bucket accordingly"
    - "Admin can free-text search by name/location/client and rows filter live"
    - "Admin can filter by a specific current step chosen from the LIVE step list (not hardcoded)"
    - "Admin can filter by created-date range and deadline-date range, and by status/payment"
    - "History expander and operations action-link gating still work exactly as before"
  artifacts:
    - path: "app/(app)/admin/timeline/table-utils.ts"
      provides: "Pure TimelineRow type + filterRows/groupRows helpers"
      contains: "export function filterRows"
    - path: "app/(app)/admin/timeline/table-utils.test.ts"
      provides: "Vitest coverage for filtering + grouping"
      contains: "describe"
    - path: "app/_components/admin-timeline-table.tsx"
      provides: "Client advanced data table (filters + grouping + full render)"
      contains: "'use client'"
    - path: "app/(app)/admin/timeline/page.tsx"
      provides: "Server component fetching once + precomputing serializable rows"
      contains: "AdminTimelineTable"
  key_links:
    - from: "app/(app)/admin/timeline/page.tsx"
      to: "app/_components/admin-timeline-table.tsx"
      via: "renders <AdminTimelineTable rows={...} steps={...} />"
      pattern: "AdminTimelineTable"
    - from: "app/_components/admin-timeline-table.tsx"
      to: "app/(app)/admin/timeline/table-utils.ts"
      via: "imports filterRows/groupRows"
      pattern: "filterRows|groupRows"
---

<objective>
Rework the admin Projects Timeline into a proper advanced data table with in-memory
filtering and grouping, matching existing hand-rolled Tailwind/M3 table conventions.

Purpose: Owner wants the timeline screen to behave like an advanced data table —
group by month ("June 2026") / year / step, and filter by search, step, date ranges,
and status/payment — without losing any current behavior.

Output: A pure filter/group helper module (tested), a client data-table component,
and a slimmed server page that fetches once and precomputes serializable rows.

Split rationale: 3 tasks — (1) pure tested logic first to establish the row contract,
(2) client UI built against that contract, (3) server page precomputes + wires. No new
dependencies; server-only workflow helpers stay on the server (the page precomputes derived
fields into plain strings so the client component imports nothing from the workflow engine).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/(app)/admin/timeline/page.tsx
@app/_components/admin-users-table.tsx
@app/(app)/admin/users/page.tsx

<interfaces>
<!-- Executor: use these directly; do not re-derive. -->

projects schema (db/schema.ts):
- name: text NOT NULL
- location: text (nullable) — labelled "No location" when null
- customerName: text (nullable) — this is the "client" search field
- deliveryDate: timestamp (nullable) — doubles as the deadline
- status: enum project_status ['not_delivered','delivered','paused'] default 'not_delivered'
- paymentStatus: enum payment_status ['unpaid','paid'] default 'unpaid'
- currentStep: integer default 2 NOT NULL
- createdAt / updatedAt: timestamp NOT NULL

lib/workflow (pure, server-safe) — already imported by current page:
  findStep(steps, n), lastStepN(steps), projectComplete(currentStep, lastStep),
  canActOnGraphStep(step, role), stepHref(step, projectId, role),
  workflowRoleLabel(role), type UserRole

lib/workflow-graph:
  getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>  — n, key, label, role, kind, stepDefId, ...
  (21 live steps today; NEVER hardcode the count — derive lastStep via lastStepN(steps))

Existing per-row derived logic in current page.tsx (fold into the server precompute — do
NOT change the semantics): complete/overdue/deliveredLate → tone ('green'|'red'|'amber')
and statusLabel ('Delivered' | 'Delivered (late)' | 'Overdue' | 'In progress'); stepLabel
('Delivered' | `${step.label} · ${currentStep}/${lastStep}` | `Step ${currentStep}`);
canAct gate (not complete && step.kind !== 'creation' && step.role === 'operations' &&
canActOnGraphStep) → actHref; waitingLabel = `Waiting on ${workflowRoleLabel(step.role)}`
when not complete && step && no actHref; audit link `/admin/projects/${id}/audit` ONLY when
role === 'super_admin'; history from projectStepCompletions joined to users.name.
</interfaces>

Conventions: Client components live in `app/_components/` with `'use client'`, `useState`,
plain Tailwind tables (see admin-users-table.tsx). Tests are Vitest (`npm test` → `vitest run`),
colocated `*.test.ts`. No new dependencies.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure TimelineRow contract + filter/group helpers (tested)</name>
  <files>app/(app)/admin/timeline/table-utils.ts, app/(app)/admin/timeline/table-utils.test.ts</files>
  <behavior>
    - filterRows returns only rows matching search (case-insensitive substring over name/location/client), and when set: exact currentStep, status, paymentStatus, and createdAt/deliveryDate within [from,to] inclusive (date-portion compare; null deliveryDate excluded when a deadline range is set)
    - filterRows with all-empty filters returns the input unchanged
    - groupRows(mode='month') buckets by createdAt month+year label e.g. "June 2026", newest bucket first
    - groupRows(mode='year') buckets by createdAt year, newest first
    - groupRows(mode='step') buckets by currentStep ascending; complete rows land in a single "Delivered" bucket
  </behavior>
  <action>Define and export `TimelineRow` (plain-serializable: id, name, location, client, currentStep, lastStep, stepLabel, statusLabel, tone, status, paymentStatus, deliveryDate as ISO string|null, createdAt/updatedAt ISO strings, complete, waitingLabel string|null, actHref string|null, auditHref string|null, history array of {label, actor, completedAt ISO, notes string|null}) and a `TimelineFilters` type (search, step number|null, status string|null, paymentStatus string|null, createdFrom/createdTo/deadlineFrom/deadlineTo string|null). Implement pure `filterRows(rows, filters)` and `groupRows(rows, mode)` returning ordered `{ key: string; rows: TimelineRow[] }[]`. No React, no DB imports — pure functions only. Write table-utils.test.ts covering each behavior bullet with small fixture rows.</action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx vitest run "app/(app)/admin/timeline/table-utils.test.ts"</automated>
  </verify>
  <done>Helper module exports TimelineRow/TimelineFilters/filterRows/groupRows; all new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Client advanced data table component</name>
  <files>app/_components/admin-timeline-table.tsx</files>
  <action>Create `'use client'` `AdminTimelineTable({ rows, steps })` where `rows: TimelineRow[]` and `steps: { n: number; label: string }[]`. Hold filter state + grouping mode ('month'|'year'|'step', default 'month') in useState. Render a filter bar (match admin-users-table Tailwind/M3 style): free-text search input; a step `<select>` populated from `steps` (live — never hardcode 21); status select (not_delivered/delivered/paused); payment select (unpaid/paid); created-from/created-to and deadline-from/deadline-to `<input type="date">`; and a grouping toggle (segmented buttons or select) for Month/Year/Step. Derive visible rows via `filterRows`, then `groupRows` by mode; render one table per group with a group-header row showing the group key + count. Table columns: Project (name + location + client + super_admin audit "View →" link when auditHref present + History `<details>` expander identical to current markup), Current Step (stepLabel + actHref "Action needed →" link OR waitingLabel), Deadline, Created, Status (tone pill), Payment. Preserve the "View →" audit link markup/class and the history `<details>` exactly. Empty filtered result shows the existing empty-state cell. Import filterRows/groupRows/types from the timeline table-utils module using the correct relative path.</action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit</automated>
  </verify>
  <done>Component compiles, imports the pure helpers, renders grouped/filtered tables with all columns and preserved audit link + history expander; no new deps.</done>
</task>

<task type="auto">
  <name>Task 3: Server page precomputes serializable rows + wires client table</name>
  <files>app/(app)/admin/timeline/page.tsx</files>
  <action>Keep the server data fetch (projects, completions join, getLiveWorkflowSteps, requireAdmin, force-dynamic). Move ALL per-row derived logic currently inline in the JSX into a precompute loop that produces `TimelineRow[]` (using findStep/lastStepN/projectComplete/canActOnGraphStep/stepHref/workflowRoleLabel exactly as today): tone, statusLabel, stepLabel, complete, actHref (same operations-only gate), waitingLabel, auditHref = role==='super_admin' ? `/admin/projects/${p.id}/audit` : null, and history mapped to {label: findStep(steps,h.stepN)?.label ?? h.stepKey, actor: h.actor ?? 'Unknown', completedAt: ISO, notes}. Convert all dates to ISO strings for serializability. Pass `steps.map(s => ({ n: s.n, label: s.label }))` and the rows to `<AdminTimelineTable rows={rows} steps={steps} />`. Preserve the page header (← Dashboard link, title, the green/red legend paragraph). Replace the inline `<table>` block with the client component. Do not touch audit page internals, positions, or the workflow engine.</action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Server page fetches once, precomputes TimelineRow[], renders AdminTimelineTable; super_admin audit link, history, and act-link gating all preserved; typecheck + lint clean.</done>
</task>

</tasks>

<verification>
Final gate (all must pass):
- `npx tsc --noEmit`
- `npm run lint`
- `npm test`
- `npm run build`
Manual smoke (best-effort): as admin, load /admin/timeline — toggle Month/Year/Step grouping,
type a search term, pick a step + date ranges + status/payment; confirm history expander opens,
operations action link appears only where it did before, and super_admin still sees "View →".
</verification>

<success_criteria>
- Timeline renders as an advanced data table with columns: Project (name/location/client), Current Step (n/last + label), Status, Payment, Deadline, Created, plus preserved super_admin "View →" audit link.
- Grouping toggle switches between By Month ("June 2026"), By Year, By Step buckets.
- Filters work in-memory: search, live-step select, created + deadline ranges, status, payment.
- Server fetches once; client filters; zero new dependencies; existing history + act-link behavior preserved.
</success_criteria>

<output>
Create `.planning/quick/260714-r7a-admin-timeline-as-an-advanced-data-table/260714-r7a-SUMMARY.md` when done.
</output>
