---
phase: quick-260726-dw4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.ts
  - lib/video-calls.ts
  - lib/notifications.ts
  - tests/lib/video-calls.test.ts
  - package.json
  - package-lock.json
  - app/api/cron/call-reminders/route.ts
  - tests/app/api/cron-call-reminders.test.ts
  - .env.local
  - netlify.toml
  - netlify/functions/send-call-reminders.mts
  - app/_components/participant-picker.tsx
  - app/_components/new-call-form.tsx
  - app/_components/schedule-call-form.tsx
  - app/(app)/calls/page.tsx
autonomous: false
requirements: [REM-01, REM-02, REM-03, REM-04, BTN-01, BTN-02]

user_setup:
  - service: netlify
    why: "The Scheduled Function needs CRON_SECRET and SITE_URL in its Netlify runtime environment to authenticate against and reach the internal Next.js route — local .env.local values are dev-only and are never read by the deployed Netlify Function."
    env_vars:
      - name: CRON_SECRET
        source: "Generated locally in Task 4 (crypto random hex, written directly into .env.local, never echoed) — copy that exact value into Netlify Dashboard -> Site configuration -> Environment variables."
      - name: SITE_URL
        source: "Netlify Dashboard -> Site configuration -> Environment variables -> set to this site's real deployed origin (e.g. https://<site-name>.netlify.app or the custom domain)."
    dashboard_config:
      - task: "Confirm a Netlify site is actually connected/deployed for this repo at all (RESEARCH.md Open Question 1) — if not, Netlify onboarding (site creation + connecting this repo + first successful build) must happen before the scheduled function can ever run."
      - task: "After the first deploy with netlify.toml + netlify/functions/send-call-reminders.mts, check the Netlify dashboard's Functions tab to confirm send-call-reminders appears in BOTH the general Functions list and the Scheduled Functions panel (proves the A1 coexistence assumption held), and that the existing app build/deploy is unaffected."
      - task: "Use the Scheduled Functions panel's 'Run now' (or netlify functions:invoke via the newly-installed netlify-cli) to manually trigger the function once and confirm it reaches the internal route successfully."

must_haves:
  truths:
    - "A super_admin or operations user sees an always-visible 'Schedule Call' button next to 'Start a call' on /calls; clicking it opens a MODAL (not an inline expand) with a title input, native date+time inputs, and a participant picker"
    - "new-call-form.tsx no longer contains any inline 'Schedule for later' checkbox, isAdmin prop, or scheduledFor state — it is a pure start-now flow for every role, with exactly one remaining way to schedule a call (the new modal)"
    - "Submitting the Schedule Call modal with a future date/time and at least one other participant creates a scheduled call via the same createVideoCallAction/createVideoCall path already used today (unchanged validation, unchanged early-join GetStream behavior)"
    - "A call whose scheduledFor falls within the next hour, is still active, and has never been reminded gets its invitees (participants minus creator) notified exactly once with a distinct 'video_call_reminder' notification; running the reminder job again afterward never re-notifies that same call"
    - "The reminder job is triggered by a Netlify Scheduled Function running every 5 minutes, which is a thin trigger that calls a CRON_SECRET-protected internal Next.js route rather than duplicating DB/GetStream/notification logic itself"
    - "The internal cron route rejects any request without the exact CRON_SECRET Bearer token with 401, and only calls sendDueCallReminders when the token matches"
    - "The genuine unknowns from RESEARCH.md (whether a Netlify site is even connected yet; whether a hand-written netlify/functions file coexists with the Next.js Runtime's own auto-generated functions) are resolved via a real deploy + dashboard check, not silently assumed to work"
  artifacts:
    - path: "db/schema.ts"
      provides: "videoCalls.reminderSentAt nullable timestamp column"
      contains: "reminder_sent_at"
    - path: "lib/video-calls.ts"
      provides: "sendDueCallReminders() — selects due calls, notifies invitees once, sets reminderSentAt"
      contains: "sendDueCallReminders"
    - path: "lib/notifications.ts"
      provides: "VIDEO_CALL_REMINDER_NOTIFICATION_TYPE constant + widened sidebar-badge count"
      contains: "video_call_reminder"
    - path: "app/api/cron/call-reminders/route.ts"
      provides: "Bearer CRON_SECRET-protected POST handler calling sendDueCallReminders"
      contains: "CRON_SECRET"
    - path: "netlify/functions/send-call-reminders.mts"
      provides: "thin Netlify Scheduled Function trigger, v2 API shape"
      contains: "schedule"
    - path: "netlify.toml"
      provides: "functions directory declaration, deliberately no [build] override"
      contains: "netlify/functions"
    - path: "app/_components/participant-picker.tsx"
      provides: "shared search+checkbox participant picker used by both call-start forms"
      contains: "ParticipantPicker"
    - path: "app/_components/schedule-call-form.tsx"
      provides: "admin-only Schedule Call button + modal"
      contains: "scheduledFor"
  key_links:
    - from: "app/_components/schedule-call-form.tsx"
      to: "actions/video-calls.ts"
      via: "createVideoCallAction(getTabToken(), { ..., scheduledFor: parsedDate.toISOString() }) — always set"
      pattern: "scheduledFor"
    - from: "app/api/cron/call-reminders/route.ts"
      to: "lib/video-calls.ts"
      via: "sendDueCallReminders()"
      pattern: "sendDueCallReminders\\("
    - from: "netlify/functions/send-call-reminders.mts"
      to: "app/api/cron/call-reminders/route.ts"
      via: "fetch(`${baseUrl}/api/cron/call-reminders`, { headers: { authorization: `Bearer ${CRON_SECRET}` } })"
      pattern: "api/cron/call-reminders"
    - from: "lib/video-calls.ts sendDueCallReminders"
      to: "lib/notifications.ts notifyUser"
      via: "notifyUser({ ..., type: VIDEO_CALL_REMINDER_NOTIFICATION_TYPE, callId: call.id })"
      pattern: "video_call_reminder"
---

<objective>
Two follow-ups to the video-call scheduling feature shipped in quick task 260724-alz:

1. Replace the buried inline "Schedule for later" checkbox in `new-call-form.tsx` with a dedicated, always-visible "Schedule Call" button + modal (admin-only), reusing the existing participant-picker logic.
2. Add a reminder notification ~1 hour before a scheduled call's start time, triggered by a Netlify Scheduled Function (net-new infra for this repo) that calls a secret-protected internal route so the actual DB/notification logic stays centralized in `lib/`.

Purpose: Make scheduling discoverable (it was hidden inside "Start a call"), and make sure invitees to a scheduled call actually get reminded before it starts instead of relying on memory.

Output: `videoCalls.reminderSentAt` column + `sendDueCallReminders()` (lib/video-calls.ts) + CRON_SECRET-protected `app/api/cron/call-reminders/route.ts` + `netlify.toml`/`netlify/functions/send-call-reminders.mts` (net-new Netlify infra) + a shared `ParticipantPicker` + a new `ScheduleCallForm` modal wired into `/calls`, with `new-call-form.tsx` reverted to a pure start-now flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@AGENTS.md
@.planning/quick/260726-dw4-schedule-call-button-modal-and-1-hour-be/260726-dw4-CONTEXT.md
@.planning/quick/260726-dw4-schedule-call-button-modal-and-1-hour-be/260726-dw4-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — do not re-explore. -->

db/schema.ts videoCalls (current, post-260724-alz):
export const videoCalls = pgTable('video_calls', {
  id:        uuid('id').primaryKey().defaultRandom(),
  title:     text('title'),
  createdBy: uuid('created_by').notNull().references(() => users.id),
  status:    text('status').default('active').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  endedAt:   timestamp('ended_at'),
  scheduledFor: timestamp('scheduled_for'),
})
`timestamp` is already imported from `drizzle-orm/pg-core` — no new import needed for `reminderSentAt`.

lib/video-calls.ts current shape (all server-only, CALL_TYPE = 'default'):
- `import { and, desc, eq, inArray } from 'drizzle-orm'` — Task 2 must widen this to also import `gt, lte, isNull, isNotNull`.
- `import { videoCalls, videoCallParticipants, users } from '@/db/schema'` — already imports everything sendDueCallReminders needs.
- `import { notifyUser } from '@/lib/notifications'` — Task 2 must also import `VIDEO_CALL_REMINDER_NOTIFICATION_TYPE` from the same module.
- `notifyUser` signature: `{ recipientId; actorId; type; title; body?; projectId?; callId?; actorId? }`.
- `createVideoCall` already inserts `scheduledFor` and varies the invitee notification title (unchanged by this plan — canonical reference only).
- `getMyCalls`/`VideoCallRow` already carry `scheduledFor: Date | null` — no change needed there.

lib/notifications.ts current shape:
- `import { and, count, desc, eq, inArray, isNull } from 'drizzle-orm'` — `inArray` already imported, reuse it.
- `export const VIDEO_CALL_NOTIFICATION_TYPE = 'video_call'`
- `export async function getVideoCallUnreadCount(userId: string): Promise<number>` currently filters with `eq(notifications.type, VIDEO_CALL_NOTIFICATION_TYPE)` inside an `and(...)` — widen to `inArray(notifications.type, [VIDEO_CALL_NOTIFICATION_TYPE, VIDEO_CALL_REMINDER_NOTIFICATION_TYPE])`.
- `notifyUser(input)` — unchanged, reused as-is.

app/_components/notifications-bell.tsx (unchanged by this plan — confirms routing convention):
- `markOne` routes to `/calls/{callId}` whenever `item.callId` is truthy, REGARDLESS of `item.type` — so a `video_call_reminder` notification with `callId` set routes correctly with zero changes needed in this file.

app/_components/pending-call-gate.tsx (unchanged by this plan — DELIBERATE, do not touch):
- Its ringing "join now" forcing modal is hardcoded to `i.type === 'video_call'` only. A reminder notification must NEVER trigger this — that is exactly why the reminder uses a distinct type string instead of reusing `'video_call'`.

app/_components/new-call-form.tsx current shape (before this plan's Task 7):
- `'use client'`, props today: `{ allUsers: PersonOption[]; isAdmin: boolean }`. State: `title`, `query`, `picked: Set<string>`, `schedule`, `scheduledFor`, `pending`, `message`.
- The inline search-input + `max-h-48` scrollable checkbox-list block (lines ~90-120 of the current file) is the exact JSX Task 7 extracts into `ParticipantPicker`.
- The `{isAdmin && (...)}` "Schedule for later" block (checkbox + `datetime-local` input) is the exact block Task 7 deletes entirely.

app/(app)/calls/page.tsx current shape:
- `const { userId, role } = await verifySession()`; `isAdmin = isAdminRole(role)` already computed via `import { verifySession, isAdminRole } from '@/lib/dal'`.
- `<NewCallForm allUsers={allUsers} isAdmin={isAdmin} />` — Task 7 drops the `isAdmin` prop from `NewCallForm` itself; Task 8 instead renders `{isAdmin && <ScheduleCallForm allUsers={allUsers} />}` alongside it.
- `formatWhen(d: Date): string` local helper already exists — not touched by this plan (the modal doesn't need it; the existing "Scheduled" list section already uses it).

tests/lib/video-calls.test.ts mocking conventions (extend, don't replace):
- `vi.hoisted(() => ({ ...Mock: vi.fn() }))` pattern for every mock referenced inside a `vi.mock(...)` factory.
- `@/db` mock already provides `update: () => ({ set: (patch) => { updateSetMock(patch); return { where: updateWhereMock } } })` — reuse directly for `reminderSentAt` updates, no new mock needed.
- `selectWhereMock` already supports `.mockResolvedValueOnce(...)` sequencing (see the existing `addVideoCallParticipants` describe block) — sequence it per call: first resolved value = the due-calls array, next resolved value(s) = each due call's participants array, in call order.
- The file's own top-level `const { createVideoCall, addVideoCallParticipants, ensureCallParticipant, removeCallParticipant } = await import('@/lib/video-calls')` must be extended to also destructure `sendDueCallReminders`.

tests/app/api/tab-refresh.test.ts convention (mirror for the new cron-route test):
- `vi.mock('@/lib/...', () => ({ someFn: someFnMock }))`, a `makeRequest(body)` helper building a `Request`, dynamic `await import('@/app/api/.../route')` inside each `it(...)` after mocks/env are set, asserting on `res.status` and `await res.json()`.

RESEARCH.md's verified-current Netlify Functions v2 shape (do not use any older `exports.handler`/`schedule()`-wrapper shape found in pre-2026 tutorials):
```typescript
import type { Config } from "@netlify/functions";
export default async (req: Request) => { /* thin fetch-only trigger */ };
export const config: Config = { schedule: "*/5 * * * *" };
```
</interfaces>
</context>

<constraints>
- Do NOT revisit the reminder trigger mechanism — Netlify Scheduled Function, every 5 minutes, is locked (CONTEXT.md).
- The old inline "Schedule for later" checkbox in `new-call-form.tsx` must be fully removed, not kept alongside the new modal — exactly one way to schedule a call going forward.
- Native `<input type="date">` + `<input type="time">` satisfy "clock UI" — no new date-picker dependency.
- Do NOT duplicate DB/GetStream/notification logic inside the Netlify function itself — it is a thin fetch-only trigger into the internal Next.js route, which is the single source of truth (CONTEXT.md).
- Do NOT touch `app/_components/pending-call-gate.tsx`'s hardcoded `'video_call'`-only filter — a reminder must never trigger that ringing "join now" gate.
- Do NOT touch the existing `scheduledFor` tests from quick task 260724-alz (`tests/lib/video-calls.test.ts`'s `createVideoCall`/`addVideoCallParticipants` describe blocks, `tests/actions/video-calls.test.ts` entirely) — only add new describe blocks/cases.
- `npm run db:push` is this project's only schema-application mechanism (no `drizzle/` migrations directory). The executor must run it for real, report its actual output, and must NOT auto-confirm any destructive-change prompt for an unrelated column — halt and surface it instead.
- Generated secrets (CRON_SECRET) must be written directly into `.env.local` and never echoed/printed in any tool output or log.
- Package installs (`@netlify/functions`, `netlify-cli`) are `[ASSUMED]` per RESEARCH.md's Package Legitimacy Audit (slopcheck was not run this session, despite both being official first-party `@netlify/*`/Netlify-org packages cited directly from live docs) — a blocking human-verify checkpoint precedes the install.
- Deploying to Netlify and confirming the scheduled function actually runs cannot be automated from this environment — a blocking human-action checkpoint covers this instead of silently assuming success.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Schema — add reminderSentAt column and push to the live DB</name>
  <files>db/schema.ts</files>
  <action>
Per REM-01 (idempotent reminder gate). In db/schema.ts's `videoCalls` table, add a nullable `reminderSentAt: timestamp('reminder_sent_at'),` column directly after `scheduledFor: timestamp('scheduled_for'),` (no default, no `.notNull()` — null means "not yet reminded"; `timestamp` is already imported at the top of the file). Do not touch any other column or table.

Run `npm run db:push` against the live Neon DB (per this project's established pattern — no `drizzle/` migrations directory, `DATABASE_URL` in `.env.local`). Read its output carefully:
- If it reports only an additive `ADD COLUMN "reminder_sent_at" timestamp` for `video_calls` with no other table affected, confirm and let it apply.
- If drizzle-kit prompts about renaming/dropping ANY other column (an unrelated destructive-change prompt), STOP immediately, do not confirm it, and surface the exact prompt text as a finding instead of proceeding.
Run `npm run db:push` a second time and confirm it reports no further changes (idempotent).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "db/schema\.ts" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && echo SCHEMA_TSC_OK</automated>
  </verify>
  <done>videoCalls.reminderSentAt exists in db/schema.ts and in the live Neon DB (confirmed via a successful db:push, second consecutive run reporting no changes); no unrelated destructive prompt was auto-accepted; tsc --noEmit reports no errors in db/schema.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — sendDueCallReminders + reminder-aware sidebar badge</name>
  <files>lib/video-calls.ts, lib/notifications.ts, tests/lib/video-calls.test.ts</files>
  <behavior>
    - `sendDueCallReminders()` selects only `videoCalls` rows where `status='active'`, `scheduledFor IS NOT NULL`, `scheduledFor > now`, `scheduledFor <= now + 1 hour`, and `reminderSentAt IS NULL`.
    - For each due call it notifies every participant except the call's creator (same invitee-derivation as `createVideoCall`) via `notifyUser` with `type: VIDEO_CALL_REMINDER_NOTIFICATION_TYPE` ('video_call_reminder'), `title: \`${call.title ?? 'Video call'} starts in 1 hour\``, `callId: call.id`, `actorId: call.createdBy`.
    - After notifying, it sets that call's `reminderSentAt` to the current time and includes its id in the returned `remindedCallIds` array.
    - Calling it again immediately afterward (same due calls, now with `reminderSentAt` set) reminds nobody a second time.
    - A call with `scheduledFor` more than 1 hour away, already in the past, or null is never selected.
    - `getVideoCallUnreadCount(userId)` counts unread notifications of EITHER `VIDEO_CALL_NOTIFICATION_TYPE` or `VIDEO_CALL_REMINDER_NOTIFICATION_TYPE` (the sidebar "Calls" badge must reflect reminders too, or a reminder would be invisible from the nav until the bell dropdown is separately opened).
  </behavior>
  <action>
Per REM-02 (the reminder logic itself, kept in `lib/`, later called by Task 4's route — CONTEXT.md's decision that the Netlify function stays a thin trigger).

1. lib/notifications.ts: export a new `VIDEO_CALL_REMINDER_NOTIFICATION_TYPE = 'video_call_reminder'` constant alongside the existing `VIDEO_CALL_NOTIFICATION_TYPE`. Widen `getVideoCallUnreadCount`'s filter from `eq(notifications.type, VIDEO_CALL_NOTIFICATION_TYPE)` to `inArray(notifications.type, [VIDEO_CALL_NOTIFICATION_TYPE, VIDEO_CALL_REMINDER_NOTIFICATION_TYPE])` (`inArray` is already imported in this file). Do NOT touch `pending-call-gate.tsx`'s own hardcoded `'video_call'` filter — deliberate, per constraints.

2. lib/video-calls.ts: widen `import { and, desc, eq, inArray } from 'drizzle-orm'` to also import `gt, lte, isNull, isNotNull`. Import `VIDEO_CALL_REMINDER_NOTIFICATION_TYPE` from `@/lib/notifications` alongside the existing `notifyUser` import. Add a new exported function `sendDueCallReminders(): Promise<{ remindedCallIds: string[] }>`.

Implementation: compute `now = new Date()` and `windowEnd = new Date(now.getTime() + 60 * 60 * 1000)`. Select `videoCalls` rows where `and(eq(videoCalls.status, 'active'), isNotNull(videoCalls.scheduledFor), gt(videoCalls.scheduledFor, now), lte(videoCalls.scheduledFor, windowEnd), isNull(videoCalls.reminderSentAt))`. For each due call, in a sequential `for...of` loop (not `Promise.all` across calls — keeps the idempotency-critical read-then-write per call simple, avoids cross-call races): query `videoCallParticipants` for that `callId`, derive `invitees = participants.map(p => p.userId).filter(id => id !== call.createdBy)`, `await Promise.all(invitees.map(recipientId => notifyUser({ recipientId, actorId: call.createdBy, type: VIDEO_CALL_REMINDER_NOTIFICATION_TYPE, title: \`${call.title ?? 'Video call'} starts in 1 hour\`, body: null, callId: call.id })))`, then `await db.update(videoCalls).set({ reminderSentAt: new Date() }).where(eq(videoCalls.id, call.id))`, then push `call.id` onto `remindedCallIds`. Return `{ remindedCallIds }`.

3. Extend tests/lib/video-calls.test.ts: widen the file's top-level `const { createVideoCall, addVideoCallParticipants, ensureCallParticipant, removeCallParticipant } = await import('@/lib/video-calls')` to also destructure `sendDueCallReminders`. Add a new `describe('sendDueCallReminders', ...)` block reusing the file's existing hand-rolled `@/db` mock (`selectWhereMock.mockResolvedValueOnce(...)` sequencing per query in call order — first call = due-calls array, subsequent calls = each due call's participants array — and the existing `updateSetMock`/`updateWhereMock` mocks already present from `endVideoCall`'s own `db.update` usage). Cover: (a) a call inside the next-hour window with a `reminderSentAt: null` shape gets its non-creator participants notified once each with `type: 'video_call_reminder'` and the expected title, `remindedCallIds` includes its id; (b) a call whose mocked select never returns it (simulating `reminderSentAt` already set, so it's excluded by the WHERE clause) results in zero notifications for that call; (c) calling `sendDueCallReminders` twice where the second mocked select returns an empty array (mirroring reality after the first run's update) results in `notifyUserMock` being called only during the first invocation; (d) the creator is never included in the invitee list even when present in that call's mocked participants rows.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/video-calls.test.ts && npx tsc --noEmit 2>&1 | grep -iE "lib/video-calls\.ts|lib/notifications\.ts" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && echo REMINDER_BACKEND_OK</automated>
  </verify>
  <done>sendDueCallReminders selects only calls in the next-hour window with reminderSentAt null, notifies participants-minus-creator with a distinct 'video_call_reminder' type, and marks reminderSentAt so a second run never re-notifies the same call; getVideoCallUnreadCount counts both video-call notification types; tests/lib/video-calls.test.ts passes (existing + new cases); tsc --noEmit reports no errors in the touched files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm @netlify/functions and netlify-cli are legitimate before installing</name>
  <what-built>N/A — pre-install legitimacy gate. RESEARCH.md's Package Legitimacy Audit ran without slopcheck this session, so both packages are conservatively tagged [ASSUMED] despite being cited directly from Netlify's own official docs fetched live this session.</what-built>
  <action>
Visit https://www.npmjs.com/package/@netlify/functions and https://www.npmjs.com/package/netlify-cli. Confirm both are published under the official `netlify` npm org, have a multi-year publication history, and that the current versions (`@netlify/functions@5.3.0`, `netlify-cli@27.0.0` per RESEARCH.md) match what's on the registry today. No code change in this task — Task 4 performs the actual `npm install` only after this is confirmed.
  </action>
  <how-to-verify>Check the npmjs.com listing for org/maintainer identity, download counts, and repository link (github.com/netlify/functions, github.com/netlify/cli) for both packages.</how-to-verify>
  <verify>
    <automated>MISSING — manual npm registry check, not automatable</automated>
  </verify>
  <done>Both packages confirmed as legitimate first-party Netlify packages (or a safer alternative identified) before any install proceeds.</done>
  <resume-signal>Type "packages verified" to proceed with Task 4's install.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Internal cron route handler — CRON_SECRET Bearer auth + sendDueCallReminders</name>
  <files>package.json, package-lock.json, app/api/cron/call-reminders/route.ts, tests/app/api/cron-call-reminders.test.ts, .env.local</files>
  <action>
Per REM-03. Run `npm install -D @netlify/functions netlify-cli` (only after Task 3's checkpoint is resumed).

Create `app/api/cron/call-reminders/route.ts` (new Route Handler, `export const dynamic = 'force-dynamic'`, mirroring `app/api/auth/tab-refresh/route.ts`'s shape — plain `Request`/`NextResponse`, no session/DAL involved since this is machine-to-machine, not user-facing): export a single `async function POST(req: Request)` that reads the `authorization` header, compares it against `Bearer ${process.env.CRON_SECRET}`, and returns `NextResponse.json({ error: 'unauthorized' }, { status: 401 })` when `process.env.CRON_SECRET` is unset OR the header doesn't match exactly. On a valid match, call `sendDueCallReminders()` (import from `@/lib/video-calls`) and return `NextResponse.json({ ok: true, remindedCount: result.remindedCallIds.length, remindedCallIds: result.remindedCallIds })`.

Add to `.env.local`: generate a random secret locally (e.g. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`) and append `CRON_SECRET=<generated-value>` as a new line — write the generated value directly into the file, never print it in any tool output/log. Also append `SITE_URL=` set to whatever `APP_URL` is already configured as in this same file (same underlying concept — the app's own base origin, already used elsewhere for email links — reuse its value rather than inventing a second one) if `APP_URL` is present, otherwise `SITE_URL=http://localhost:3000`.

Create `tests/app/api/cron-call-reminders.test.ts` mirroring `tests/app/api/tab-refresh.test.ts`'s conventions exactly (`vi.mock('@/lib/video-calls', () => ({ sendDueCallReminders: sendDueCallRemindersMock }))`, a `makeRequest` helper building a `Request` with an `authorization` header, dynamic `await import('@/app/api/cron/call-reminders/route')` inside each `it(...)` after mocks/env are set). Cover: (a) missing `authorization` header returns 401 and never calls `sendDueCallRemindersMock`; (b) an `authorization` header present but not matching `Bearer ${CRON_SECRET}` returns 401 (set `process.env.CRON_SECRET` in the test file before each relevant assertion, matching how `tests/lib/video-calls.test.ts` sets `GETSTREAM_APIKEY`/`GETSTREAM_SECRET` before its own dynamic import); (c) a correct `authorization: Bearer <secret>` header returns 200 with a JSON body reflecting the mocked `remindedCallIds`, and `sendDueCallRemindersMock` is called exactly once.
  </action>
  <verify>
    <automated>npx vitest run tests/app/api/cron-call-reminders.test.ts && npx tsc --noEmit 2>&1 | grep -iE "app/api/cron/call-reminders" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && echo CRON_ROUTE_OK</automated>
  </verify>
  <done>package.json lists @netlify/functions and netlify-cli as devDependencies; app/api/cron/call-reminders/route.ts rejects any request without the exact CRON_SECRET Bearer token with 401 and otherwise calls sendDueCallReminders and returns its summary; .env.local has CRON_SECRET and SITE_URL set (values never echoed in any output); new test file passes; tsc --noEmit reports no errors in the route file.</done>
</task>

<task type="auto">
  <name>Task 5: Netlify Scheduled Function trigger + minimal netlify.toml</name>
  <files>netlify.toml, netlify/functions/send-call-reminders.mts</files>
  <action>
Per REM-04, using the verified-current Netlify Functions v2 API shape from RESEARCH.md (inline `export const config: Config = { schedule }`, `.mts` extension, ES module default export — NOT the deprecated `exports.handler`/`schedule()`-wrapper shape from pre-2026 tutorials).

Create `netlify/functions/send-call-reminders.mts`: a thin trigger with zero DB/GetStream/notification logic (CONTEXT.md decision — all real logic lives in `lib/` via the internal route). Default-export an async function that: reads `const baseUrl = process.env.SITE_URL ?? process.env.URL` (SITE_URL primary per RESEARCH.md Pitfall #3 — `URL`'s behavior specifically for scheduled/cron invocations across deploy contexts isn't explicitly documented, only used as a fallback); if `baseUrl` is falsy, `console.error(...)` and return without throwing; otherwise `fetch(\`${baseUrl}/api/cron/call-reminders\`, { method: 'POST', headers: { authorization: \`Bearer ${process.env.CRON_SECRET ?? ''}\` } })` (Bearer auth matches Task 4's route — adapt RESEARCH.md's illustrative `x-internal-secret` example to this project's actual `Authorization: Bearer` + `CRON_SECRET` naming, do not introduce a second secret-header convention); on a non-ok response, `console.error('call-reminders trigger failed:', res.status, await res.text())`. Export `const config: Config = { schedule: '*/5 * * * *' }` (`import type { Config } from '@netlify/functions'`).

Create `netlify.toml` at the repo root. Deliberately do NOT add a `[build]` section — RESEARCH.md's Open Question 1 plus this project's own STATE.md history (a prior quick task, 260723-cme, references "Netlify's env var" already existing for this app) implies a Netlify site/deploy already exists today with NO in-repo `netlify.toml`, meaning the actual build command/publish directory are almost certainly already configured via Netlify's dashboard UI — an in-repo `[build]` block would take precedence and could silently override those unknown, currently-working dashboard settings. Include only a functions-directory declaration for explicitness (`[functions]` / `directory = "netlify/functions"` — matches Netlify's own default location, so this is a documentation-only no-op today) plus a top-of-file comment explaining why `[build]` was deliberately omitted and that Task 6's checkpoint must confirm the next deploy's build behavior is unaffected.
  </action>
  <verify>
    <automated>test -f netlify.toml && test -f netlify/functions/send-call-reminders.mts && grep -q "schedule" netlify/functions/send-call-reminders.mts && ! grep -q "^\[build\]" netlify.toml && echo NETLIFY_FILES_OK</automated>
  </verify>
  <done>netlify/functions/send-call-reminders.mts exists, exports the v2-API default handler + config.schedule = '*/5 * * * *', fetches the internal route with a Bearer CRON_SECRET header, and contains zero DB/GetStream logic; netlify.toml exists with no [build] section (to avoid overriding unknown existing dashboard build config) and a documented functions directory.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Deploy and verify the Netlify Scheduled Function actually runs</name>
  <what-built>netlify.toml + netlify/functions/send-call-reminders.mts (Task 5) and the CRON_SECRET-protected internal route (Task 4) — none of this can be proven to work end-to-end without a real Netlify deploy, which this environment cannot perform.</what-built>
  <action>
Two genuinely unverified items from RESEARCH.md must be resolved by deploying for real, not assumed:
1. Open Question 1: confirm whether this repo already has a live Netlify site connected (dashboard-configured build settings), or whether Netlify onboarding (site creation, connecting this repo, first successful build) needs to happen before anything here can run at all.
2. Assumption A1: after adding netlify.toml + netlify/functions/, deploy and check the Netlify dashboard's Functions tab — confirm the build succeeds, `send-call-reminders` appears in BOTH the general Functions list and the Scheduled Functions panel (proving the hand-written function coexists with the Next.js Runtime's own auto-generated functions), and that the existing app itself still builds/deploys/serves correctly (no regression from adding netlify.toml).

Then: set `CRON_SECRET` (the exact value generated into `.env.local` in Task 4) and `SITE_URL` (this site's real deployed origin) as environment variables in the Netlify dashboard (Site configuration -> Environment variables), scoped to at least the Functions context. Use the Scheduled Functions panel's "Run now" (or `netlify functions:invoke send-call-reminders` via the newly-installed `netlify-cli`) to manually trigger the function once and confirm it reaches the internal route successfully (check the function's logs for the absence of the `console.error` trigger-failed line, and/or a 200 in the internal route's own logs).
  </action>
  <how-to-verify>
Netlify dashboard: Functions tab shows send-call-reminders listed (both general + scheduled panels) after a successful deploy. Environment variables page shows CRON_SECRET and SITE_URL set. "Run now" (or CLI invoke) produces no error in the function's logs and a success response from the internal route.
  </how-to-verify>
  <verify>
    <automated>MISSING — requires a real Netlify deploy + dashboard access, not automatable from this environment</automated>
  </verify>
  <done>Netlify site confirmed live for this repo (or newly onboarded); send-call-reminders function confirmed present post-deploy with no build regression; CRON_SECRET/SITE_URL confirmed set in Netlify's dashboard; a manual "Run now" invocation confirmed the trigger reaches the internal route successfully.</done>
  <resume-signal>Type "netlify verified" once confirmed, or describe what broke (e.g. "site not connected yet", "function missing from dashboard", "build failed") so it can be triaged before relying on the 5-minute cron in production.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Shared participant picker + revert new-call-form.tsx to pure start-now flow</name>
  <files>app/_components/participant-picker.tsx, app/_components/new-call-form.tsx</files>
  <action>
Per BTN-02 (remove the buried inline schedule checkbox entirely — exactly one way to schedule a call going forward, the new dedicated modal from Task 8) and the discretionary choice to extract shared picker logic (the search-input + candidate-list + checkbox-toggle block is near-identical between `new-call-form.tsx` and the not-yet-created `schedule-call-form.tsx`, so extracting is cleaner than duplicating it).

1. Create `app/_components/participant-picker.tsx` (`'use client'`): export `type PersonOption = { id: string; name: string; role: string }` and a default-exported `ParticipantPicker({ allUsers, picked, onToggle }: { allUsers: PersonOption[]; picked: Set<string>; onToggle: (id: string) => void })` component. Internally owns its own `query` search-input state (`useState('')`) and a `useMemo`-derived `candidates` filtered list (`allUsers.filter(u => u.name.toLowerCase().includes(query.trim().toLowerCase()))`) — extract this byte-for-byte in markup/classes from `new-call-form.tsx`'s current implementation (label "Who's on the call?", search input, `max-h-48` scrollable checkbox list, "No matching people." empty state, `picked.has(u.id)` highlight styling), just parameterized by the `picked`/`onToggle` props instead of owning `picked` state itself (each consumer needs its own submit-time access to the picked set, so that state stays with the consumer).

2. Refactor `app/_components/new-call-form.tsx`:
   - Remove the `isAdmin` prop, the `schedule`/`scheduledFor` state, and the entire `{isAdmin && (...)}` "Schedule for later" block.
   - Replace the inline search-input + candidate-list JSX with `<ParticipantPicker allUsers={allUsers} picked={picked} onToggle={toggle} />` (keep `picked`/`toggle` owned locally in `new-call-form.tsx` exactly as today — only the rendering moves).
   - In `submit()`, remove all `willSchedule`/`scheduledFor` handling — the payload to `createVideoCallAction` becomes unconditionally `{ title: title.trim() || undefined, participantUserIds: [...picked] }`, never including `scheduledFor`. Revert the submit button label to always read `pending ? 'Starting…' : \`Start call${picked.size ? \` (${picked.size + 1})\` : ''}\`` (drop the `isAdmin && schedule` conditional entirely).
   - The component's props type becomes `{ allUsers: PersonOption[] }` only (no `isAdmin`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "participant-picker\.tsx|new-call-form\.tsx" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && npm run lint 2>&1 | grep -iE "participant-picker|new-call-form" | grep -viE "warning" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && ! grep -q "Schedule for later" app/_components/new-call-form.tsx && echo NEWCALLFORM_REVERT_OK</automated>
  </verify>
  <done>new-call-form.tsx contains no "Schedule for later" checkbox, no isAdmin prop, no scheduledFor state, and always submits without scheduledFor — a pure start-now flow for every role; participant-picker.tsx exists and is used by new-call-form.tsx with identical visible behavior to before the refactor; tsc/lint clean.</done>
</task>

<task type="auto">
  <name>Task 8: Schedule Call button + modal, wired into /calls</name>
  <files>app/_components/schedule-call-form.tsx, app/(app)/calls/page.tsx</files>
  <action>
Per BTN-01 (locked decisions: always-visible admin-only button next to "Start a call"; opens a MODAL not an inline expand; native `<input type="date">` + `<input type="time">` satisfy "clock UI", no new dependency; reuses `new-call-form.tsx`'s picker logic via Task 7's `ParticipantPicker`).

1. Create `app/_components/schedule-call-form.tsx` (`'use client'`): props `{ allUsers: PersonOption[] }` (import `PersonOption` from `./participant-picker`). Local state: `open` (modal visibility, default `false`), `title`, `date` (bound to `<input type="date">`), `time` (bound to `<input type="time">`), `picked: Set<string>` + `toggle` (same pattern as `new-call-form.tsx`), `pending` (`useTransition`), `message`.
   - Collapsed state (`!open`): render a button styled like `new-call-form.tsx`'s collapsed "Start a call" button (`border border-primary ... text-primary`, a `material-symbols-outlined` icon distinct from the video-camera one — e.g. `event`) reading "Schedule Call", `onClick` sets `open` to `true`.
   - Open state: render a modal overlay (`fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4`, clicking the backdrop calls a `close()` that hides the modal and resets all local state) matching this codebase's existing modal convention (see `admin-users-table.tsx`'s confirm-dialogs), containing a card (`w-full max-w-md rounded-xl bg-white p-6 shadow-xl`, `onClick={(e) => e.stopPropagation()}` so inner clicks don't bubble to the backdrop) with: a heading ("Schedule a video call"), the title input (same styling as `new-call-form.tsx`'s), a "Date" label + `<input type="date">`, a "Time" label + `<input type="time">` (side-by-side via a `flex gap-2` row), `<ParticipantPicker allUsers={allUsers} picked={picked} onToggle={toggle} />`, and a submit/cancel button row mirroring `new-call-form.tsx`'s (`bg-primary` submit button reading `pending ? 'Scheduling…' : 'Schedule call'`, `text-gray-400` cancel button calling `close()`).
   - `submit()`: set `message` and return without submitting if `picked.size === 0` ("Pick at least one other person to call."), or if `date`/`time` are empty, or if `new Date(\`${date}T${time}\`)` is invalid, or if that parsed date is `<= Date.now()` ("Pick a future date and time.") — mirrors `createVideoCallAction`'s own server-side check, client-side. On success, call `createVideoCallAction(getTabToken(), { title: title.trim() || undefined, participantUserIds: [...picked], scheduledFor: parsedDate.toISOString() })` — `scheduledFor` is ALWAYS set here (unlike `new-call-form.tsx`, which never sends it). On `res.status === 'success'`, `router.push(\`/calls/${res.callId}\`)`; otherwise set `message` to `res.message`.

2. Wire into `app/(app)/calls/page.tsx`:
   - Import `ScheduleCallForm` from `@/app/_components/schedule-call-form`.
   - `isAdmin` is already computed (`isAdminRole(role)`) — remove the now-unused `isAdmin` prop from `<NewCallForm allUsers={allUsers} />` (Task 7 dropped that prop) and render `{isAdmin && <ScheduleCallForm allUsers={allUsers} />}` directly alongside it — wrap both in a shared `flex flex-wrap items-start gap-3` row so the pair reads as one row (adjust either button's own `mb-6` as needed so spacing doesn't double up), placing "Schedule Call" next to "Start a call" per the locked decision.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "schedule-call-form\.tsx|app/\(app\)/calls/page\.tsx" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && npm run lint 2>&1 | grep -iE "schedule-call-form|calls/page" | grep -viE "warning" | grep -v '^#' | wc -l | tr -d ' ' | grep -qx 0 && echo SCHEDULE_MODAL_OK</automated>
  </verify>
  <done>An admin-role user sees an always-visible "Schedule Call" button next to "Start a call" on /calls; clicking it opens a modal (native date+time inputs + reused ParticipantPicker); submitting with a future date/time and >=1 other participant calls createVideoCallAction with scheduledFor always set and redirects to the new call room; a non-admin sees no Schedule Call button at all; tsc/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Netlify Scheduled Function -> app/api/cron/call-reminders/route.ts | Machine-to-machine, over the public internet — the route is a normal HTTP endpoint even though its caller isn't user-facing |
| Browser (schedule-call-form.tsx) -> createVideoCallAction | Untrusted client input: scheduledFor, participant list — already validated server-side by the unchanged createVideoCallAction (260724-alz) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-dw4-01 | Spoofing | app/api/cron/call-reminders/route.ts | mitigate | Bearer CRON_SECRET compared exactly; 401 on missing/mismatched header or unset env var — covered by Task 4's new tests |
| T-dw4-02 | Denial of Service / Repudiation | lib/video-calls.ts sendDueCallReminders, invoked every 5 minutes | mitigate | reminderSentAt gate makes repeated invocation idempotent — a call is notified exactly once no matter how many times the cron fires while inside the window — covered by Task 2's tests |
| T-dw4-03 | Information Disclosure | .env.local CRON_SECRET/SITE_URL | mitigate | Gitignored file; generated value never echoed/logged per constraints; Netlify dashboard env vars are the production source of truth (Task 6) |
| T-dw4-04 | Elevation of Privilege | app/_components/schedule-call-form.tsx (client-only isAdmin gate) | accept | Server-side isAdminRole gate on scheduledFor already exists in createVideoCallAction (260724-alz, unchanged) — a tampered request from a non-admin is rejected there regardless of this new client-only button's visibility |
| T-dw4-SC | Tampering | npm install @netlify/functions / netlify-cli | mitigate | Task 3 blocking human-verify checkpoint (both [ASSUMED] per RESEARCH.md's Package Legitimacy Audit) before install |
</threat_model>

<verification>
- `npx tsc --noEmit` passes project-wide (or at minimum zero errors in every file listed in `files_modified`).
- `npm run lint` passes for every touched file.
- `npx vitest run tests/lib/video-calls.test.ts tests/app/api/cron-call-reminders.test.ts` passes (existing + new cases); `tests/actions/video-calls.test.ts` and 260724-alz's own `scheduledFor` cases remain untouched and green.
- `npm run db:push` was run against the live Neon DB and confirmed idempotent on a second consecutive run.
- Task 3 and Task 6 checkpoints both resumed with a positive outcome before this work is considered production-ready.
- Manual smoke (dev server): as an admin, click "Schedule Call", fill the modal, submit with a future date/time — confirm redirect to the new call room and that it appears under "Scheduled" on /calls exactly as before; as a non-admin, confirm no "Schedule Call" button and that "Start a call" behaves exactly as before (no schedule checkbox anywhere).
</verification>

<success_criteria>
- All 8 tasks' `<done>` criteria met, including both checkpoints resumed.
- Every `must_haves` truth is observable in the running app (post-deploy, for the Netlify-dependent truths).
- No duplication of DB/GetStream/notification logic inside the Netlify function — it stays a thin trigger.
- Exactly one way to schedule a call exists in the UI going forward (the modal); `new-call-form.tsx` is a pure start-now flow for every role.
- A scheduled call's invitees are reminded exactly once, 1 hour before start, regardless of how many times the 5-minute cron fires in that window.
</success_criteria>

<output>
Create `.planning/quick/260726-dw4-schedule-call-button-modal-and-1-hour-be/260726-dw4-SUMMARY.md` when done.
</output>
