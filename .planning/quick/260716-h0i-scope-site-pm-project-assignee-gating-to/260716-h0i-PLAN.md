---
phase: quick-260716-h0i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/workflow-graph.ts
  - lib/my-work.ts
  - actions/checklists.ts
  - actions/readiness.ts
  - actions/workflow.ts
  - tests/lib/workflow-graph-assignee-gate.test.ts
  - tests/actions/readiness.test.ts
  - tests/actions/workflow.test.ts
autonomous: true
requirements: [SEC-ASSIGNEE-GATE]
must_haves:
  truths:
    - "Only the site_pm assigned via 'ops_design_confirmation' can act on confirmation, materials_readiness (site_pm half), installation_process, and sign_off for that project"
    - "A factory_pm acting on their own half of the dual-role materials_readiness step is completely unaffected by the site_pm assignment gate"
    - "The 6 pre-existing assigneeGoverningStepKey assertions continue to pass unmodified"
    - "sign_off is enforced automatically via the existing generic authorizeStep once the map entry is added (no changes to actions/workflow-graph.ts)"
  artifacts:
    - path: "lib/workflow-graph.ts"
      provides: "Extended ASSIGNEE_GATED_STEPS map + assigneeGatedRole() export"
      contains: "assigneeGatedRole"
    - path: "lib/my-work.ts"
      provides: "Role-scoped gate resolution in getMyWork"
      contains: "assigneeGatedRole(step.key) === role"
  key_links:
    - from: "actions/checklists.ts, actions/readiness.ts, actions/workflow.ts"
      to: "lib/workflow-graph.ts"
      via: "assigneeGatedRole + getStepAssigneeGate"
      pattern: "assigneeGatedRole\\(step.key\\) === role"
---

<objective>
Extend the existing hardcoded `ASSIGNEE_GATED_STEPS` mechanism so the specific
site_pm assigned via the `ops_design_confirmation` step ("Assign Site PM for
Site Confirmation") — and only that person — can act on every remaining
site_pm-gated step for that project: `confirmation`, `materials_readiness`
(site_pm half only), `installation_process`, and `sign_off`. Today any site_pm
role-holder can act on these project-wide; this closes that authorization gap.

Purpose: Real server-side authorization, not just a "your turn" visibility hint.
Output: Extended gate map with a role-scoping field, enforced at every relevant
server-action call site, plus test coverage proving the dual-role safety property.

The design is fully diagnosed and specified — apply exactly what is written, do
not re-derive or re-diagnose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Existing gate mechanism (extend, do not rebuild)
@lib/workflow-graph.ts
@lib/my-work.ts

# Enforcement call sites to extend
@actions/checklists.ts
@actions/readiness.ts
@actions/workflow.ts

# Tests to extend (do not modify existing assertions)
@tests/lib/workflow-graph-assignee-gate.test.ts
@tests/actions/readiness.test.ts
@tests/actions/workflow.test.ts

<interfaces>
Confirmed current shapes:
- lib/workflow-graph.ts (~line 133): `const ASSIGNEE_GATED_STEPS: Record<string, string>` with 3 entries (brief_taking, kickoff_meeting, design_stage). `assigneeGoverningStepKey(stepKey): string | null`. `getStepAssigneeGate(graph, projectId, stepKey): Promise<string | null>`. `WorkflowRole` is already imported in this file — reuse it.
- lib/my-work.ts: `getMyWork(role, userId)` — gate loop at ~lines 86-96; `pending` filter later does `if (gate && gate !== userId) return false`; `activeProjects[].gatedToUserId` consumed by client components.
- actions/checklists.ts:14 `import { getLiveWorkflowSteps } from '@/lib/workflow-graph'`; gate block at ~line 93 (`if (projectId && input?.expectedStepN)`) checks `canActOnGraphStep`. `userId` is in scope.
- actions/readiness.ts:8 `import { getLiveWorkflowSteps } from '@/lib/workflow-graph'`; gate block at ~line 67 (`if (input?.projectId && input?.expectedStepN)`). `userId` in scope.
- actions/workflow.ts:9 `import { getLiveWorkflowSteps } from '@/lib/workflow-graph'`; `advanceProjectStep` role gate at ~line 41 (`if (!canRoleActOnStep(step.role, role as UserRole)) return false`); `confirmDualRoleStepAs` dualRoles gate at ~line 120 (`if (!(step.dualRoles as string[]).includes(role)) { return ... }`). `projectId` and `userId` in scope in both.
- tests/actions/readiness.test.ts:3 hoists `getLiveStepsMock`; :19 `vi.mock('@/lib/workflow-graph', () => ({ getLiveWorkflowSteps: getLiveStepsMock }))`; :27 default `verifyMock` resolves `{ userId: 'f1', role: 'factory_pm' }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend gate map with role-scoping + role-scope my-work resolution</name>
  <files>lib/workflow-graph.ts, lib/my-work.ts, tests/lib/workflow-graph-assignee-gate.test.ts</files>
  <behavior>
    - assigneeGoverningStepKey('confirmation'|'materials_readiness'|'installation_process'|'sign_off') → 'ops_design_confirmation'
    - assigneeGoverningStepKey('ops_design_confirmation') → null (assignment step itself not gated)
    - assigneeGatedRole('confirmation'|'materials_readiness') → 'site_pm'
    - assigneeGatedRole('brief_taking') → 'design'
    - assigneeGatedRole('invoice_upload') → null
    - The 6 existing assigneeGoverningStepKey assertions still pass unmodified.
  </behavior>
  <action>
In lib/workflow-graph.ts, restructure `ASSIGNEE_GATED_STEPS` from `Record<string, string>` to `Record<string, { governingKey: string; gatedRole: WorkflowRole }>`. Keep the 3 design entries (brief_taking/kickoff_meeting/design_stage governed by assign_designer_brief/design_initiation, gatedRole 'design'). Add 4 new entries — confirmation, materials_readiness, installation_process, sign_off — each `{ governingKey: 'ops_design_confirmation', gatedRole: 'site_pm' }` with the explanatory comment from the spec noting materials_readiness is dual-role and the gate applies ONLY to the site_pm party. Update `assigneeGoverningStepKey` to read `?.governingKey ?? null` WITHOUT changing its exported signature/behavior. Add a new export `assigneeGatedRole(stepKey: string): WorkflowRole | null` returning `?.gatedRole ?? null`. Reuse the existing `WorkflowRole` import (do not add a duplicate). Grep this file to confirm no other internal reference to `ASSIGNEE_GATED_STEPS`'s old shape breaks.

In lib/my-work.ts, add `assigneeGatedRole` to the existing import from '@/lib/workflow-graph'. In the `gateByProjectId` loop (~lines 86-96), change the gate condition to also require `assigneeGatedRole(step.key) === role` so an ungated role (e.g. factory_pm on materials_readiness) never sees a gate that isn't theirs. Touch nothing else — the later `pending` filter and `activeProjects[].gatedToUserId` propagate correctly since `gate` becomes null for non-gated viewers.

In tests/lib/workflow-graph-assignee-gate.test.ts, do NOT modify the 6 existing assertions. Add the new assigneeGoverningStepKey cases (confirmation, materials_readiness, installation_process, sign_off → 'ops_design_confirmation'; ops_design_confirmation → null) and assigneeGatedRole cases (confirmation → 'site_pm', materials_readiness → 'site_pm', brief_taking → 'design', invoice_upload → null). Import assigneeGatedRole in the test's dynamic import alongside assigneeGoverningStepKey.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx vitest run tests/lib/workflow-graph-assignee-gate.test.ts</automated>
  </verify>
  <done>All existing + new assertions pass; assigneeGatedRole exported; my-work gate condition role-scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the assignee gate at all server-action call sites</name>
  <files>actions/checklists.ts, actions/readiness.ts, actions/workflow.ts, tests/actions/readiness.test.ts, tests/actions/workflow.test.ts</files>
  <behavior>
    - Assigned site_pm (getStepAssigneeGate returns caller's userId) → success.
    - Unassigned site_pm (getStepAssigneeGate returns a different userId) → error, no DB insert / no advance.
    - factory_pm on dual-role materials_readiness (assigneeGatedRole returns 'site_pm', caller role 'factory_pm') → success regardless of gate userId — the single most important dual-role safety case.
    - gatedRole null or !== role → gate is a no-op.
  </behavior>
  <action>
In each of the 3 action files, extend the existing step-linked gate block: AFTER the existing `canActOnGraphStep`/`canRoleActOnStep`/dualRoles check passes and BEFORE any DB write, add:
`if (assigneeGatedRole(step.key) === role) { const gateUserId = await getStepAssigneeGate('live', <projectId>, step.key); if (gateUserId && gateUserId !== userId) { <reject> } }`

- actions/checklists.ts (~line 93 block): projectId is `projectId`; reject with `return { status: 'error', message: 'This step is assigned to a specific Site PM for this project.' }`.
- actions/readiness.ts (~line 67 block): projectId is `String(input.projectId)`; same error return.
- actions/workflow.ts `advanceProjectStep` (after ~line 41): projectId is `projectId`; reject with `return false`.
- actions/workflow.ts `confirmDualRoleStepAs` (after ~line 120): projectId is `projectId`; reject with `return { ok: false, advanced: false, message: 'This step is assigned to a specific Site PM for this project.' }`.

In each file, extend the existing `import { getLiveWorkflowSteps } from '@/lib/workflow-graph'` line to also import `assigneeGatedRole, getStepAssigneeGate`.

In tests/actions/readiness.test.ts, extend the SAME `vi.mock('@/lib/workflow-graph', ...)` to also export hoisted `assigneeGatedRoleMock` and `getStepAssigneeGateMock`. Add cases: (a) assigned site_pm — role 'site_pm', assigneeGatedRoleMock returns 'site_pm', getStepAssigneeGateMock resolves the caller's userId → status 'success'; (b) unassigned site_pm — same but getStepAssigneeGateMock resolves a DIFFERENT userId → status 'error', insertValuesMock NOT called; (c) DUAL-ROLE SAFETY (most important) — step dualRoles ['factory_pm','site_pm'], assigneeGatedRoleMock returns 'site_pm', caller role 'factory_pm' → status 'success' even though getStepAssigneeGateMock would resolve a different userId, since assigneeGatedRole(step.key) === role is false. Set caller userId/role via verifyMock as needed per case.

In tests/actions/workflow.test.ts, extend the existing dbMock/workflow-graph mocks analogously and add assigned/unassigned/dual-role-unaffected cases for advanceProjectStep (mirroring the readiness pattern). If a confirmDualRoleStepAs describe block exists, add analogous cases there too; if adding them does not fit the budget, note it as a gap in SUMMARY.md rather than silently skipping.

Do NOT create a test file for actions/checklists.ts (out of scope; readiness.test.ts is the primary proof since both share the identical gate pattern).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx vitest run tests/actions/readiness.test.ts tests/actions/workflow.test.ts</automated>
  </verify>
  <done>Gate enforced in all 4 functions; readiness + workflow tests pass including the factory_pm dual-role-unaffected case.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification sweep</name>
  <files>(no source edits — verification only)</files>
  <action>
Run the full verification suite since this task edits a shared helper (lib/workflow-graph.ts) consumed by many tests/consumers: `npx tsc --noEmit`, then `npm run lint`, then the FULL `npx vitest run` (not just touched files — a full sweep catches regressions in other consumers, mirroring how quick task 260716-g5l caught mock-shape issues). Fix any regression surfaced (e.g. a test mocking @/lib/workflow-graph that now needs the new exports). Do NOT touch actions/workflow-graph.ts, app/(app)/workflow/step/page.tsx, or any client component — those are confirmed to work automatically once the map is extended. Record any deferred gap (e.g. unwritten confirmDualRoleStepAs test cases) in SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && npx vitest run</automated>
  </verify>
  <done>tsc clean, lint clean, full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | An authenticated site_pm submits a checklist/readiness/advance for a project they were never assigned to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h0i-01 | Elevation of Privilege | submitChecklistAction / submitReadinessAction / advanceProjectStep / confirmDualRoleStepAs | mitigate | Server-side assignee gate: reject when `assigneeGatedRole(step.key) === role` and the recorded assignee for `ops_design_confirmation` differs from the caller's userId, before any DB write |
| T-h0i-02 | Spoofing | Unassigned site_pm impersonating the project's owner | mitigate | userId sourced from verifySession (server), compared to persisted assignedUserId; never trusts client-supplied identity |
| T-h0i-03 | Denial of Service | Gate incorrectly locking out a legitimate factory_pm on dual-role materials_readiness | accept (mitigated by test) | gatedRole scoping ensures the gate applies only to site_pm; dedicated dual-role-safety test proves factory_pm is unaffected |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run lint` clean
- Full `npx vitest run` green (all existing + new tests)
- 6 pre-existing assigneeGoverningStepKey assertions unmodified and passing
</verification>

<success_criteria>
- Only the assigned site_pm can act on confirmation, materials_readiness (site_pm half), installation_process, sign_off for a given project
- factory_pm unaffected on the dual-role materials_readiness step (proven by test)
- sign_off enforced with zero changes to actions/workflow-graph.ts or the step page
- No client components, schema, or migrations touched
</success_criteria>

<output>
Create `.planning/quick/260716-h0i-scope-site-pm-project-assignee-gating-to/260716-h0i-SUMMARY.md` when done
</output>
