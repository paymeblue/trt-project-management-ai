---
phase: quick-260713-c0d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_components/workflow-configurator-editor.tsx
autonomous: true
requirements: [QUICK-260713-c0d]
must_haves:
  truths:
    - "List view shows an up (↑) and down (↓) icon button on every step row"
    - "Clicking up/down reorders the step immediately with one click — no drag, no confirm modal"
    - "Each row has a 'move to position #' number input that jumps the step to an arbitrary 1-based position"
    - "The up button is disabled on the first step; the down button is disabled on the last step"
    - "Branch/join steps still only change display position (server guarantee via moveConfigStepToIndexAction is untouched)"
    - "No native HTML5 drag-and-drop remains in the list view"
  artifacts:
    - path: "app/_components/workflow-configurator-editor.tsx"
      provides: "List-view step reordering via up/down buttons + move-to-position input"
      contains: "keyboard_arrow_up"
  key_links:
    - from: "app/_components/workflow-configurator-editor.tsx"
      to: "moveConfigStepToIndexAction"
      via: "startTransition async call with computed 0-based targetIndex"
      pattern: "moveConfigStepToIndexAction\\(graph"
---

<objective>
Replace the Workflow Configurator list-view's native HTML5 drag-and-drop step reordering
(clunky over ~23 tall rows, and requiring a separate confirm-modal click after each drop)
with the proven up/down icon-button pattern already used in `checklist-editor.tsx`, plus a
per-row "move to position #" number input for long jumps.

Purpose: Make reordering a single-click, immediate action instead of a drag-then-confirm flow.
Output: Updated `app/_components/workflow-configurator-editor.tsx` client component. No backend change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/_components/workflow-configurator-editor.tsx
@app/_components/checklist-editor.tsx

<interfaces>
<!-- Server action already exists — do NOT modify actions/workflow-config.ts -->
From actions/workflow-config.ts:
```typescript
// targetIndex is 0-based. Moves stepId to that position within the graph's ordered steps.
export async function moveConfigStepToIndexAction(
  graph: string,
  stepId: string,
  targetIndex: number,
): Promise<ConfigActionState>
export type ConfigActionState = { status: 'idle' | 'success' | 'error'; message?: string }
```

Proven up/down button pattern from app/_components/checklist-editor.tsx (ItemRow):
- Two `<button>`s stacked in a `flex flex-col`, each wrapping
  `<span className="material-symbols-outlined text-lg">keyboard_arrow_up</span>` /
  `keyboard_arrow_down`, with `title="Move up"` / `"Move down"`.
- Up button `disabled={pending || isFirst}`, down button `disabled={pending || isLast}`.
- Classes: `text-gray-400 hover:text-primary disabled:opacity-30`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove drag state and wire an immediate move-to-index handler in ConfiguratorEditor</name>
  <files>app/_components/workflow-configurator-editor.tsx</files>
  <action>
In the top-level `ConfiguratorEditor` component, delete the drag/confirm machinery that is now
unused: the `dragIndex`/`overIndex`/`pendingMove` state, the `onDrop` and `confirmMove` functions,
the drag-wrapper `<div>` around each `StepRow` (the one with `onDragOver`/`onDragLeave`/`onDrop`/
ring classes), and the entire `pendingMove` confirmation modal at the bottom of the JSX. Keep the
`moveState` state and the `<StatusNote state={moveState} />`.

Add one immediate handler used by rows:
`function moveToIndex(stepId: string, targetIndex: number)` that guards `targetIndex` into
`[0, steps.length - 1]`, returns early if the step is already at that index, and inside
`startTransition` awaits `moveConfigStepToIndexAction(graph, stepId, targetIndex)`, sets
`moveState`, and calls `refresh()` on success. Reuse the existing `pending`/`startTransition`.

Update the info banner (currently the `drag_indicator` box beginning "Drag any step by its
handle to reorder it.") to describe the new interaction, e.g. change the icon to `swap_vert` and
the bold sentence to "Use the ↑ / ↓ buttons or the position box to reorder any step." Keep the
second sentence about branch/join steps only moving display position — that server guarantee is
unchanged.

In the `steps.map(...)`, render `<StepRow>` directly (no wrapper div). Pass it the new props:
`stepIndex={i}`, `totalSteps={steps.length}`, `disabled={pending}`,
`onMoveToIndex={(target) => moveToIndex(step.id, target)}`, plus existing `step`, `stepNumber={i + 1}`,
`onSaved={refresh}`. Remove the old `dragging`/`onDragStartHandle`/`onDragEndHandle` props from the call.

Also update the `AddStepRow` heading copy that says "added at the end — drag it into place
afterward" to "added at the end — move it into place afterward".
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && grep -c "pendingMove\|dragIndex\|overIndex\|onDragStart\|onDragOver\|onDrop\|confirmMove" app/_components/workflow-configurator-editor.tsx | grep -qx 0 && echo "drag state removed"</automated>
  </verify>
  <done>No drag/confirm state or handlers remain in the file; a `moveToIndex(stepId, targetIndex)` transition handler calls `moveConfigStepToIndexAction`; the confirm modal and drag wrapper div are gone; banner and AddStepRow copy updated.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite StepRow with up/down buttons and a move-to-position input</name>
  <files>app/_components/workflow-configurator-editor.tsx</files>
  <action>
Change the `StepRow` props to: `step: GraphStep`, `stepNumber: number`, `stepIndex: number`,
`totalSteps: number`, `disabled: boolean`, `onMoveToIndex: (targetIndex: number) => void`,
`onSaved: () => void`. Remove `dragging`, `onDragStartHandle`, `onDragEndHandle` and the
`dragging`-based border/opacity classes on the outer div (use static
`border-gray-200` styling).

Replace the drag-handle block (the `draggable` `drag_indicator` span) with a controls column,
following the `checklist-editor.tsx` ItemRow pattern:
- A stacked pair of icon buttons: up button calls `onMoveToIndex(stepIndex - 1)`, titled
  "Move up", using `keyboard_arrow_up`, `disabled={disabled || stepIndex === 0}`; down button
  calls `onMoveToIndex(stepIndex + 1)`, titled "Move down", using `keyboard_arrow_down`,
  `disabled={disabled || stepIndex === totalSteps - 1}`. Use the same
  `text-gray-400 hover:text-primary disabled:opacity-30` button classes.
- Keep the existing big step-number badge and "Step N" label below the buttons.
- Below the badge, add a small "move to position" control: a compact `<input type="number">`
  (local `useState` seeded to `stepNumber`, `min={1}`, `max={totalSteps}`) plus a small "Go"
  button. On Go (and on the input's Enter keydown), parse the value, and if it is a valid number
  call `onMoveToIndex(parsedValue - 1)` (convert 1-based display to 0-based index — the parent
  clamps out-of-range). Disable the control when `disabled` is true. Give the input a narrow
  width (e.g. `w-14`) and the existing form-input styling so it stays inside the controls column.

Leave the right-hand `StepFieldsPanel` / `step.key` block unchanged.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && grep -q "keyboard_arrow_up" app/_components/workflow-configurator-editor.tsx && grep -q "keyboard_arrow_down" app/_components/workflow-configurator-editor.tsx && grep -q "onMoveToIndex" app/_components/workflow-configurator-editor.tsx && ! grep -q "draggable" app/_components/workflow-configurator-editor.tsx && echo "up/down + position input in place, no draggable"</automated>
  </verify>
  <done>StepRow renders up/down icon buttons (disabled at first/last) and a move-to-position number input that calls `onMoveToIndex` with a 0-based index; no `draggable` attribute or drag handlers remain.</done>
</task>

</tasks>

<verification>
Run from the trt-pm root, all must pass:
- `npx tsc --noEmit`
- `npm run lint`
- `npm run build`
Manual smoke (optional): in List view, up/down move a step one slot immediately with no modal; the position box jumps a step to an arbitrary slot; Graph view is unaffected.
</verification>

<success_criteria>
- List-view reordering uses single-click up/down buttons plus a move-to-position input.
- No native HTML5 drag-and-drop and no confirm modal remain in the list view.
- `moveConfigStepToIndexAction` is called with a correctly computed 0-based `targetIndex`.
- `actions/workflow-config.ts` is unchanged (no backend edits).
- `tsc --noEmit`, `eslint`, and `npm run build` all pass.
</success_criteria>

<output>
Create `.planning/quick/260713-c0d-replace-the-workflow-configurator-s-list/260713-c0d-SUMMARY.md` when done.
</output>
