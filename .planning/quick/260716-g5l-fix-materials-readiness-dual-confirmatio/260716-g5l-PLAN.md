---
phase: quick-260716-g5l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/readiness.ts
  - tests/actions/readiness.test.ts
  - actions/workflow.ts
  - tests/actions/workflow.test.ts
autonomous: true
requirements: [QUICK-260716-g5l]
must_haves:
  truths:
    - "A step-linked readiness submission from an unauthorized role is rejected and persists nothing"
    - "A dualRoles-authorized non-primary role can submit a step-linked readiness form"
    - "Non-step-linked readiness submissions are unaffected by the new gate"
    - "Two simultaneous dual-role confirmations both persist (no lost update) via an atomic DB upsert"
    - "The last dual-role confirmation advances the project; earlier ones do not"
    - "Full test suite (readiness + workflow) passes with zero regressions"
  artifacts:
    - path: "actions/readiness.ts"
      provides: "Server-side authorization gate for step-linked readiness submissions"
      contains: "canActOnGraphStep"
    - path: "actions/workflow.ts"
      provides: "Atomic dual-role confirmation upsert"
      contains: "array_append"
    - path: "tests/actions/readiness.test.ts"
      provides: "Coverage for the readiness auth gate"
    - path: "tests/actions/workflow.test.ts"
      provides: "Coverage for confirmDualRoleStepAs atomicity + advancement"
  key_links:
    - from: "actions/readiness.ts"
      to: "lib/workflow-graph.ts getLiveWorkflowSteps"
      via: "server-side step lookup"
      pattern: "getLiveWorkflowSteps"
    - from: "actions/workflow.ts confirmDualRoleStepAs"
      to: "workflowStepStates unique(projectId, stepDefId)"
      via: "onConflictDoUpdate with array_append CASE expression"
      pattern: "array_append"
---

<objective>
Close the two remaining reliability gaps in the materials_readiness dual-confirmation step (workflow step 17), mirroring fixes already shipped in quick tasks 260716-c6s and 260716-djj.

Purpose: Prevent unauthorized readiness form persistence and eliminate the lost-update race that can strand a project at the dual-confirmation step forever.
Output: Authorization gate in `submitReadinessAction`, atomic upsert in `confirmDualRoleStepAs`, and matching test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Do NOT re-explore. -->

Mirror source — actions/checklists.ts submitChecklistAction gate (already shipped in 260716-djj):
```ts
const { userId, role } = await verifySession()
// ...
const steps = await getLiveWorkflowSteps()
const step = findStep(steps, Number(input.expectedStepN))
if (!step || step.slug !== slug || !canActOnGraphStep(step, role as UserRole)) {
  return { status: 'error', message: 'You are not authorized to submit this checklist for this step.' }
}
```
Note: readiness has NO `slug` field on ReadinessInput — omit the `step.slug !== slug` comparison.

From lib/workflow.ts:
```ts
export function findStep<T extends WorkflowStep>(steps: T[], n: number): T | undefined
export function canActOnGraphStep(step, userRole: UserRole): boolean  // honors step.role AND step.dualRoles
export type UserRole  // 'factory_pm' | 'site_pm' | 'super_admin' | 'design' | ...
```

From lib/workflow-graph.ts:
```ts
export async function getLiveWorkflowSteps(): Promise<LiveWorkflowStep[]>
// LiveWorkflowStep carries: n, key, label, role, kind, slug?, stepDefId, dualRoles?: WorkflowRole[] | null
```

From db/schema.ts (already verified — do NOT re-check):
```ts
workflowStepStates.confirmedRoles = text('confirmed_roles').array()   // plain text[], no enum cast needed
// unique().on(t.projectId, t.stepDefId)  — valid onConflictDoUpdate target
```

actions/workflow.ts already imports `and, eq` from 'drizzle-orm' and `getLiveWorkflowSteps` from '@/lib/workflow-graph'. Extend the drizzle-orm import to add `sql`; do NOT add a duplicate import line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add authorization gate to submitReadinessAction + tests</name>
  <files>actions/readiness.ts, tests/actions/readiness.test.ts</files>
  <behavior>
    - Step-linked submission (has projectId AND expectedStepN) from a role NOT authorized for the live step → status 'error', zero DB writes (insertValuesMock NOT called).
    - Step-linked submission from a dualRoles-authorized non-primary role (step.role='factory_pm', dualRoles=['factory_pm','site_pm'], caller='site_pm') → status 'success'.
    - Non-step-linked submission (no projectId/expectedStepN) → gate skipped, existing behavior unchanged.
    - Existing 'advances the workflow step when launched from a project' test still returns status 'success', advanced true.
  </behavior>
  <action>
    In actions/readiness.ts: extend imports to add `import { getLiveWorkflowSteps } from '@/lib/workflow-graph'` and add `findStep, canActOnGraphStep, type UserRole` imported from '@/lib/workflow'. Change `const { userId } = await verifySession()` to `const { userId, role } = await verifySession()`. Immediately after the digital-mode signature validation block and BEFORE the `try { await db.insert(readinessForms)... }` block, insert a gate: when `input?.projectId && input?.expectedStepN`, call `getLiveWorkflowSteps()`, resolve the step via `findStep(steps, Number(input.expectedStepN))`, and if the step is missing or `canActOnGraphStep(step, role as UserRole)` is false, return `{ status: 'error', message: 'You are not authorized to submit this form for this step.' }`. Do NOT compare a slug (ReadinessInput has none). Do NOT modify the photo/signature validation, the readinessForms insert, or the advanceOrConfirmDualRole call.

    In tests/actions/readiness.test.ts: add `getLiveStepsMock: vi.fn()` to the `vi.hoisted(...)` return, and add `vi.mock('@/lib/workflow-graph', () => ({ getLiveWorkflowSteps: getLiveStepsMock }))`. In `beforeEach`, add `getLiveStepsMock.mockResolvedValue([{ n: 3, key: 'test_step', label: 'Test', role: 'factory_pm', kind: 'readiness', slug: undefined, stepDefId: 'stepdef-3', dualRoles: null }])` so the existing 'advances the workflow step...' test (caller role 'factory_pm', step 3) stays authorized. Do NOT reconstruct a full db.select chain — the module mock is sufficient. Add three new test cases: (a) unauthorized role for a step-linked submission (step role 'factory_manager', caller 'factory_pm', projectId 'p1' + expectedStepN 3) → res.status 'error' AND insertValuesMock NOT called; (b) dualRoles-authorized non-primary role (step role 'factory_pm', dualRoles ['factory_pm','site_pm'], caller overridden to role 'site_pm') → res.status 'success'; (c) confirm an existing no-projectId test still passes unmodified.
  </action>
  <verify>
    <automated>npx vitest run tests/actions/readiness.test.ts</automated>
  </verify>
  <done>readiness.test.ts passes; unauthorized step-linked submission returns error with zero DB writes; dualRoles non-primary role succeeds; non-step-linked submissions unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make confirmDualRoleStepAs atomic + tests</name>
  <files>actions/workflow.ts, tests/actions/workflow.test.ts</files>
  <behavior>
    - First confirmation (caller 'factory_pm', .returning resolves [{confirmedRoles:['factory_pm']}]) → { ok:true, advanced:false }; the onConflictDoUpdate set.confirmedRoles is a Drizzle SQL fragment, not a plain array.
    - Second confirmation (.returning resolves [{confirmedRoles:['factory_pm','site_pm']}]) → { ok:true, advanced:true }; project currentStep updated to 18.
    - Unauthorized role ('design', not in dualRoles) → { ok:false, advanced:false, message:'Not your step.' } with no workflowStepStates write.
    - All 6 existing advanceProjectStep tests still pass after the mock restructuring.
  </behavior>
  <action>
    In actions/workflow.ts: extend the existing `import { and, eq } from 'drizzle-orm'` to also import `sql` (single import line, no duplicate). In `confirmDualRoleStepAs`, replace the SELECT-then-JS-push-then-upsert block (the `const [existing] = ...` select, the JS `confirmedRoles.push(role)`, and the `await db.insert(workflowStepStates)...onConflictDoUpdate({ set: { confirmedRoles, ... } })`) with a single atomic upsert: `.values({ ..., confirmedRoles: [role], ... })` and `.onConflictDoUpdate({ target: [...], set: { confirmedRoles: sql\`CASE WHEN ${role} = ANY(${workflowStepStates.confirmedRoles}) THEN ${workflowStepStates.confirmedRoles} ELSE array_append(${workflowStepStates.confirmedRoles}, ${role}) END\`, actedBy: userId, updatedAt: new Date() } })` chained with `.returning({ confirmedRoles: workflowStepStates.confirmedRoles })`. Read the returned row into `const confirmedRoles = row?.confirmedRoles ?? [role]`, then compute `allConfirmed` as before. Everything after (the `!allConfirmed` branch, the complete update, projectStepCompletions insert, projects.currentStep update, revalidateBoards, return) stays EXACTLY as-is. Use the parameterized `sql` tag only — never string-concatenate role.

    In tests/actions/workflow.test.ts: restructure the `insert` mock to be table-aware so it supports BOTH the plain awaited `.values(...)` (projectStepCompletions) AND the chained `.values(...).onConflictDoUpdate(...).returning(...)` (workflowStepStates) without breaking the existing advanceProjectStep assertions on insertValuesMock. Import `workflowStepStates` from '@/db/schema' in the test file and branch `insert: (table) => table === workflowStepStates ? { values: workflowStepStatesInsertMock } : { values: insertValuesMock }`. Add `onConflictDoUpdateMock` (returns `{ returning: returningMock }`) and `returningMock` to the hoisted block. Patch a dualRoles field onto the step-17 (materials_readiness) fixture row — `const dualRoleStepDefRows = liveStepDefRows.map((r) => r.stepKey === 'materials_readiness' ? { ...r, dualRoles: ['factory_pm','site_pm'] } : r)` — and use `selectOrderByMock.mockResolvedValue(dualRoleStepDefRows)` in the new dual-role tests. Add a new `describe('confirmDualRoleStepAs', ...)` block with three cases matching the behavior section: first-confirmation (assert onConflictDoUpdateMock called with set.confirmedRoles being a SQL fragment — e.g. has a `queryChunks` property, NOT Array.isArray true), second-confirmation-advances (setMock called with currentStep 18), and unauthorized-role rejected before any write. Confirm all existing advanceProjectStep tests still pass.
  </action>
  <verify>
    <automated>npx vitest run tests/actions/workflow.test.ts</automated>
  </verify>
  <done>workflow.test.ts passes; confirmDualRoleStepAs uses an atomic array_append CASE upsert; new dual-role tests pass; all 6 advanceProjectStep tests pass unmodified in behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification sweep</name>
  <files>(no source changes — verification only)</files>
  <action>
    Run the full verification gates to confirm zero regressions from the mock restructuring: TypeScript typecheck, ESLint on changed files, and the FULL vitest suite (not just the two touched files). If any gate fails, fix the offending change in Task 1/2 files only and re-run. Do NOT touch actions/checklists.ts, app/(app)/checklists/[slug]/page.tsx, app/(app)/factory-pm/readiness/page.tsx, or requiredPosition/approval_installation code.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run</automated>
  </verify>
  <done>tsc clean, lint clean, full vitest suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/actions/readiness.test.ts` — auth gate coverage passes.
- `npx vitest run tests/actions/workflow.test.ts` — atomic upsert + dual-role coverage passes, existing advanceProjectStep tests unregressed.
- `npx tsc --noEmit && npm run lint && npx vitest run` — full sweep green.
</verification>

<success_criteria>
- submitReadinessAction rejects unauthorized step-linked submissions before any DB write.
- confirmDualRoleStepAs computes confirmedRoles atomically at the DB via array_append CASE upsert (no JS read-then-write).
- Both fixes covered by new tests; full suite passes with zero regressions.
</success_criteria>

<output>
Create `.planning/quick/260716-g5l-fix-materials-readiness-dual-confirmatio/260716-g5l-SUMMARY.md` when done
</output>
