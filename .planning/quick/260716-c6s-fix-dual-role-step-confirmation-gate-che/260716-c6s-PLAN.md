---
phase: quick-260716-c6s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/checklists/[slug]/page.tsx
  - app/(app)/factory-pm/readiness/page.tsx
  - scripts/inspect-factory-pm-gap.ts
autonomous: true
requirements: [DUAL-ROLE-GATE]
must_haves:
  truths:
    - "A site_pm landing on the materials_readiness step (/checklists/delivery_site_readiness) can submit their half of the dual confirmation instead of hitting 'It is not your turn to act on this step.'"
    - "Both page-level gates authorize via dualRoles-aware canActOnGraphStep, matching the documented contract in lib/workflow.ts"
    - "No unused canRoleActOnStep import remains in either changed file"
    - "The throwaway investigation script scripts/inspect-factory-pm-gap.ts is deleted"
  artifacts:
    - path: "app/(app)/checklists/[slug]/page.tsx"
      provides: "dualRoles-aware step gate for the checklist wizard page"
      contains: "canActOnGraphStep(step"
    - path: "app/(app)/factory-pm/readiness/page.tsx"
      provides: "dualRoles-aware step gate for the readiness page"
      contains: "canActOnGraphStep(step"
  key_links:
    - from: "app/(app)/checklists/[slug]/page.tsx"
      to: "lib/workflow.ts canActOnGraphStep"
      via: "step authorization gate"
      pattern: "canActOnGraphStep\\(step, role"
    - from: "app/(app)/factory-pm/readiness/page.tsx"
      to: "lib/workflow.ts canActOnGraphStep"
      via: "step authorization gate"
      pattern: "canActOnGraphStep\\(step, role"
---

<objective>
Fix the dual-role step confirmation gate so a `site_pm` can confirm their half of the merged `materials_readiness` step (order 17, checklistSlug `delivery_site_readiness`, `dualRoles = ['factory_pm', 'site_pm']`).

Two page-level gates were missed during the Phase 22e migration to the dualRoles-aware check and still use the bare `canRoleActOnStep(step.role, role)`, which returns false for the second dual role. As a result a `site_pm` who is routed to `/checklists/delivery_site_readiness` hits `workflowNotice = 'It is not your turn to act on this step.'` and can never submit — even though the backend action (`confirmDualRoleStepAs`) already accepts any role in `dualRoles`. This is the confirmed, reproducible bug the user reported.

Purpose: Unblock the second dual-role actor at both page gates, aligning them with the documented contract in `lib/workflow.ts` (~lines 263-276) already honored by lib/my-work.ts, header-project-switcher.tsx, project-steps-board.tsx, and admin/timeline/page.tsx.
Output: Two corrected gates + deletion of the throwaway investigation script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- From lib/workflow.ts — the correct dualRoles-aware gate the executor swaps to. -->
<!-- Signature is satisfied by the `step` object already in scope (findStep(...) returns WorkflowStep, -->
<!-- and WorkflowStep carries `dualRoles?: WorkflowRole[] | null` — confirmed in lib/workflow.ts line 141 -->
<!-- and populated by getLiveWorkflowSteps() in lib/workflow-graph.ts line 42/85). No exploration needed. -->

export function canActOnGraphStep(
  step: { role: WorkflowRole; dualRoles?: WorkflowRole[] | null },
  userRole: UserRole,
): boolean
// returns true if canRoleActOnStep(step.role, userRole) OR step.dualRoles includes userRole

export function canRoleActOnStep(stepRole: WorkflowRole, userRole: UserRole): boolean
// bare check — the WRONG one for "can this user act on this step as theirs"

export function canEditChecklist(...) // unrelated — must remain imported in checklists/[slug]/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap both page gates to dualRoles-aware canActOnGraphStep</name>
  <files>app/(app)/checklists/[slug]/page.tsx, app/(app)/factory-pm/readiness/page.tsx</files>
  <action>
In `app/(app)/checklists/[slug]/page.tsx` (line ~70), replace the gate condition `!canRoleActOnStep(step.role, role as UserRole)` with `!canActOnGraphStep(step, role as UserRole)`. In the import block (lines ~12-18), remove `canRoleActOnStep` and add `canActOnGraphStep`; keep `findStep`, `canEditChecklist`, and `type UserRole` intact. `canRoleActOnStep` has no other usage in this file (grep-confirmed), so it must be dropped from the import.

In `app/(app)/factory-pm/readiness/page.tsx` (line ~38), apply the identical replacement: gate becomes `!canActOnGraphStep(step, role as UserRole)`. Update the line-6 import — remove `canRoleActOnStep`, add `canActOnGraphStep`, keep `findStep` and `type UserRole`. `canRoleActOnStep` has no other usage in this file (grep-confirmed), so drop it.

Do NOT touch the out-of-scope files: actions/workflow-graph.ts, actions/bypass.ts, app/(app)/workflow/step/page.tsx (their bare check is correct — those step kinds have no dualRoles), actions/workflow.ts (already correct), or tests/lib/workflow.test.ts.
  </action>
  <verify>
    <automated>test -z "$(grep -n canRoleActOnStep 'app/(app)/checklists/[slug]/page.tsx' 'app/(app)/factory-pm/readiness/page.tsx')" && grep -q 'canActOnGraphStep(step, role' 'app/(app)/checklists/[slug]/page.tsx' && grep -q 'canActOnGraphStep(step, role' 'app/(app)/factory-pm/readiness/page.tsx' && echo GATES_OK</automated>
  </verify>
  <done>Both files call canActOnGraphStep(step, role as UserRole) at the gate; neither file references canRoleActOnStep anymore; canEditChecklist import preserved in the checklists page.</done>
</task>

<task type="auto">
  <name>Task 2: Delete throwaway investigation script and verify no regression</name>
  <files>scripts/inspect-factory-pm-gap.ts</files>
  <action>
Delete the untracked, throwaway investigation script `scripts/inspect-factory-pm-gap.ts` (used only by the orchestrator to diagnose this bug; must not be committed).

Then run verification: type-check the project (`npx tsc --noEmit`), lint the two changed files (`npx eslint 'app/(app)/checklists/[slug]/page.tsx' 'app/(app)/factory-pm/readiness/page.tsx'`), and run the workflow test suite to confirm `canRoleActOnStep` behavior is unregressed (`npx vitest run tests/lib/workflow.test.ts`). The test file exercises `canRoleActOnStep` directly and is unaffected by this change — it must still pass.
  </action>
  <verify>
    <automated>test ! -e scripts/inspect-factory-pm-gap.ts && npx tsc --noEmit && npx vitest run tests/lib/workflow.test.ts</automated>
  </verify>
  <done>Script deleted; tsc --noEmit passes; tests/lib/workflow.test.ts passes; eslint reports no new errors on the two changed files.</done>
</task>

</tasks>

<verification>
- A `site_pm` routed to `/checklists/delivery_site_readiness` for the merged readiness step no longer sees "It is not your turn to act on this step." and can submit their half.
- Both gates now defer to `canActOnGraphStep`, matching the four call sites already migrated in Phase 22e.
- `npx tsc --noEmit` clean; `tests/lib/workflow.test.ts` green; no lint regressions on changed files.
- `scripts/inspect-factory-pm-gap.ts` is gone from the working tree.
</verification>

<success_criteria>
- app/(app)/checklists/[slug]/page.tsx and app/(app)/factory-pm/readiness/page.tsx use `canActOnGraphStep(step, role as UserRole)` and no longer import or call `canRoleActOnStep`.
- `canEditChecklist` remains imported and functional in the checklists page.
- Type-check and workflow tests pass; the investigation script is deleted.
- No out-of-scope files modified.
</success_criteria>

<output>
Create `.planning/quick/260716-c6s-fix-dual-role-step-confirmation-gate-che/260716-c6s-SUMMARY.md` when done
</output>
