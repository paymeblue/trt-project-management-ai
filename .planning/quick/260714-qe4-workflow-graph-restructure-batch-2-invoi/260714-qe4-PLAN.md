---
phase: quick-260714-qe4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-workflow-restructure-batch2.ts
  - db/workflow-live-steps.ts
  - db/seed-checklists.ts
  - scripts/verify-live-workflow.ts
  - lib/workflow-graph.ts
  - lib/workflow.ts
  - actions/projects.ts
  - actions/workflow.ts
  - actions/workflow-graph.ts
  - app/_components/trt-flow-diagram.tsx
  - app/_components/dashboard-shell.tsx
autonomous: true
requirements:
  - WF-RESTRUCTURE-B2
user_setup: []

must_haves:
  truths:
    - "Live graph has 21 steps: Invoicing (customer_care, 2-phase) and Set Delivery Timeline (operations) are two distinct steps at orderIndex 4 and 5"
    - "Step 4 'Invoicing' phase 2/2 sets projects.paymentStatus='paid' when the client-paid confirmation is submitted"
    - "Set Delivery Timeline (step 5, operations) runs setInvoiceTimelineAction and creates the ops-timeline step with a +1d auto-deadline"
    - "The assign-site-pm step (stepKey ops_design_confirmation) is 'Assign Site PM for Site Confirmation' — assignment-kind, owned by Head of Projects (super_admin + head_of_projects position), targeting site_pm"
    - "The site_pm 'confirmation' checklist step sits immediately after the assign-site-pm step and before confirmation_correction"
    - "installation_readiness no longer exists in the live graph"
    - "A single 'Installation Process' checklist step (slug installation_process) replaces the former sorting + close_out steps"
    - "sign_off is a site_pm yes_no_upload step (uploads the sign-off document), not a super_admin ack"
    - "positions table contains a head_of_projects row (slug head_of_projects, label 'Head of Projects')"
    - "npm run verify:live-workflow passes against the new 21-step shape"
  artifacts:
    - path: "scripts/migrate-workflow-restructure-batch2.ts"
      provides: "Idempotent, assert-guarded, tsx-runnable migration for the whole restructure"
      contains: "GRAPH = 'live'"
    - path: "db/workflow-live-steps.ts"
      provides: "Updated 21-step canonical bootstrap reference"
      contains: "installation_process"
    - path: "scripts/verify-live-workflow.ts"
      provides: "Parity + behavioural assertions for the new shape"
    - path: "actions/projects.ts"
      provides: "setInvoiceTimelineAction wired to the new step 5 with +1d auto-deadline"
  key_links:
    - from: "actions/projects.ts (setInvoiceTimelineAction)"
      to: "new set_delivery_timeline step (orderIndex 5)"
      via: "step-key lookup + auto-deadline creation"
      pattern: "set_delivery_timeline"
    - from: "step 4 phase 2/2 handler"
      to: "projects.paymentStatus"
      via: "update projects set paymentStatus='paid'"
      pattern: "paymentStatus.*paid"
---

<objective>
Apply workflow-graph restructure batch 2 exactly as specified in the owner's
committed note `.planning/notes/2026-07-14-workflow-restructure-batch2.md`
(the authoritative, ambiguity-resolved requirements list). This un-merges the
Invoicing/Timeline step into two ownership-correct steps, converts step 8 into
a Head-of-Projects site-PM assignment, relocates the site confirmation
checklist, removes installation_readiness, folds sorting+close_out into one
Installation Process checklist, converts sign_off to a site_pm upload step,
and fixes SLA copy + auto-deadlines. The live graph goes from 22 to 21 steps.

Purpose: Bring the live production workflow into line with the owner's real
operational process and correct ownership boundaries.
Output: One migration script, updated bootstrap/parity references, engine +
action changes, UI/copy updates, and a green full-verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/notes/2026-07-14-workflow-restructure-batch2.md
@db/workflow-live-steps.ts
@lib/workflow-graph.ts
@lib/workflow.ts
@actions/projects.ts
@actions/workflow-graph.ts
@scripts/verify-live-workflow.ts
@app/_components/trt-flow-diagram.tsx

<interfaces>
<!-- Extracted from the codebase — use directly, no re-exploration needed. -->

Current live graph (db/workflow-live-steps.ts, 22 steps, in order):
  1 new_project (customer_care, creation)
  2 assign_designer_brief (design, assignment)
  3 brief_taking (design, yes_no_upload)
  4 invoice_upload "Invoice & Delivery Timeline" (operations, yes_no_upload; live DB row carries additionalKinds=[timeline_setting] -> 2-part wizard)
  5 design_initiation (design, assignment)
  6 kickoff_meeting (design, yes_no_upload)
  7 design_stage (design, yes_no_upload)
  8 ops_design_confirmation (operations, yes_no_upload)
  9 confirmation_correction (design, yes_no_upload)
  10 internal_approval (operations, yes_no_upload)
  11 send_for_production (operations, approval)
  12 project_review_authorisation (operations, yes_no_upload)   <- in-flight "Test Project" sits HERE
  13 production_process (factory_operations, checklist:production_process)
  14 confirmation (site_pm, checklist:confirmation)
  15 factory_manager_readiness (factory_manager, checklist)
  16 materials_readiness (factory_pm, readiness, dualRoles=[factory_pm,site_pm])
  17 delivery_project_check (factory_pm, checklist)
  18 approval_installation (operations, checklist)
  19 installation_readiness (site_pm, checklist)   <- DELETE
  20 sorting (site_pm, checklist:sorting)           <- FOLD into installation_process
  21 close_out (site_pm, checklist:close_out)       <- FOLD into installation_process
  22 sign_off (super_admin, ack)                    <- becomes site_pm yes_no_upload

TARGET live graph (21 steps, in order) — resolve the exact orderIndex values
empirically from the live DB inside the migration, do NOT hardcode blindly:
   1 new_project (customer_care, creation)                                  [unchanged]
   2 assign_designer_brief (design, assignment)                            [unchanged]
   3 brief_taking (design, yes_no_upload)                                  [unchanged]
   4 invoice_upload "Invoicing" (customer_care, 2-phase — see Task 2)       [role+label+kind change; drop timeline additionalKind]
   5 set_delivery_timeline "Set Delivery Timeline" (operations, timeline)   [NEW — split out of old 4]
   6 design_initiation (design, assignment)                                [was 5]
   7 kickoff_meeting (design, yes_no_upload)                               [was 6]
   8 design_stage (design, yes_no_upload)                                  [was 7]
   9 ops_design_confirmation "Assign Site PM for Site Confirmation"
       (role super_admin, requiredPosition head_of_projects, assignment,
        targetRoles=[site_pm]; KEEP stepKey ops_design_confirmation + id)   [was 8]
  10 confirmation (site_pm, checklist:confirmation)                        [MOVED from old 14]
  11 confirmation_correction (design, yes_no_upload)                       [was 9]
  12 internal_approval (operations, yes_no_upload)                         [was 10]
  13 send_for_production (operations, approval)                            [was 11]
  14 project_review_authorisation (operations, yes_no_upload)   <- "Test Project" remaps here [was 12]
  15 production_process (factory_operations, checklist)                    [was 13]
  16 factory_manager_readiness (factory_manager, checklist)               [was 15]
  17 materials_readiness (factory_pm, readiness, dualRoles)               [was 16]
  18 delivery_project_check (factory_pm, checklist)                        [was 17]
  19 approval_installation (operations, checklist)                        [was 18]
  20 installation_process (site_pm, checklist:installation_process)        [MERGES old sorting+close_out]
  21 sign_off (site_pm, yes_no_upload)                                     [was super_admin ack]

Approval engine (lib/workflow-graph.ts) — receiveApproval() throws
'approval-requires-two-parties' when state.sentBy === actorId. So the
approval kind CANNOT model same-role/same-user 2-phase invoicing.

2-part wizard precedent: the CURRENT invoice_upload already runs a 2-part
wizard driven by additionalKinds=[timeline_setting] on the live DB row (part
1 upload, part 2 timeline). This is the proven mechanism for two phases on
ONE step.

payment_confirmation StepKind still exists (lib/workflow.ts) and stepHref()
routes it to /admin/payment-confirmation; its historical purpose was
confirming payment.

Assignment engine: assignUserAction (actions/workflow-graph.ts) + assignUser();
ASSIGNEE_GATED_STEPS map (lib/workflow-graph.ts) currently:
  { brief_taking: assign_designer_brief, kickoff_meeting: design_initiation,
    design_stage: design_initiation }

Migration precedent (in-place orderIndex shift, id-preserving, idempotent,
remaps projects.currentStep + project_step_deadlines.stepN +
project_step_completions.stepN): scripts/migrate-insert-payment-confirmation-step.ts.

positions table: pgTable('positions', { slug PK text, label text }). Seed
precedent: scripts/migrate-positions-table.ts (INSERT ... ON CONFLICT DO
NOTHING). Existing slugs: head_of_operations, head_designer,
chief_production_officer. NEEDED: head_of_projects / "Head of Projects".

verify-live-workflow.ts: server-only Module._load shim; compares
getLiveWorkflowSteps() to LIVE_WORKFLOW_STEPS on n/key/label/role/kind/slug
for every step, plus a dualRole behavioural assertion for materials_readiness.

DETAIL copy (app/_components/trt-flow-diagram.tsx) — a Record<string,string>
keyed by step.key. Current entries needing edits: assign_designer_brief
("5-day target" -> 1 day), brief_taking ("5-day max" -> 2 days), invoice_upload
(rewrite for 2-phase CC invoicing), ops_design_confirmation (rewrite for
assign-site-pm), confirmation_correction (add front-page-only help text),
installation_readiness/sorting/close_out (remove keys), sign_off (rewrite for
site_pm upload). Add keys: set_delivery_timeline, installation_process.

dashboard-shell.tsx references step-title substrings 'confirmation',
'sorting' — audit after the merge so removed steps don't leave dead matches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + bootstrap/parity references</name>
  <files>scripts/migrate-workflow-restructure-batch2.ts, db/workflow-live-steps.ts, db/seed-checklists.ts, scripts/verify-live-workflow.ts</files>
  <action>
Write ONE idempotent, assert-guarded, tsx-runnable migration
`scripts/migrate-workflow-restructure-batch2.ts` (config path '.env.local',
neon + drizzle, GRAPH='live'), mirroring the id-preserving in-place style of
scripts/migrate-insert-payment-confirmation-step.ts (read that precedent fresh
first). Never delete+recreate a definition that real projects reference — only
shift orderIndex/role/label/kind/position/targetRoles/checklistSlug in place,
and INSERT genuinely-new rows.

The migration must, in one pass (transactional — no partial writes):
  1. Seed positions: INSERT head_of_projects / "Head of Projects" ON CONFLICT
     (slug) DO NOTHING (mirror scripts/migrate-positions-table.ts INSERT
     shape). Do this even if the positions table is otherwise untouched.
  2. Step 4 invoice_upload -> role customer_care, label "Invoicing",
     fulfillmentKind matching the 2-phase mechanism chosen in Task 2; drop the
     additionalKinds=[timeline_setting] (timeline moves to step 5). Keep id.
  3. INSERT new step set_delivery_timeline at orderIndex 5: role operations,
     requiredPosition=null (no position narrowing per note), label "Set
     Delivery Timeline", the timeline-setting fulfillment kind the current
     invoice_upload row uses for its part-2 (investigate the live row's kinds
     to match). Shift design_initiation/kickoff_meeting/design_stage +1.
  4. ops_design_confirmation -> role super_admin, requiredPosition
     head_of_projects, fulfillmentKind assignment, targetRoles=[site_pm],
     label "Assign Site PM for Site Confirmation" (keep stepKey + id).
  5. MOVE confirmation (site_pm checklist) to immediately after
     ops_design_confirmation and before confirmation_correction; renumber the
     intervening steps to both close the gap where confirmation used to be
     (old 14) and open its new slot.
  6. DELETE installation_readiness (assert no live project currentStep resolves
     to it — see remap guard) and its edges; rewire predecessor->successor.
  7. Replace sorting + close_out with ONE step: keep the sorting row (id
     preserved), label "Installation Process", checklistSlug
     'installation_process'; DELETE close_out + rewire edges so
     approval_installation -> installation_process -> sign_off.
  8. sign_off -> role site_pm, fulfillmentKind yes_no_upload (keep stepKey+id).
  9. Normalise orderIndex to contiguous 1..21 in TARGET order and rewire
     workflow_step_edges to the exact linear sequence (one predecessor/
     successor per step). Assert exactly one first step and one last step.
 10. In-flight remap (CRITICAL): build an explicit OLD-orderIndex -> step-key ->
     NEW-orderIndex map from the live DB BEFORE mutating. Pre-flight ASSERT:
     for every live project, resolve its currentStep OLD key; if that key was
     DELETED (installation_readiness, close_out) OR is the SPLIT boundary (old
     invoice_upload) OR is a MOVED step landing behind the project, ABORT the
     whole migration and print: "In-flight project <id> sits on a step that
     cannot be safely remapped; finish or reset it, then re-run." The known
     "Test Project" at project_review_authorisation has a surviving,
     non-split, non-deleted key shifting +2 (12->14) -> it remaps cleanly and
     the migration PROCEEDS. Apply the key-map to projects.currentStep,
     project_step_deadlines.stepN, and project_step_completions.stepN (rows on
     surviving keys -> new index; rows on removed/merged keys left as-is audit
     trail, matching the precedent's stepDefId=null handling). Do NOT use blind
     arithmetic — the confirmation MOVE and 4->(4,5) split make uniform +N
     wrong.
 11. Idempotency guard at top: if set_delivery_timeline already exists in
     graph='live', exit without changes.

Then update db/seed-checklists.ts to define the installation_process checklist
(slug installation_process) with sorting / execution / close-out sections —
reuse existing sorting + close_out template items where sensible, add execution
items as needed; follow the existing seed helper shape in that file.

Then update db/workflow-live-steps.ts LIVE_WORKFLOW_STEPS to the new 21-step
shape (n 1..21, correct key/label/role/kind/slug per TARGET order) and add a
short header note referencing this migration (follow the existing convention of
documenting DB-only fields like additionalKinds/targetRoles in comments).

Then update scripts/verify-live-workflow.ts: adjust the expected count to 21;
the n/key/label/role/kind/slug parity loop follows LIVE_WORKFLOW_STEPS
automatically; keep the dualRole assertion (materials_readiness still exists).
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit 2>&1 | grep -E "migrate-workflow-restructure-batch2|workflow-live-steps|verify-live-workflow|seed-checklists" | head; echo "tsc-scoped-check-done"</automated>
  </verify>
  <done>Migration script compiles, is idempotent + assert-guarded + transactional; db/workflow-live-steps.ts lists 21 steps in TARGET order; seed defines installation_process; verify script expects 21 steps. Migration is RUN in Task 4.</done>
</task>

<task type="auto">
  <name>Task 2: Engine + server actions (2-phase invoicing, timeline move, sign_off, gating audit)</name>
  <files>lib/workflow-graph.ts, lib/workflow.ts, actions/projects.ts, actions/workflow.ts, actions/workflow-graph.ts</files>
  <action>
FIRST investigate and CHOOSE the 2-phase invoicing mechanism (do not default
blindly): read lib/workflow-graph.ts's approval path (sendApproval /
receiveApproval — receiveApproval throws 'approval-requires-two-parties' when
sentBy===actorId, which BLOCKS a same-role invoicing flow), the existing
2-part wizard driven by additionalKinds on the current invoice_upload row, and
the payment_confirmation StepKind + stepHref route. Pick the cleanest mechanism
that ALREADY exists. Recommended default: reuse the proven 2-part wizard
pattern (single invoice_upload step, phase 1/2 "I have sent this client the
invoice" + invoice upload, phase 2/2 "The client has finally paid") — it avoids
the approval two-party block and matches how this step already worked. In the
code path that completes phase 2/2, set projects.paymentStatus='paid'
(transactional with step completion; projects.paymentStatus already exists).
Record the chosen mechanism in a code comment on the handler.

Move the timeline-setting behaviour to the new step 5: setInvoiceTimelineAction
(actions/projects.ts) currently targets the merged step 4 — retarget it to
resolve by stepKey 'set_delivery_timeline' (not a hardcoded orderIndex). Read
the current action fresh; keep its date/timeline logic, only change which step
it completes. Add a +1 day auto-deadline for the ops-timeline step at creation
(note: ops timeline deadline becomes +1d; the old merged step got +2d) — follow
the existing auto-deadline creation pattern already in actions/projects.ts
(grep the current deadline-setting logic and reuse its date helper). Keep the
invoicing step 4 deadline behaviour as-is (owner said keep +2d).

sign_off is now site_pm yes_no_upload: confirm the generic yes_no_upload path
handles it and nothing special-cases sign_off as a super_admin ack. Grep for
'sign_off' and completeAckStepAction; only remove/guard ack-specific handling
if a hardcoded reference pins this step to ack/super_admin.

Gating audit (do NOT change brief_taking/kickoff/design_stage governance):
grep ASSIGNEE_GATED_STEPS and every reference to the changed/removed step keys
(ops_design_confirmation, confirmation, installation_readiness, sorting,
close_out, sign_off) across lib/ and actions/. Confirm nothing pins a removed
key (installation_readiness, close_out). The new step 8 assigns a site_pm for
the step-10 'confirmation' checklist — if the engine follows an assignment->
gated-step pattern (as design_initiation gates kickoff/design_stage), add
`confirmation: 'ops_design_confirmation'` to ASSIGNEE_GATED_STEPS so the
assigned site_pm owns the confirmation checklist. Only add this if it matches
the existing pattern; otherwise leave gating untouched and note why in a
comment.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit 2>&1 | grep -E "workflow-graph|actions/projects|actions/workflow|lib/workflow" | head; grep -rn "paymentStatus.*paid\|set_delivery_timeline" actions/ lib/ | head; echo "task2-check-done"</automated>
  </verify>
  <done>2-phase invoicing sets paymentStatus='paid' on phase 2/2; setInvoiceTimelineAction targets set_delivery_timeline with +1d auto-deadline; sign_off handled as yes_no_upload; gating audit complete with no dead/removed-key references. tsc clean for touched files.</done>
</task>

<task type="auto">
  <name>Task 3: UI copy + flow-diagram DETAIL + gating-substring audit</name>
  <files>app/_components/trt-flow-diagram.tsx, app/_components/dashboard-shell.tsx</files>
  <action>
Update the DETAIL Record in app/_components/trt-flow-diagram.tsx for every
changed step (read the file fresh; DETAIL is keyed by step.key):
  - assign_designer_brief: change "5-day target" -> "1 day" (SLA fix).
  - brief_taking: change "5-day max" -> "2 days" (SLA fix).
  - invoice_upload: rewrite for the new CC 2-phase Invoicing — phase 1/2
    "customer care confirms the invoice has been sent to the client (uploads
    it)", phase 2/2 "customer care confirms the client has paid, marking the
    project paid". Remove any timeline language (timeline moved to step 5).
  - set_delivery_timeline: ADD a new entry — "Operations sets the overall
    delivery date and a deadline for every remaining step (1-day target)."
  - ops_design_confirmation: rewrite — "Head of Projects assigns a Site PM to
    carry out the site confirmation."
  - confirmation: keep/adjust copy for the site_pm confirmation checklist (now
    earlier in the flow).
  - confirmation_correction: ADD front-page-only help text — the designer
    re-uploads only the FRONT PAGE of the drawing (per note item 4).
  - installation_process: ADD a new entry describing sorting, execution and
    close-out as one on-site checklist.
  - sign_off: rewrite — "Site PM uploads the signed-off document to close the
    project."
  - REMOVE the installation_readiness, sorting, and close_out DETAIL keys.

Audit app/_components/dashboard-shell.tsx: it matches step-title substrings
('confirmation', 'sorting', etc.). After the merge, ensure removed steps leave
no dead/incorrect matches — update the 'sorting'/'close_out' substring logic to
'installation process' where it drove behaviour for those steps, and confirm
the 'confirmation' match still resolves to the correct (relocated) step. Make
only the minimal edits needed to keep behaviour correct — do NOT restructure
the component.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && grep -n "set_delivery_timeline\|installation_process\|front page\|1 day\|2 days" app/_components/trt-flow-diagram.tsx | head; grep -c "installation_readiness\|'sorting'\|close_out" app/_components/trt-flow-diagram.tsx; npx tsc --noEmit 2>&1 | grep -E "trt-flow-diagram|dashboard-shell" | head; echo "task3-check-done"</automated>
  </verify>
  <done>DETAIL has entries for set_delivery_timeline + installation_process, front-page help on confirmation_correction, SLA copy fixed to 1/2 days, and no installation_readiness/sorting/close_out keys remain; dashboard-shell substring logic updated; tsc clean for touched files.</done>
</task>

<task type="auto">
  <name>Task 4: Run migration + full verification gate</name>
  <files>(none — runs migration + gate commands)</files>
  <action>
Run the migration against the live DB, then the full gate. Migration first
because verify:live-workflow and build assert the live shape.
  1. npx tsx scripts/migrate-workflow-restructure-batch2.ts  (must print
     idempotent success; re-run once to confirm it is a no-op the 2nd time).
  2. npx tsc --noEmit
  3. npm run lint
  4. npm test
  5. npm run verify:live-workflow  (parity + behavioural assertions vs the new
     21-step shape must PASS)
  6. npm run build
If verify:live-workflow reports a parity mismatch, reconcile
db/workflow-live-steps.ts (Task 1) with what the migration actually wrote — the
DB is the source of truth; the bootstrap array must match it exactly on
n/key/label/role/kind/slug. If the migration's pre-flight abort fires (in-flight
project on an unmappable step), STOP and report to the owner that the "Test
Project" must be finished or reset before the restructure can proceed — do not
force the migration past its guard.
  </action>
  <verify>
    <automated>cd /Users/uzochukwuamara/Code/PayMeBlue/trt-project-manager/trt-pm && npx tsc --noEmit && npm run lint && npm test && npm run verify:live-workflow && npm run build && echo "FULL-GATE-PASS"</automated>
  </verify>
  <done>Migration applied idempotently; tsc, lint, test, verify:live-workflow, and build all pass; live graph is the 21-step TARGET shape and the in-flight project is correctly remapped (or the migration cleanly aborted with owner instructions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action -> live DB | Step completions / payment-status writes cross here; must be authorized + transactional |
| migration script -> live production data | Rewrites real workflow_step_definitions/edges and remaps a live in-flight project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qe4-01 | Tampering | migration remap of live in-flight project | mitigate | Key-based remap + pre-flight abort guard; transactional single-pass; idempotency guard; re-run must be a no-op |
| T-qe4-02 | Elevation | step 4 phase 2/2 sets paymentStatus='paid' | mitigate | Payment mutation only via authorized step-completion path (authorizeStep), transactional with completion; customer_care-owned |
| T-qe4-03 | Elevation | assign-site-pm step (head_of_projects) | mitigate | requiredPosition=head_of_projects on an assignment-kind step; engine authorizeStep enforces role+position |
| T-qe4-04 | Repudiation | removal of sorting/close_out/installation_readiness completions | accept | Historical completion rows on removed keys retained as audit trail (stepDefId=null precedent); no destructive delete of completion history |
| T-qe4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced by this plan; no install tasks |
</threat_model>

<verification>
- npx tsc --noEmit passes
- npm run lint passes
- npm test passes
- npm run verify:live-workflow passes against the 21-step shape
- npm run build passes
- Migration is idempotent (second run is a no-op) and did not corrupt the
  in-flight "Test Project" (currentStep remapped 12 -> 14)
</verification>

<success_criteria>
- Live graph is the exact 21-step TARGET order in <interfaces>
- Invoicing (CC, 2-phase, sets paymentStatus='paid') and Set Delivery Timeline
  (operations, +1d auto-deadline) are distinct steps 4 and 5
- Assign Site PM (head_of_projects, assignment, targetRoles=[site_pm]) precedes
  the relocated site_pm confirmation checklist, which precedes
  confirmation_correction (front-page-only help text)
- installation_readiness removed; sorting+close_out folded into one
  installation_process checklist; sign_off is a site_pm yes_no_upload
- positions table has head_of_projects
- SLA copy fixed (1-day assign / 2-day brief / 1-day ops timeline)
- Full gate green; do NOT touch positions-rename files, audit page, or the
  Configurator reorder UI
</success_criteria>

<output>
Create `.planning/quick/260714-qe4-workflow-graph-restructure-batch-2-invoi/260714-qe4-SUMMARY.md` when done.
</output>
